# Aevatar 工作流验收案例

[![验证工作流](https://github.com/eanz17/aevatar-workflow-acceptance-cases/actions/workflows/validate.yml/badge.svg)](https://github.com/eanz17/aevatar-workflow-acceptance-cases/actions/workflows/validate.yml)

本仓库采用“29 个 workflow + 3 个 Lark channel E2E case + 21 个风险验收 case”的口径：29 个公开、安全、可复现的 Aevatar workflow 与 Ornn skill 一一对应；channel case 独立验证 Lark 回调；risk case 验证 catalog、Assistant、源定义、runtime 原语、准入负例和报告一致性。案例覆盖基础原语、文件与图片输入、Lark Bot 入站附件、Base、Lark 审批、Lark 消息、contact、通用代码执行、`codex_exec`、schedule、NyxID provider receipt、typed tool approval resume、inline `workflow_call`、确定性 `parallel`、确定性 `race` 和发票 no-submit 审批路由预览等能力。

公开 YAML 只保存占位符和合成数据，不包含组织专属 Base、用户、审批、NyxID 资源标识或未脱敏运行 ID。工作流 `name`、步骤 `id`、工具名、API 字段、错误码等技术契约保留英文，其余说明使用中文。

## 当前结论

验证基线日期：2026-08-05；状态更新：2026-08-07。

> **最新全量 fresh 快照（2026-08-07 13:15-13:49 SGT）：**本地与 production preview 均为 29/29；direct runtime 为 25 passed、1 platform-blocked、3 skipped；Assistant 代表集为 4 validated、1 chat-failed；Ornn 版本为 28/29 一致；Lark channel 为 2 passed、1 start-blocked。非绿项是 Case 11 `codex_execution_capacity_unavailable`、Assistant Case 15 `STREAM_FAILURE`、`supplier-risk-tier-aggregation` public 版本 1.0 落后本地 1.1，以及 Channel Case 22 `InvalidWorkflowYaml`。Cases 06/24/25 和 schedule 本轮未获副作用授权，未 fresh 执行。窗口后 13:56 在 Ready `5f51f6d0`、14:34 在当前 Ready `eead35c0` 两次复验 Case 11，均 committed `failed` 且错误码不变；最新 run hash `106ecf7b750a`，不改写全量汇总。另据 [#3290](https://github.com/aevatarAI/aevatar/issues/3290)，源 P2 在 14:09 完成 14/14 并与现行系统复算一致后，14:18 同定义出现 `NYXID_PROXY_HTTP_502`，14:20-14:42 byte-identical preview 连续 5 次 `NYXID_ADMISSION_SOURCE_CREDENTIAL_REQUIRED`；P1 在第 1/27 步 `Forwarding failed`。该 issue 证据不回算 fresh 分母，但两条迁移主链当前均为 `regression-blocked`。详见 [当前状态报告](report/2026-08-07-current-status-report.md) 与 [脱敏机器快照](validation/full-revalidation-2026-08-07.json)。下方长期结论和日志保留各自时间点，不覆盖这次 fresh 结果。

- 29/29 个 workflow 通过本地 YAML、步骤图、安全边界和专用契约校验；`production_validate.rb` 的动态 registry 已同步覆盖 29/29 个严格业务 artifact contract。新增 26/27/28 分别钉住 inline `workflow_call`、三路 deterministic `parallel` 和三路 deterministic `race`；新增 29 使用合成 PDF 覆盖附件提取、审批历史只读窗口、contact 只读路由和 no-submit typed artifact，全部禁止外部写入。
- 29/29 个已配置定义通过 Aevatar 主网 `interactive` explicit-request preview；当前直接 runtime 严格结果为 28/29，唯一非绿项为 Case 11 的外部 managed execute capacity blocker。Case 12 已在祖先 Ready `6558db8d` 恢复：UserService 独立回读为 `forward=true`、`inject=true`、scope=`proxy:*`，NyxID `/execute` 固定合成探针 6 秒完成；fresh direct run `6659aabee079` committed `completed`、state 31、4/4，严格命中 `structured_receipt=true`、`total_cents=16623`、`side_effects=false`。祖先 Ready `49cb76e4` 的 fresh Lark run `ebe0e10241f0` 又 committed `completed`、state 33、4/4，并命中同一严格 artifact，证明 Ornn 搜索、skill mount、callback、workflow start、普通 sandbox 和 Bot reply 当前可用。同部署首次 run `c257460dc47a` 的 HTTP 524 保留为瞬时 capacity 对照。Case 29 的审批历史 GET 与 contact `batch_get_id` POST 均精确为 `read_only`、`approvalRequired=false` 和 `interactive`；fresh run `a729912ee5d9` committed `completed`、state 77、11/11，15 项 no-submit artifact 全部精确命中，0 approval、0 resume、0 side effect。旧 01-10、13-20 保留既有 committed 基线；21/22/23 在用 `config.local.yaml` 重新物化后 fresh committed `completed`，分别为 7/7、8/8、4/4 且 artifact 精确命中。此前三项同时出现的 `NYXID_PROXY_HTTP_400` 已确认来自共享 `build/workflows/` 被示例配置覆盖，不是 Aevatar 回归；24/25 保留最近一次 5/5、8/8 completed 的 typed approval 证据。新增 26/27/28 已在 Ready `4c0596c7` 上完成 3/3 preview 与 3/3 committed `completed`，分别为 5/5、14/14、6/6，严格 artifact 全部命中且无副作用。
- 新增 21/22/23 无副作用；24/25 在本轮显式授权和 typed approval 下分别完成 1 次、2 次 resume。本轮 Cases 05/24/25 共创建 4 条固定合成 Base 探针记录，随后连同 2 条同契约历史残留精确清理；回读匹配数为 0，未匹配记录未触碰。
- 3/3 个 Lark channel E2E case 已严格通过。Ready 镜像 `de801ca7` 上，Case 20 的显式挂载先完成唯一 mount 审批与同一 AgentRun 恢复，再完成唯一 workflow 工具审批；新 run `e18081f2211b` committed 3/3、state 30 且脱敏 artifact 精确命中。Case 21 的唯一 mount 卡被拒绝后返回 typed `Denied` / `approval_denied`，mount、workflow start 与目标 run 增量均为 0。Case 22 的既有挂载 workflow 审批链继续通过；旧 `ee031038` / `InvalidWorkflowYaml` 结果只保留为历史负证据。
- Regression R01 的 local fault-injection 与 fresh Lark `/init` 生产层均通过。Ready `4c0596c7` 包含所需提交；唯一 `/init` 消息得到 1 条授权更新回复、重复回复为 0，关联日志窗口未出现 `Activation access violation` 或 `CommittedStatePublicationException`。未打开 OAuth 链接、未改变绑定，公开证据未保存 Lark 或 callback 标识。
- 案例 19 public skill 已升级并精确回读为 `1.1`。Direct synthetic fixture run hash `e6d17331400b` 为 committed `completed`、`stateVersion=30`、4/4；fresh Lark canary 在 6 条既有目标 run 的隔离基线上只新增 1 条，run hash `03c3f4ded68e` 为 committed `completed`、`stateVersion=32`、4/4，typed artifact 精确命中 `lark_bot_ingress_validated=true`、文件名/正文/SHA-256、脱敏和无副作用断言，严格状态升级为 `validated`。Lark 文件卡片为 114 字节，committed descriptor/extraction 因尾随 LF 归一化为 113 字节；历史 `service_catalog_missing` blocker 已关闭，Risk 24 严格通过。
- 本地与 Ornn 线上 29/29 个 skill 已完成结构、服务端格式、精确名称、版本和 public 回读；Case 26 包内同时携带父定义和固定 inline 子定义。本轮 missing-only 只新增并公开 `invoice-approval-routing-preview` 1.0，既有 28 项全部精确匹配并跳过；正式 verify-only 与独立 catalog 均为 29/29，missing/private/version mismatch 为空。
- `/api/chat` 的历史 Ready `ee031038` 基线仍为 01、12、13、14、15 共 5/5 `validated`。最近代表证据为 2/2：Case 01 run `535e9029486f` 保留 13/13 committed artifact；Ready `6558db8d` 上 Case 12 run `6fa89cd62b15` fresh 完成 Ornn search、精确 mount、workflow start 与 artifact read，committed `completed`、state 31、4/4，严格 `workflowValidationStatus=validated`、`caseValidated=true`。Ornn、`/api/chat` 和普通 sandbox execution authority 当前均可用。
- 案例 15 又在生产镜像 `d7844b5e` 上完成 artifact actor identity 回归：Assistant 读取到 committed typed artifact 并明确报告 `Completed`，没有再把最终结果误报为 pending。
- 历史五个代表案例均按 `ornn_search_skills -> use_skill -> mount approval -> aevatar_start_workflow -> committed observation` 到达可判定终态；当前 Case 01 与 Case 12 都严格通过，均未出现重复 tool start call ID。
- 当前 Ready `eead35c0`（完整提交 `eead35c089758b26f7b0fd4c277dbbe71815b0cc`，1/1 Ready、0 restart）包含 bounded upstream body 与 committed safe code 两轮诊断修复；固定 managed `codex_exec` fresh run `106ecf7b750a` 仍 committed `failed`、state 31、4/4，稳定错误为 `codex_execution_capacity_unavailable`，无 final artifact、approval、resume 或副作用，本次未观察到 allowlisted upstream code。此前 Ready `6558db8d` 的 run `39a374f8815a` 及 `6a656d75` 的 `705a69901de5`、`4caa726585f6` 同码失败，有界日志将最近 transport 边界确认为 NyxID POST HTTP 502 / `managed_proxy_unavailable`。同一 UserService health 为 healthy/OpenSandbox connected，普通 `/execute` 与祖先 Ready 的 fresh Lark Case 12 也已绿色，但都不证明 managed execute capacity。历史镜像 `f7f543c5` 的 30/30 恢复证据继续保留，不能覆盖当前 blocker。
- 本轮 14 和 17 均真实进入 `awaiting_tool_approval`，验证器从 SSE 取得完整 typed identity 后各 resume 一次，最终 committed 3/3、4/4 且 artifact 命中；旧的“当前运行未观察到 per-run pending/resume”结论已被 fresh 证据取代。
- 历史生产曾验证 scope 特定的 sandbox bearer 转发修复：单步 `code_execute`、可访问 Base 的 P2 no-send 同类链、exact P1 v5 sanitized image + `submit=false` 均 committed completed。#3290 证明这些历史成功不能外推为当前时间连续性：同一 P2 定义在 14/14 后出现 runtime 502 与准入 credential failure，P1 又出现 actor forwarding failure。Aevatar 提交 `6558db8db` 取消 managed readiness 对 `forward_access_token=false` 的过度约束，同时继续强制 `inject=true` 与 scope=`proxy:*`；UserService 回读为 `true/true/proxy:*`。Managed transport 仍精确使用 `X-API-Key` 且不发送 `Authorization`；Case 12 direct、NyxID 最小 probe 与 `/api/chat` 的历史/fresh 成功仍有效，但都不能证明 source workflow 的跨 silo continuity。既有 PDF attachment probe 2/2 completed 证据继续有效。
- Durable schedule 已在 Ready `4c0596c7` fresh 重跑完整闭环：1 个 workflow / 6 个 read-only GET 的 `confirmation_required`，typed `accepted/pending_binding`，binding committed `succeeded`（state 7）、provisioning committed `succeeded`（state 11，attempt 2）；owner-scoped detail 中 schedule enabled，每分钟 Cron 在 `Asia/Singapore` 自然触发 1 次，workflow run hash `b9859494e2a9` 为 11/11 committed `completed`、state 73，六路 Base、周/月预算、分类计数与 `side_effects=false` 全部精确命中。Typed DELETE 返回 `accepted/pending`，随后 detail 不存在、owner list 为 0，跨下一分钟 run count 保持 `1 -> 1`。本轮不持久化 confirmation token 或原始 member/schedule/operation/command ID。
- 历史镜像 `b010ba61` 的六次 Cron 闭环证据继续保留；相关修复提交 `748f98e7d`、`7a7781067` 和 `b010ba614` 已包含在当前部署历史中。该历史验证为 HTTP 200 `confirmation_required` -> HTTP 202 typed `pending_binding` receipt -> binding/provisioning committed success；六次真实 cron 均完成，`fireCount=6`、`failureCount=0`，DELETE 后 list 为空且 run count `6 -> 6`。`f7f543c5` 上的 `NyxIdOperationAuthorityContractUnavailable` 只作为历史回归背景保留；POST、WRITE 与 DESTRUCTIVE 仍 fail-closed。编译修复提交 `b010ba614` 的 Release publish、真实 `linux/amd64` Docker build、镜像内 `.NET 10.0.10 linux-x64`、完整架构/稳定性门禁和排除 3 个本机 Redis 版本契约用例后的 solution tests 均通过。
- `~/workflows` 中除 n8n 外的 41 个可解析定义已按 7 个版本族比较；剩余边界包括受安全约束未运行的源发送、审批和排程定义，以及现有 case 未覆盖的重新绑定、member/部署变化和 silo relocation 时间连续性。公开案例 15 的 Durable schedule 已通过，不替代源财务 schedule 或 #3290 暴露的 source credential/forwarding continuity。
- 风险验收 21 个 case 的 fresh 严格汇总为：16 passed、0 blocked、0 failed、0 pending-execution、4 not-configured、1 skipped-expired。Risk 26 由 Case 12 的 fresh `/api/chat`、direct run 与 UserService `/execute` 三层严格成功关闭；Risk 23 在 Ready `4c0596c7` 上取得 fresh Lark workflow 运行期拒绝证据；Risk 37/38/39 分别由 Cases 26/27/28 的 inline definition、deterministic parallel 与 deterministic race committed artifact 关闭。Risk 42 保留 v2 源定义、SHA 和旧验收要求，但其无 capability 的 `code_execute` 与硬编码集成契约已由严格通过的 Case 29 原生 no-submit 链替代；其余 4 项仍是明确的安全配置缺口。该 21-case 集合没有时间连续性 case：Risk 28 只证明过单次 P1 14/14，Risk 29 尚未配置，Risk 40 与 Risk 41 只约束副作用目标，均不能覆盖 #3290。
- 已检查 #3161 作者此前在 `aevatarAI/aevatar` 提交的全部 11 条 issue，并用 13、15、16 做新一轮只读 committed 回归；没有为 channel/scheduler 外层缺口复制无效 workflow。详见 [定向回归报告](report/2026-08-05-issue-3161-author-regression.md) 与 [机器摘要](validation/issue-3161-author-regression-2026-08-05.json)。

`preview`、`202 Accepted`、Assistant 正常结束、模型文案和 pending artifact 都不等于 workflow 成功。逐案例证据见 [历史生产验证摘要](validation/production-validation-2026-08-05.json)、[21-25 生产证据](validation/production-validation-2026-08-06-cases-21-25.json)、[26-28 生产证据](validation/production-validation-2026-08-07-cases-26-28.json) 与 [风险机器摘要](validation/risk-validation-2026-08-06.json)，完整对比见 [分析页面](report/index.html)。

### 本轮验证日志

| 时间（SGT） | 验证层 | 目标 | 结果 | 证据 / blocker | 下一步 |
|---|---|---|---|---|---|
| 2026-08-07 15:10 | Case 11 managed API key 与 service allowlist 只读检查 | NyxID API key inventory；当前 `chrono-sandbox` / `chrono-llm-public` | 通过；managed credential lifecycle 不是当前 blocker | 精确找到 1 个名称契约匹配且绑定当前 sandbox 的 managed key，ID hash `3cffd111e4a0`；key active、未过期、scope=`proxy`、`allow_all_services=false`，allowed service 恰为 2 个。两项绑定均 active/connected，sandbox hash 仍为 `872833a52fc1`；key `last_used_at` 精确命中 14:34 Case 11 请求窗口，证明 Aevatar 已使用当前 key 到达 NyxID。未输出 key、prefix、原始 ID 或 endpoint | Fresh `/codex/execute` admission 路由证据已变化；先对固定 Case 11 做 explicit-request preview，仍满足 canonical contract 后只运行一次 runtime，并以 committed terminal 与严格 typed artifact 判定 |
| 2026-08-07 15:07 | Case 11 Aevatar X-API-Key 与 NyxID bearer forwarding 源码归因 | Aevatar Ready `eead35c0` 对应源码；本地 NyxID proxy handler | 排除 `forward_access_token=true` 导致 managed 下游 Authorization 的假设 | 当前 Aevatar `ProxyRequestBoundedWithApiKeyAsync` 只设置 `X-API-Key`，回归测试精确断言 `Authorization=null` 且 API key 不进入 body。NyxID `caller_bearer_token_for_downstream` 只从入站 `Authorization: Bearer` 提取 caller token；X-API-Key 请求没有该 header，因此即使 UserService `forward_access_token=true` 也不会向 chrono-sandbox 注入 `Authorization`。Direct 用户探针的 401 是 bearer 路径专属 fail-closed，不解释 Case 11 的 HTTP 502；无需拆分 UserService 或回退 `forward=false` | 只读检查 managed API key 的 lifecycle、UserService allowlist、最近使用与 proxy/audit 终态；若 key/binding 正常，则把修复边界推进到 NyxID proxy/upstream 502，而不是继续修改 Aevatar transport |
| 2026-08-07 15:05 | Case 11 同路由无副作用 admission 最小探针 | 唯一 personal `chrono-sandbox`；NyxID `POST /codex/execute`；故意无效合成请求 | 路由可达；HTTP 401 typed fail-closed；未尝试 sandbox creation | 首次未声明 JSON content type，在 Actix extractor 以 HTTP 400 `Content type error` 结束；补齐 `Content-Type: application/json` 后，同一路由稳定返回 `CODEX_CALLER_CREDENTIAL_FORWARDED` / HTTP 401。部署版 chrono-sandbox 在 delegation 验证后、请求校验和 semaphore/sandbox creation 前拒绝下游 `Authorization`；当前 UserService 为 `forward=true`、`inject=true`、scope=`proxy:*`。两次请求均未进入执行或创建 sandbox，未产生副作用。该证据证明 direct user bearer 路径可达但按安全契约被拒绝，尚不能外推 Aevatar X-API-Key managed 路径 | 对照本地 NyxID API-key proxy 实现与 Aevatar bounded client，确认 `forward_access_token=true` 在 X-API-Key principal 下是否注入下游 `Authorization`；若会注入则修 credential/service 分离，若不会则继续定位 502 的 node/upstream body 边界 |
| 2026-08-07 14:56 | #3290 源迁移连续性证据与 case gap 复核 | P2 同一 member/binding/definition；P1 既有 27 步 binding；当前 production pod/log | 两条迁移线当前 `regression-blocked`；现有 case 未覆盖时间/silo 连续性 | Issue 记录 P2 14:09:19 为 14/14 completed 且复算一致，14:18:27 为 7/8 / `NYXID_PROXY_HTTP_502`，14:20-14:42 同一 preview 连续 5 次 `NYXID_ADMISSION_SOURCE_CREDENTIAL_REQUIRED`；P1 为 0/27 / `Forwarding failed`。同期 sandbox direct via NyxID 200、proxy/token/read plane 正常。14:55 只读检查的 `eead35c0` 为 1/1 Ready、0 restart，但 Pod 14:28:33 才启动，90 分钟 stdout 仅 10 行、相关命中 0，不能覆盖 incident 旧 pod/silo 窗口 | 仅同步脱敏机器证据、Markdown/HTML 与 report validator；不修复、不重跑。后续新增连续性 case 时要求 same exact source 在 rebind/member/rollout 前后两次 preview+run、双 credential 分离、P2 14/14 复算、P1 forwarding/27 步和脱敏 deployment/silo 证据 |
| 2026-08-07 14:53 | Heartbeat Case 11 状态变化门禁 | production deployment、真实 `origin/feature/integrate`、唯一 personal `chrono-sandbox` | 无状态变化；按门禁未 preview、未重跑 Case 11 | 远端 tip 仍为 `eead35c089758b26f7b0fd4c277dbbe71815b0cc`；deployment generation/observed `2741/2741`、image `eead35c0`、desired/updated/ready/available 1/1/1/1，唯一 Pod Running/Ready、0 restart。UserService hash `872833a52fc1` 仍为 active/connected/personal、`forward=true`、`inject=true`、scope=`proxy:*`；同一服务只读 `/health` 仍为 healthy、`opensandbox_connected=true`，不构成 sandbox creation capacity 新证据。近 20 分钟 production stdout 对 managed/codex/capacity 精确筛选 0 命中；未把空日志当作恢复 | 保留 `codex_execution_capacity_unavailable` 唯一 active blocker；继续等待新的 Ready/remote 或明确 managed execute capacity 证据，状态变化后先 preview、再单次 runtime，并以 committed terminal 与严格 typed artifact 判定 |
| 2026-08-07 14:52 | Case 11 历史成功与当前失败的跨仓源码时间线 | Aevatar `f7f543c51..eead35c08`；chrono-sandbox deployed source `1e8134d`；NyxID 本地源码 | 排除本轮 Aevatar request-contract 回归；确认 health/capacity 是不同证明面 | Case 11 最后一次严格成功实际为 2026-08-06 00:31 SGT：Ready `f7f543c5`、run `fa77c0c49035`、30/30、`CODEX_EXEC_READY` 和五项 gate；不是今天数小时前。Aevatar managed transport 对照显示请求仍使用同一 `X-API-Key` bounded proxy、service/path、POST body 和 timeout，新增变化只读取 bounded 非 2xx body并传播 allowlisted code。chrono-sandbox `/health` 只请求 OpenSandbox 根 `/health`；`/codex/execute` 另有进程内 concurrency semaphore，并实际 POST 创建 sandbox，分别可返回 `CODEX_CAPACITY_UNAVAILABLE` 429 或 OpenSandbox/create 502。普通 Case 12/Lark 绿色走 `/execute`，不能证明该 managed 路径 | Aevatar 当前没有可继续修的请求语义证据；保持 fixed Case 11 不变。等待 managed semaphore/OpenSandbox sandbox-creation 或 NyxID proxy 状态出现可验证变化，再先 preview、后单次 runtime；若新 run 返回 allowlisted `managed_upstream_codex_*`，按精确 producer 继续修复 |
| 2026-08-07 14:48 | Case 11 当前证据同步后的 risk/report 一致性门禁 | 两份 production/full-revalidation JSON、21 个 risk case、Markdown/HTML、report validator | 通过；Case 11 继续严格阻塞 | 两份 JSON 均通过 `jq empty`，`scripts/validate_report.rb` 语法通过；`validate_risk_cases.rb` 为 16 passed、0 blocked、0 failed、0 pending-execution、4 not-configured、1 skipped-expired；`validate_report.rb` 通过 workflow=29、risk=21 和当前 `eead35c0` / `106ecf7b750a` / 祖先 Lark Case 12 交叉断言；`git diff --check` 通过。门禁没有把 `codex_execution_capacity_unavailable` 改写为成功 | 不在无状态变化时重跑 Case 11；核对历史成功与当前失败的时间线和代码/外部容量边界，明确可修范围与恢复条件 |
| 2026-08-07 14:47 | Case 11 状态变化门禁与报告 current-evidence 同步 | production deployment、真实 `origin/feature/integrate`、production validation/report validator | 无生产状态变化；未重跑 Case 11 | 远端 tip 仍精确为 `eead35c089758b26f7b0fd4c277dbbe71815b0cc`；deployment generation/observed `2741/2741`、image `eead35c0`、desired/updated/ready/available 1/1/1/1，唯一 Pod Running/Ready、0 restart。没有新的 deployment、remote 或 managed execute capacity 证据，故没有 preview/runtime。机器摘要、Markdown/HTML 已使用当前 image/run；报告校验器中的 current Case 11、current deployment 和 29-case 页面断言同步到 `eead35c0` / `106ecf7b750a`，祖先 `49cb76e4` 的 Lark Case 12 严格断言保留 | 运行 production JSON、risk/report 和 diff 一致性门禁；若通过，保持 Case 11 唯一 blocker，等待明确 managed sandbox creation capacity 或新的可追溯部署证据 |
| 2026-08-07 14:36 | Case 11 fresh failure 日志、NyxID 与 chrono-sandbox 契约归因 | Ready `eead35c0`；run hash `106ecf7b750a`；唯一 personal `chrono-sandbox` | 确认 managed execute capacity / 上游错误体不可见阻塞；不是 Ornn/chat | fresh run 后 5 分钟按 transport、bounded proxy、admitted executor 与 `/codex/execute` 精确筛选 production stdout 均 0 命中；10 分钟窗口总日志仅 10 行，空匹配不能证明调用未发生。UserService hash `872833a52fc1` 为 active/connected、`forward=true`、`inject=true`、scope=`proxy:*`；同一 service 的只读 health 为 healthy、`opensandbox_connected=true`。部署版 chrono-sandbox 源码确认 `/health` 只探测 OpenSandbox 连接、不探测 sandbox creation capacity；502 契约使用小型 `error.code=CODEX_*` JSON，NyxID 当前 proxy 源码原样转发 buffered 非 2xx body。本次 committed 仍为通用码，说明 Aevatar 实际未取得 allowlisted code，不能据此修改 case 或重试制造绿色 | 不再运行 Case 11，直到出现明确 managed execute capacity 状态变化或可追溯上游诊断；同步本次 Ready/run 证据到 validation 与报告，保持唯一 active blocker |
| 2026-08-07 14:34 | Case 11 committed 诊断修复部署 fresh runtime | Ready `eead35c0`；run hash `106ecf7b750a` | committed `failed`；仍未闭环 | preview 通过后仅启动一次 fixed probe；committed read model 为 `failed`、state 31、4/4，失败仍为 `codex_execution_capacity_unavailable`，无 final artifact、approval、resume 或副作用。该结果不能判定新 resolver 未生效：若 transport 未取得 allowlisted `CODEX_*` body，安全设计仍应回退通用码 | 不重跑；立即查询本次精确窗口的 transport、bounded proxy 与 admitted executor 受限 Warning，区分 `managed_proxy_unavailable`、`managed_upstream_codex_*` 被后续折叠和完全无日志三类边界 |
| 2026-08-07 14:33 | Case 11 committed 诊断修复部署 production explicit-request preview | Ready `eead35c0`；scope hash `237314c29964` | 通过 | 单独 Case 11 preview 为 `runEnabled=false`、`inlineWorkflowCount=0`、`callSiteCount=0`，未 provision、未启动 workflow、无 approval 或副作用；production validator 输出已脱敏 | 使用同一 Ready、scope/team 和 production materialization 仅启动一次 Case 11；等待 committed terminal 并严格断言 typed artifact |
| 2026-08-07 14:30 | Case 11 新 Ready 复验前 production 物化与身份检查 | `config.local.yaml` -> `build/workflows-local`；NyxID auth/team read model | 通过，29/29 | 29 个 workflow 在独立 ignored 目录重新生成并全部 `YAML.safe_load`；完整 validator 为 29/29、16 个占位符。Case 11 仍为 32 步、0 external call、唯一 `codex_exec`，参数精确为 managed sandbox/empty git/固定 prompt/180 秒且 step timeout 360000 ms。NyxID auth 顶层/session scope 一致，scope hash `237314c29964`；本地 team 在对应 roster 中精确 active 匹配 1 条，team hash `db2102f24ba6`；未输出原始标识或 replacement | 在 Ready `eead35c0` 上单独执行 Case 11 explicit-request preview；确认 `runEnabled=false` 后再运行一次 runtime |
| 2026-08-07 14:29 | Case 11 committed 诊断修复 production rollout | image `eead35c0`；commit `eead35c089758b26f7b0fd4c277dbbe71815b0cc` | Ready，可追溯包含修复 | deployment generation/observed `2741/2741`、desired/updated/ready/available 1/1/1/1、unavailable 空，Available/Progressing 均 true；唯一新 Pod Running/Ready、0 restart；新 ReplicaSet 1/1，旧 `61a1f8c6` 已缩至 0。镜像短 tag 与修复 commit 前缀精确一致，且该提交是当前真实 `origin/feature/integrate` 祖先 | 重新物化 production definitions 并做本地 Case 11 固定契约检查；随后单独执行 explicit-request preview，通过后仅运行一次 Case 11 runtime |
| 2026-08-07 14:28 | Case 11 committed 诊断修复部署轮询 3-7 | production deployment / registry | 等待部署，无状态变化 | 连续低频回读均为 generation/observed `2739/2739`、updated/ready/available 1/1/1、image `61a1f8c6`；`eead35c08` manifest 尚未出现。现役实例无 unavailable，未运行 Case 11 | 继续按当前发布链历史延迟低频等待；出现新 image 后检查 deployment、ReplicaSet、Pod 三面 Ready 证据 |
| 2026-08-07 14:22 | Case 11 committed 诊断修复发布链只读归因 | GitHub Actions、Docker registry、当前 Ready 基线 | 等待外部发布，未发现失败 rollout | `feature/integrate` 远端仍为 `eead35c08`；仓库 GitHub Actions 最近 run 不包含本提交，符合既有 branch/path trigger；registry 尚无 `aevatar-console-backend:eead35c08` manifest。当前 Ready `61a1f8c6` 的提交时间为 13:42、Pod 创建于 14:05，表明外部发布链存在约二十分钟级延迟；未触发或重跑任何 workflow | 继续低频检查 registry/deployment；镜像出现后要求 workload generation 收敛、Pod Ready 且 0 restart，再进入 preview/runtime |
| 2026-08-07 14:21 | Case 11 committed 诊断修复部署轮询 2 | production deployment | 等待部署，无状态变化 | 约 30 秒后仍为 generation/observed `2739/2739`、updated/ready/available 1/1/1，image `61a1f8c6`；未在旧镜像重跑 Case 11 | 延长轮询间隔，继续等待包含 `eead35c08` 的可追溯 Ready image |
| 2026-08-07 14:20 | Case 11 committed 诊断修复部署轮询 1 | production deployment；远端 `feature/integrate` | 等待部署，无新 Ready | 真实远端 tip 仍精确为 `eead35c08`；production deployment generation/observed `2739/2739`、desired/updated/ready/available 1/1/1/1，现役 image 仍为基线 `61a1f8c6`；唯一 Pod Running/Ready、0 restart。首次只读命令因 zsh 未引用 custom-columns 的方括号而在本地拒绝，引用后正常回读，未调用 production workflow | 保持低频只读轮询；只有 image 更新且 Ready、提交可追溯包含 `eead35c08` 后才执行 Case 11 preview/runtime |
| 2026-08-07 14:19 | Case 11 Aevatar committed 诊断修复提交与普通推送 | `origin/feature/integrate` | 已推送 `eead35c089758b26f7b0fd4c277dbbe71815b0cc` | 提交 `Preserve managed upstream codes in committed failures` 仅包含 admitted executor、共享安全 code resolver、audit failure resolver 和回归测试。提交前后二次 fetch 均确认远端基线 `61a1f8c6e` 未前进；普通 push 成功，`git ls-remote` 回读真实远端 tip 与本地提交精确一致；隔离 worktree 干净 | 低频只读轮询 production deployment；只有 Ready image 可追溯包含 `eead35c08` 后才先执行 Case 11 preview，再真实运行一次并读取 committed terminal/typed artifact |
| 2026-08-07 14:17 | Case 11 Aevatar committed 诊断修复完整门禁 | 隔离分支 `fix/2026-08-07_preserve-managed-upstream-code` | 全部通过；尚未部署 | `Aevatar.AI.Tests` 2476/2476、`Aevatar.AI.Infrastructure.ChronoSandbox.Tests` 216/216、architecture tests 15/15；完整 architecture guards、test stability、docs lint 87/87、定向 `dotnet format --verify-no-changes`、`git diff --check` 和 diff 敏感信息扫描均通过。ChronoSandbox 首次 `--no-restore` 仅因隔离 worktree 缺少 assets file 未启动测试，正常 restore 后 216/216；仅保留仓库既有 analyzer/package warning | Fetch `origin/feature/integrate`；远端若前进则 rebase 并重跑聚焦/相关门禁，否则复核单一 diff 后提交并普通推送；等待可追溯 Ready 部署后先 preview，再仅运行一次 Case 11 |
| 2026-08-07 14:12 | Case 11 committed 诊断码 Aevatar 聚焦回归 | 隔离 worktree，基线 `61a1f8c6e` | 红测复现后修复，19/19 通过；尚未部署 | 上一版 transport 已安全解析 `managed_upstream_codex_*`，但 admitted executor 与 audit failure resolver 仍把它折叠成泛化码。修复后只有精确 `managed_upstream_codex_` 前缀、非空后缀、长度不超过 96 且仅含小写字母/数字/下划线的 code 可贯穿 receipt/audit；普通 provider detail、相似前缀、空后缀、前导空白和换行继续 fail closed。未调用 production、未产生外部写入 | 运行完整 `Aevatar.AI.Tests`、ChronoSandbox 回归、测试稳定性、docs/architecture/format/diff/敏感信息门禁；全部通过后 fetch/rebase，单一提交并普通推送 `origin/feature/integrate` |
| 2026-08-07 13:56 | Case 11 managed 诊断部署 fresh runtime 与首轮日志 | Ready `5f51f6d0`；run hash `72b16525a3d8` | committed `failed`；新诊断尚未命中 | 固定 probe 在 preview 通过后真实启动；committed read model 为 `failed`、state 31、4/4，稳定失败仍为 `codex_execution_capacity_unavailable`，无 final artifact、approval、resume 或副作用。随后按 `managed_upstream`、`managed_proxy`、`codex.*fail`、`failurecode`、`capacity` 过滤 5 分钟生产日志，0 命中；该空结果不能证明上游返回体为空，也不能把失败归为 Ornn/chat | 不重跑 Case 11；扩大有界时间窗并查询源码中的精确安全日志消息，核对 coordinator/executor 日志级别与错误折叠路径，先区分上游无 allowlisted code、日志被抑制和中间层丢码 |
| 2026-08-07 13:55 | Case 11 managed 诊断部署 production explicit-request preview | Ready `5f51f6d0`；scope hash `237314c29964` | 通过 | 从已认证 `/api/auth/me` 在同一进程提取并交叉校验 scope，ignored config 提供 team；原始值未回显。固定 Case 11 preview 为 `runEnabled=false`、0 inline workflow、0 external call site，未 provision、未启动 workflow、无副作用 | 在同一 Ready 和 production materialization 上启动一次 Case 11；等待 committed terminal，严格断言 managed receipt、`CODEX_EXEC_READY`、exit 0、非空脱敏 diagnostic、5 项并行 gate 与 `side_effects=false`；若失败立即读取新安全 failure code |
| 2026-08-07 13:54 | Case 11 Ready 复验前生产配置物化 | `config.local.yaml` -> `build/workflows-local`；动态 inventory | 通过，29/29 | 在 ignored 独立目录重新生成 29 个 workflow，29/29 均通过 `YAML.safe_load` 且数量与当前公开源一致；未打印 replacement 值，配置与 build 仍未跟踪 | 从已认证 NyxID `/api/auth/me` 在同一进程提取 scope，不回显原值；对 Ready `5f51f6d0` 单独执行 Case 11 explicit-request preview |
| 2026-08-07 13:53 | Aevatar managed 诊断修复 Ready 部署 | Ready `5f51f6d0`；目标完整提交 `5f51f6d0ea2b103416306d5bb316f8e066f768dd` | 通过，可进入复验 | registry manifest 已发布，config digest 为 `sha256:f2cbce2d836d4cf5ecb50117165164d8360ad835bf9aaed6af1ba499be6788ff`；deployment generation/observedGeneration 2737/2737，replicas/updated/ready/available 均为 1，rollout status 成功。唯一 Pod Running/Ready、0 restart，运行 image digest 为 `sha256:2063930436fcd78b2a187ac4e19488f5cd09422d3c4a8fd159154b67e9d1ec78`。远端随后前进到后代 `61a1f8c6e`，ancestry 明确包含本修复；当前 Ready 仍精确为目标提交 | 在该 Ready 上先执行 Case 11 explicit-request preview；preview 通过后启动一次 fixed managed probe，以 committed terminal 与严格 typed artifact 判定，并用新 allowlisted failure code 关联 producer 边界 |
| 2026-08-07 13:52 | Aevatar managed 诊断修复部署第九次轮询 | 目标 `5f51f6d0e`；registry / production | 等待，无状态变化 | registry 尚无目标 manifest；production 仍为父提交 `a087e509`、generation 2735、1/1 Ready。未重跑 Case 11 | 继续低频等待构建发布 |
| 2026-08-07 13:51 | Aevatar managed 诊断修复部署第八次轮询 | 目标 `5f51f6d0e`；registry / production | 等待，无状态变化 | registry 尚无目标 manifest；production 仍为父提交 `a087e509`、generation 2735、1/1 Ready。未重跑 Case 11 | 继续低频等待构建发布 |
| 2026-08-07 13:50 | Aevatar managed 诊断修复部署第七次轮询 | 目标 `5f51f6d0e`；registry / production | 等待，无状态变化 | registry 尚无目标 manifest；production 仍为父提交 `a087e509`、generation 2735、1/1 Ready。未重跑 Case 11 | 继续低频等待构建发布 |
| 2026-08-07 13:49 | Aevatar managed 诊断修复部署第六次轮询 | 目标 `5f51f6d0e`；registry / production | 等待，无状态变化 | registry 尚无目标 manifest；production 仍为父提交 `a087e509`、generation 2735、1/1 Ready。未重跑 Case 11 | 继续低频等待构建发布 |
| 2026-08-07 13:48 | Aevatar managed 诊断修复部署第五次轮询 | 目标 `5f51f6d0e`；registry / production | 等待，无状态变化 | 目标 manifest 尚未发布；production 仍为父提交 `a087e509`、generation 2735、1/1 Ready。未 preview、未运行 Case 11 | 继续低频等待；目标发布后检查 rollout 是否成功 |
| 2026-08-07 13:46 | Aevatar managed 诊断修复部署第四次轮询 | 目标 `5f51f6d0e`；registry / production | 等待，无状态变化 | 目标 manifest 仍不存在；deployment 保持 generation 2735、父提交 `a087e509` 1/1 Ready。未触发 Case 11 | 继续低频等待目标镜像发布 |
| 2026-08-07 13:45 | Aevatar managed 诊断修复部署第三批连续轮询 | 目标 `5f51f6d0e`；remote / registry / production | 等待，无状态变化 | 连续两次 registry/deployment/Pod 只读检查中，远端 tip 仍精确为目标提交；目标 registry manifest 仍不存在；production 仍为父提交 `a087e509`、generation 2735、唯一 1/1 Ready、0 restart。未 preview、未运行 Case 11 | 继续低频等待；不在父部署重复产生无新证据的 run |
| 2026-08-07 13:45 | Aevatar managed 诊断修复部署第二次轮询 | 目标 `5f51f6d0e`；registry / production deployment | 等待，无状态变化 | 目标短 SHA tag 的 registry manifest 仍不存在；production 仍为 generation 2735、父提交 `a087e509` 唯一 1/1 Ready。未重跑 Case 11、未产生生产写入 | 拉长轮询间隔继续等待构建发布；目标 Ready 后再 preview/runtime |
| 2026-08-07 13:45 | Aevatar managed 诊断修复部署首次轮询 | 目标 `5f51f6d0e`；production deployment | 等待构建/部署 | 真实远端 tip 仍精确为目标提交；production 已从 `49cb76e4` 前滚到目标的直接父提交 `a087e509`，deployment generation/observedGeneration 2735/2735，replicas/updated/ready/available 均为 1，唯一 Pod Running/Ready、0 restart。目标提交尚未成为 Ready workload，故未 preview、未重跑 Case 11 | 保持低频轮询；只有 `5f51f6d0e` 或明确包含它的后代成为唯一 1/1 Ready 后才进入 Case 11 preview/runtime 复验 |
| 2026-08-07 13:43 | Aevatar managed 诊断修复提交与推送 | `origin/feature/integrate` | 通过 | 单一提交 `5f51f6d0ea2b103416306d5bb316f8e066f768dd`（`Preserve managed upstream failure diagnostics`）已从最新父提交 `a087e5097` 创建并普通推送；`git ls-remote origin refs/heads/feature/integrate` 回读 tip 精确等于本地提交，未 force push。隔离 Aevatar worktree 干净；验收仓库既有脏改动仍未暂存、未提交 | 低频检查 registry/deployment；只有该提交或明确包含它的后代成为唯一 Ready workload 后，才先 preview 再运行 Case 11，并立即用新安全 failure code 关联 producer 边界 |
| 2026-08-07 13:40 | Aevatar managed 诊断格式、diff 与远端竞态审核 | 本轮 6 个 Aevatar 文件；真实 `origin/feature/integrate` | 通过，可提交 | `dotnet format whitespace --verify-no-changes` 通过；工作树只有 2 个文档、2 个生产文件、2 个测试文件，无 untracked/generated 文件；`git diff --check` 与新增 diff 敏感信息模式扫描通过。再次 fetch 后本地基线和远端 tip 均精确为 `a087e5097a689fc6cc858a9b43664ea34d75b2fa`，无需 rebase | 创建单一 Aevatar 提交；回读提交内容后普通推送 `HEAD:feature/integrate`，要求 `git ls-remote` tip 精确匹配；随后低频等待可追溯 Ready 部署 |
| 2026-08-07 13:38 | Aevatar managed 诊断提交前门禁 | test stability、docs lint、architecture guards、diff check | 通过 | test stability 与关联 guard 全部通过；docs lint 为 87 files、0 errors；完整 architecture guards 通过，其中 architecture tests 15/15，并覆盖 workflow、projection、CQRS、channel、Nyx relay 与 runtime callback 等边界；`git diff --check` 通过。仅出现仓库既有 NuGet vulnerability/analyzer warning | 对 6 个本轮文件运行格式验证并审阅完整 diff、生成物和敏感信息；再次 fetch，若远端前进则 rebase 后重跑定向验证，再创建单一 Aevatar 提交并普通推送 |
| 2026-08-07 13:34 | Aevatar managed 诊断完整受影响项目回归 | `Aevatar.AI.Tests`；`Aevatar.AI.Infrastructure.ChronoSandbox.Tests` | 通过 | 完整项目测试分别为 2471/2471 与 216/216 通过；定向新增覆盖已包含在总数中。构建仅出现仓库既有 package/analyzer warning，无测试失败；本批未访问生产、未运行 Case 11 | 运行 test stability、docs lint、architecture guards、`git diff --check` 与 secrets/diff 审核；门禁通过后同步 README 并准备 Aevatar 单一提交 |
| 2026-08-07 13:32 | Aevatar managed 上游诊断定向回归 | bounded NyxID client；`NyxIdManagedCodexChronoTransport` | 通过 | 生产代码现对非 2xx 使用与成功响应相同的 Content-Length 和流式字节上限；transport 只解析 chrono 固定 `error.code` 或 NyxID 包装 `body` 中的显式白名单码，继续丢弃 message、未知码和原始 body。定向测试分别为 5/5、21/21 通过，覆盖 HTTP 502、直接/包装错误、超限不读取和合成敏感串不泄漏；未增加重试或普通 `/execute` 回退 | 更新 managed Codex canon/runbook，运行两个完整受影响测试项目、test stability/architecture guards 与 diff/敏感信息检查；通过后 fetch/rebase 最新远端并普通推送 `feature/integrate` |
| 2026-08-07 13:30 | Aevatar managed 上游诊断回归红测 | 最新 `origin/feature/integrate` `a087e5097`；bounded NyxID client 与 managed transport | 预期红测复现，确认 Aevatar 缺陷 | 隔离 worktree 已 rebase 到最新远端。`NyxIdApiClientBoundedProxyTests` 精确证明 HTTP 502 的 66-byte 合成 JSON 被当前 bounded client 丢弃为空；transport 的已知 `CODEX_SANDBOX_CREATION_FAILED` 用例因此实际退化为 `managed_proxy_unavailable`，而未知且包含合成敏感串的错误用例继续不泄漏。该批未访问生产、未运行 Case 11、未产生副作用 | 在 Aevatar 内只读取现有字节上限内的非 2xx body，并由 managed transport 解析显式白名单 `CODEX_*` code；不保留 message、不记录原始 body、不重试或回退普通 `/execute`，随后运行定向和相关项目测试 |
| 2026-08-07 13:21 | 当前证据同步后的 Risk/report 一致性门禁 | production JSON、21 个 risk case、Markdown/HTML、report validator | 通过 | production JSON 与 Ruby syntax 通过；`validate_risk_cases.rb` 为 16 passed、0 blocked、0 failed、0 pending-execution、4 not-configured、1 skipped-expired；`validate_report.rb` 通过 workflow=29、risk=21 及 current `49cb76e4` / Case 11 / Lark Case 12 交叉断言；`git diff --check` 通过。首次 report 运行发现 HTML 的 direct Case 12 state 被误写成 Lark state 33，已恢复 direct state 31，并把 Lark state 33 留在独立 canary 证据 | 进入 Aevatar 隔离 worktree，先以测试复现 bounded 非 2xx body 被丢弃，再做最小诊断修复和相关门禁；验收仓库继续不暂存、不提交 |
| 2026-08-07 13:20 | 当前证据同步与 managed 502 源码诊断 | Ready `49cb76e4`；production JSON、Markdown/HTML；Aevatar `NyxIdManagedCodexChronoTransport` 与 chrono-sandbox `1e8134d` | Ornn/聊天绿色已同步；发现 Aevatar 丢弃 bounded 非 2xx body 的诊断缺陷 | 当前 deployment/远端仍精确为 `49cb76e4acea8c65b74922e1dcd0949712213903`，1/1 Ready；近 3 小时 Aevatar 日志按 codex/capacity/502 过滤仍无命中，故未重跑 Case 11。机器摘要现记录 Case 11 run `397f4f9d90bf` 与 fresh Lark Case 12 run `ebe0e10241f0`。源码对照确认 chrono-sandbox 502 body 提供稳定 `CODEX_*` error code，但 Aevatar bounded NyxID client 对所有非 2xx 将 content 置空，transport 只能退化为 `managed_proxy_unavailable`，导致无法区分 sandbox creation、workspace、command、output 或 cleanup 边界 | 先运行 risk/report/diff 一致性门禁；通过后从最新 `origin/feature/integrate` 建隔离 worktree，补回归测试并只保留 bounded、allowlisted、脱敏 upstream code，普通推送并等待可追溯部署后再复验 Case 11 |
| 2026-08-07 13:09 | Case 12 fresh Lark Ornn/聊天全链 canary | `Aevatar` Agent；Ready `49cb76e4`；run hash `ebe0e10241f0` | 严格 `validated`；用户可见聊天链路当前已恢复 | 发送前目标 workflow catalog 基线为 10；Lark 发送 1 条仅含公开合成断言的消息，Bot 返回新的非 destructive `use_skill` typed approval，本轮只批准该卡 1 次。发送窗口内 catalog 精确新增 1 个 `safe_code_execute_validation` run；committed detail 为 `completed`、success=true、state 33、4/4，首个工具输出存在，artifact contract 精确命中 `case=safe_code_execute_validation`、`success=true`、`structured_receipt=true`、`total_cents=16623`、`side_effects=false`。Bot 回传表格可见但不参与成功判定；未保存 message/approval/run/member/actor/UUID 原值，无其他副作用 | 将本次 Lark transport 与 committed workflow 证据同步到 production 机器摘要、Markdown/HTML；Case 11 继续独立保留 `codex_execution_capacity_unavailable`，随后运行 risk/report/diff 一致性门禁 |
| 2026-08-07 13:04 | Case 11 chrono-sandbox 源码/日志权限诊断 | managed 分支 `1e8134d`；当前 production kube RBAC | 失败边界已缩小，但缺 producer 日志权限 | managed 源码确认 HTTP 429 仅来自进程内 `CODEX_MAX_CONCURRENCY` semaphore；HTTP 502 可来自 OpenSandbox create/readiness、workspace/execd、runner command 或 LLM 子链，均在 chrono-sandbox 以 `diagnostic_id/stage/failure_point/error_code` 记录。当前 Aevatar transport 将 429/502/503 统一映射为 `managed_proxy_unavailable`，且当前 kube token 无权跨 namespace list pod，无法读取 producer 日志；未修改 chrono-sandbox 的既有脏 worktree | 不在 Aevatar 增加普通 `/execute` fallback 破坏 managed 契约；转而 fresh 验证用户可见 Lark Ornn/聊天链路。Case 11 保留为外部 capacity blocker，待 producer 日志或 capacity 状态变化 |
| 2026-08-07 13:03 | chrono-sandbox 线上只读 health/OpenAPI 边界检查 | 唯一 `chrono-sandbox` UserService；线上 API 0.2.0 | 控制面健康且 managed 路由存在；执行子链仍未证明 | 经 NyxID proxy 回读 `GET /health` 为 `status=healthy`、`opensandbox_connected=true`；`/openapi.json` 路径集合明确包含 `/codex/execute` 与普通 `/execute`。本地仓库 `main` 不含 managed route，但远端 `feat/managed-codex-execution` / `pr-8` 精确位于 runbook 要求的 `1e8134d`，线上接口与该分支一致。health 不能证明 sandbox quota、runner pull、Codex/LLM 或 cleanup 子链 | 读取 `1e8134d` 的 managed error/diagnostic 契约并寻找只读生产日志入口；若只能得到外部 capacity 证据，则不在 Aevatar 用 fallback 绕过 managed 安全契约 |
| 2026-08-07 13:01 | Case 11 fresh failure 边界诊断 | Ready `49cb76e4`；managed credential status；10 分钟 production 日志 | Aevatar readiness 通过，transport capacity 仍阻塞 | lifecycle GET 脱敏回读为 enabled/eligible/active、`execution_ready=true`、reason=`ready`、state 3、cleanup 0，说明 credential/readiness 已越过。运行窗口内 `codex`/capacity 与全量 warning/error 过滤均 0 命中；源码确认 `codex_execution_capacity_unavailable` 来自 managed transport 对 NyxID `POST chrono-sandbox /codex/execute` 的 HTTP 429/502/503 或无响应映射。无 diagnostic/final artifact，当前仍不能唯一归到 Aevatar 或 chrono-sandbox | 经唯一 UserService 只读请求线上 `/health` 和 `/openapi.json`，只输出健康布尔值与路径集合；核对本地 chrono-sandbox 历史/分支是否包含 managed endpoint，再决定能否形成可修复根因 |
| 2026-08-07 12:59 | Case 11 新 Ready fresh managed runtime | Ready `49cb76e4`；run hash `397f4f9d90bf` | committed `failed`；capacity blocker 未恢复 | 运行前 preview 再次通过并复用现有 revision；committed read model 为 `failed`、state 31、4/4，固定 `codex_exec` 仍返回 `codex_execution_capacity_unavailable` / `CodexExecutionException`，无 final artifact、approval/resume 或副作用。严格 `CODEX_EXEC_READY` 与五项 typed gate 未产生，不能记为通过 | 拉取本次运行窗口的有界脱敏 production 日志，定位 Aevatar provider、NyxID managed proxy 与 sandbox capacity 边界；同步机器摘要和 Markdown/HTML 报告后重跑一致性门禁 |
| 2026-08-07 12:57 | Case 11 新 Ready production explicit-request preview | Ready `49cb76e4`；scope hash `237314c29964` | 通过 | 已认证 `/api/auth/me` 顶层/session scope 一致，本地 team 在 scope read model 中唯一且 active；固定 managed probe preview 为 `runEnabled=false`、0 inline workflow、0 external call site，未 provision、未启动 workflow、无副作用。preview 不证明 managed execute capacity | 在同一 Ready 上启动唯一一次 fresh Case 11；等待 committed terminal，严格断言 managed receipt、裁剪后 `CODEX_EXEC_READY`、exit 0、非空脱敏 diagnostic、5 项并行 gate 与 `side_effects=false` |
| 2026-08-07 12:56 | Case 11 production preview 首次调用 | 本地验证器参数边界 | 未触达生产；缺少 scope 输入 | `production_validate.rb --cases 11` 在发起 NyxID 请求前以“缺少 `--scope-id` 或 `AEVATAR_SCOPE_ID`”退出；未 preview、未 provision、未启动 workflow、无副作用，因此不能据此判断新 Ready 的 Case 11 状态 | 通过已认证 NyxID `/api/auth/me` 在同一进程内提取并校验 scope，不打印或落盘真实标识；随后重试 Case 11 preview |
| 2026-08-07 12:55 | Production Ready/远端/UserService 状态变化检查 | deployment generation 2733；远端/镜像 `49cb76e4` | 发现可追溯新 Ready，可继续 Case 11 | production generation/observedGeneration 2733/2733，replicas/updated/ready/available 均为 1；唯一 Pod Running/Ready、0 restart，镜像 digest `sha256:5dbb85b4540a4e21033c4acb4c00a28f117793031e862d068b2d79d11ffa3f21`。真实 `origin/feature/integrate` tip 精确为 `49cb76e4a` 且包含 `6558db8db` 修复；后续提交只修改 Admin Studio OAuth client 配置。NyxID 独立脱敏回读确认唯一 `chrono-sandbox` active，策略仍为 `forward=true`、`inject=true`、scope=`proxy:*` | 用 `config.local.yaml` 隔离物化，先执行 Case 11 explicit-request preview；preview 通过后才运行固定 managed probe，并以 committed terminal 与严格 typed artifact 判定 |
| 2026-08-07 12:54 | Case 11/12 状态同步后的 Risk/report 一致性门禁 | 当前工作树；21 个 risk case；Markdown/HTML/机器摘要 | 通过；Case 11 blocker 如实保留 | `validate_risk_cases.rb` 为 16 passed、0 blocked、0 failed、0 pending-execution、4 not-configured、1 skipped-expired；`validate_report.rb` 通过 workflow=29、risk=21 及当前/历史证据交叉断言；`git diff --check` 通过。未访问生产、未重跑无状态变化的 Case 11，也未暂存或提交验收仓库改动 | 只读检查 production Ready 镜像、真实 `origin/feature/integrate` tip 与 managed execute capacity；仅在出现新证据时先 preview 再运行 Case 11，同时独立复核真实 Lark 会话链路 |
| 2026-08-07 12:40 | Case 11 新部署/策略后的 fresh managed runtime | Ready `6558db8d`；run hash `39a374f8815a` | committed `failed`；capacity blocker 仍存在 | preview 通过且复用现有 revision；唯一 run committed `failed`、state 31、4/4，稳定失败仍为 `codex_execution_capacity_unavailable` / `CodexExecutionException`，无 final artifact、approval/resume 或副作用。普通 `/execute` 和 Assistant 绿色不证明 managed `/codex/execute` capacity | 同步 Case 11/12 新部署、run 与 Assistant 的脱敏机器摘要、Markdown/HTML 报告；运行 report validator。后续仅在 deployment 或 managed capacity 有新证据时重跑 Case 11 |
| 2026-08-07 12:39 | Case 11 production explicit-request preview | Ready `6558db8d`；`true/true/proxy:*` | 通过 | scope/team 前置精确匹配；固定 managed probe preview 为 `runEnabled=false`、0 inline workflow、0 external call site，未 provision、未启动 workflow、无副作用。preview 不代表 managed execute capacity | fresh 运行 Case 11；等待 committed terminal，严格断言 managed receipt、裁剪后 `CODEX_EXEC_READY`、exit 0、非空脱敏 diagnostic、5 项并行 gate 与 `side_effects=false` |
| 2026-08-07 12:39 | Case 12 `/api/chat` Ornn/Assistant fresh 全链 | Ready `6558db8d`；run hash `6fa89cd62b15` | 严格通过，用户可见阻塞已关闭 | Assistant 回合 completed；完成 Ornn search、精确 `safe-code-execute-validation` skill、1 次非 destructive typed mount approval、workflow start、artifact read/observe，无重复或混合 presentation call ID。committed workflow 为 `completed`、state 31、4/4，artifact contract verified；`workflowValidationStatus=validated`、`workflowValidated=true`、`caseValidated=true`、blocker 为空 | 因部署、UserService 策略和 `/execute` capacity 均有新证据，按规则先 preview 再 fresh 运行 Case 11 固定 managed probe；严格 typed artifact 判定 |
| 2026-08-07 12:36 | Case 12 capacity 变化后 fresh direct runtime | Ready `6558db8d`；run hash `6659aabee079` | 严格通过，普通 `code_execute` 已恢复 | preview 通过并复用现有 revision；run committed `completed`、state 31、4/4，first tool 与 contract probe 输出均存在。typed artifact 精确为 `case=safe_code_execute_validation`、`success=true`、`structured_receipt=true`、`total_cents=16623`、`side_effects=false`；0 approval/resume，严格 `finalArtifactVerified=true` | fresh 运行 Case 12 `/api/chat` Assistant；必须证明 Ornn search、精确 skill mount、workflow start 与 committed typed artifact，不能仅凭聊天文案 |
| 2026-08-07 12:35 | Case 12 NyxID `/execute` 修正最小 probe | 唯一 `chrono-sandbox`；固定合成 JavaScript | 通过，downstream 当前有 execute capacity | 显式 JSON Content-Type 后 CLI 6 秒返回 typed `{success, output}`；`success=true`、`exit_code=0`，stdout 精确等于合成 `case12-direct` / `total_cents=16623` / `side_effects=false`，无 error、无副作用。唯一 UserService、bearer/delegation、NyxID proxy 与 `/execute` 当前链路均可用 | 基于新的下游 capacity 证据只 fresh 重跑一次 Case 12；仍要求 committed completed 与严格业务 artifact，若再 524 则按可复现冷启动/timeout 缺陷处理 |
| 2026-08-07 12:35 | Case 12 NyxID `/execute` 最小 probe 首次请求 | 唯一 `chrono-sandbox`；固定合成 JavaScript | 输入在准入层拒绝，未形成下游证据 | CLI 约 1 秒返回 HTTP 400 `Content type error`；请求使用 stdin JSON 但未显式声明 Content-Type，因此未证明 chrono-sandbox execute 成功或失败，也没有副作用。未输出服务 ID/token/原始配置 | 对同一固定无副作用 payload 仅增加 `Content-Type: application/json`，执行一次修正 probe；按 elapsed、HTTP/typed shape 与精确 stdout 判定 |
| 2026-08-07 12:35 | Case 12 524 源码与请求契约对照 | workflow、Aevatar `code_execute` provider、HTTP client | 请求封装正确；需下游最小探针 | Case 12 仅执行固定无副作用 JavaScript；provider 精确 POST `chrono-sandbox /execute`，modern body 为 `{language, script}`，仅 upstream 404 才回退 `/run`。Aevatar NyxID client 生产注册上限为 420 秒，本次约 100 秒后的 HTTP 524 不是该本地上限主动取消；静态证据仍不能区分 NyxID gateway 与 chrono-sandbox | 经唯一 UserService 执行一次等价无副作用 NyxID `/execute` 最小 probe；只输出 HTTP/typed shape 和合成断言，不记录 ID/token |
| 2026-08-07 12:34 | Case 12 fresh 524 有界 production 日志 | Ready `6558db8d`；04:29Z 后窗口 | Aevatar 日志无对应异常，边界未收敛 | 先按 `code_execute`、524、chrono-sandbox 与稳定失败文案过滤最近 10 分钟，0 命中；再读取 04:29Z 后有界脱敏尾部，仅见 Pod 健康检查 200。日志流已脱敏且未落盘。该证据不能把 524 归为 Aevatar，也不能证明 chrono-sandbox 成功处理请求 | 对照 workflow 固定 payload、Aevatar provider 与 chrono-sandbox API/timeout 契约；必要时只通过 NyxID 执行一次等价无副作用最小 probe，区分 gateway/downstream 与 workflow 封装 |
| 2026-08-07 12:33 | Case 12 修复后 fresh direct runtime | Ready `6558db8d`；run hash `c257460dc47a` | committed `failed`；鉴权恢复，出现新 524 blocker | preview 再次通过；唯一 run 已查询 committed read model，terminal=`failed`、state 12、1/1，稳定失败为 `NYXID_PROXY_HTTP_524`，无 final artifact、approval 或副作用。此前 `NYXID_PROXY_UNAUTHORIZED` 未再出现，说明 bearer forwarding 回归已越过；但严格 `total_cents=16623` artifact 未产生，不能记为通过 | 拉取该运行窗口有界脱敏 production 日志，区分 Aevatar timeout、NyxID gateway 524 与 chrono-sandbox 下游执行超时；基于同一 run 证据做最小归因 |
| 2026-08-07 12:30 | Case 12 production explicit-request preview | Ready `6558db8d`；scope hash `237314c29964` | 通过 | 已认证 `/api/auth/me` 的顶层/session scope 一致，ignored 配置中的 team 在该 scope 精确匹配 1 个 active team；Case 12 preview 为 `runEnabled=false`、0 inline workflow、0 external call site，未 provision、未启动 workflow、无副作用 | fresh 运行 Case 12 direct runtime；要求 committed `completed`、步骤完整并严格命中 `total_cents=16623` 与 `side_effects=false` typed artifact |
| 2026-08-07 12:28 | Case 12 NyxID UserService 策略更新与独立回读 | 唯一 `chrono-sandbox` | 通过，普通与 managed 契约已兼容 | 正式 CLI update 进程成功，仅设置 `forward=true`、`inject=true`、scope=`proxy:*`；响应只输出字段名。随后独立 `service list` 脱敏聚合确认 count=1、active/connected/personal credential 均为 true，策略精确为 `true/true/proxy:*`。未输出或保存服务 ID、endpoint、credential/token | 在 Ready `6558db8d` 先执行 Case 12 explicit-request preview；通过后 fresh direct runtime，并只以 committed terminal 与严格 typed artifact 判定 |
| 2026-08-07 12:27 | Case 12 NyxID UserService 变更前只读断言 | 正式 CLI；`chrono-sandbox` | 通过，可执行唯一策略更新 | 脱敏聚合确认目标数精确为 1，active=true、connected=true、credential source type=`personal`、service type=`http`；当前策略精确为 `forward=false`、`inject=true`、scope=`proxy:*`。服务 ID、endpoint 和 credential 未输出或落盘 | 仅将该唯一目标更新为 `forward=true`、`inject=true`、scope=`proxy:*`；随后使用独立 list 进程脱敏回读，不复用 update 响应声明成功 |
| 2026-08-07 12:26 | Case 12 修复目标 Ready 部署确认 | production generation 2731；镜像 `6558db8d` | 通过，可进入配置恢复 | deployment generation/observedGeneration 2731/2731，replicas/updated/ready/available 均为 1，唯一 Pod Running/Ready、0 restart；现役镜像精确为目标 tag `6558db8d`，digest `sha256:ce65792b8adc41f70d4dedaf42d3223b4f8c172dd97c73a6b93b4653f9037aa1`。该完整提交已精确推送并有本批独立 registry 证据 | 使用正式 NyxID CLI 先只读断言唯一 active/connected/personal `chrono-sandbox`，再将策略改为 `forward=true`、`inject=true`、scope=`proxy:*` 并独立脱敏回读 |
| 2026-08-07 12:26 | Case 12 目标 manifest 与远端后代检查 | 目标 `6558db8db`；远端 `49cb76e4a` | 目标镜像已构建；等待 rollout | Docker Hub 目标 8 位 tag 于 2026-08-07 04:25:15Z 生成；fetch 后真实 `origin/feature/integrate` 前进到 `49cb76e4a`，`git merge-base --is-ancestor` 确认其明确包含 `6558db8db` 修复。Ready deployment 仍需独立验证，manifest 与后代提交本身不等于部署成功 | 有界轮询 deployment；目标 tag 或该后代镜像成为唯一 1/1 Ready、0 restart 后再修改 UserService |
| 2026-08-07 12:25 | Case 12 修复后 production 部署轮询 10-11 | 目标 `6558db8db`；Ready `e1a1d3f3` | 推送约 10 分钟后仍等待构建 | 两次 45 秒间隔只读复核均为目标 manifest 不存在、deployment 2729/2729、`e1a1d3f3` 唯一 1/1 Ready；Ready 健康且无 restart/rollout failure，但只包含修复祖先。未修改 UserService，未运行生产 case | 同时追踪 registry 最新 tag、真实远端后代与 deployment；目标提交或明确包含修复的后代镜像均可进入 Ready 验证 |
| 2026-08-07 12:23 | Case 12 合并有界部署轮询 6-9 | 目标 tag `6558db8d`；Ready `e1a1d3f3` | 4 轮均无状态变化 | 12:20:57、12:21:29、12:22:00、12:22:32 四次只读检查均为 manifest HTTP 404、deployment generation/observedGeneration 2729/2729、`e1a1d3f3` 唯一 1/1 Ready；脚本有界退出。该祖先镜像不含修复，未修改 UserService、未重跑 Case 12/11 | 保留准确外部构建 blocker并继续低频监视 registry/deployment；目标 manifest 出现即验证 rollout，明确失败则读取有界日志 |
| 2026-08-07 12:20 | Case 12 registry 发布节奏检查 | Docker Hub 公开 tag 列表 | 中间构建刚完成，目标等待下一周期 | 当前最新 tag `e1a1d3f3` 的 registry 更新时间为 2026-08-07 04:14:46Z（12:14:46 SGT），接近 `6558db8db` 推送时间；其前一 tag `4389ecd3` 为 03:22:03Z。证据表明本次 rollout 锁定了推送前的中间提交，目标尚未进入同一构建，不是目标镜像启动失败 | 对 registry/deployment 合并执行约两分钟有界低频轮询；观察到 manifest 或 generation 变化后立即进入 Ready 验证 |
| 2026-08-07 12:20 | Case 12 目标镜像与 deployment 轮询 5 | 目标 tag `6558db8d`；Ready `e1a1d3f3` | 等待构建，无状态变化 | 再等待 45 秒后 registry 仍无目标 manifest；deployment 仍为 2729/2729、唯一 1/1 Ready `e1a1d3f3`，远端 tip 保持目标提交。未出现 rollout failure 或新 managed capacity 信号 | 只读查看公开 registry 最近 tag 时间以判断发布节奏；随后继续有界低频轮询 |
| 2026-08-07 12:19 | Case 12 发布流水线只读检查 | 提交 `6558db8db`；GitHub Actions | 未发现目标 run；无失败证据 | 按目标 commit 查询 Actions 返回空集合；`feature/integrate` 最近可见 Actions 也属于更早提交，和刚完成的中间镜像 rollout 不构成同一权威发布入口。因此不能把“无 Actions”写成构建失败，也不能据此提前复验 | 继续以 registry manifest 和 production deployment 为权威证据低频轮询；出现明确 rollout failure 才拉取有界生产日志诊断 |
| 2026-08-07 12:18 | Case 12 目标镜像与 deployment 轮询 4 | 目标 tag `6558db8d`；Ready `e1a1d3f3` | 等待构建，无状态变化 | 30 秒后 registry 仍无目标 manifest；deployment 继续为 generation 2729、`e1a1d3f3` 唯一 1/1 Ready，远端 tip 仍精确为目标提交。无新部署或 capacity 证据，未重跑生产 case | 只读检查目标提交的 GitHub Actions 状态，区分 queued/in-progress/failure；构建成功后继续追踪 Ready rollout |
| 2026-08-07 12:17 | Case 12 目标镜像与 deployment 轮询 3 | 目标 tag `6558db8d`；Ready `e1a1d3f3` | 等待构建，无状态变化 | registry 尚无目标 8 位 tag manifest；deployment 保持 generation/observedGeneration 2729/2729、`e1a1d3f3` 唯一 1/1 Ready。未观察 rollout failure，也未在祖先镜像重复运行 Case 12 或 Case 11 | 延长有界轮询间隔；manifest 出现后继续等待目标或明确后代成为唯一 Ready |
| 2026-08-07 12:16 | Case 12 修复后 production 部署轮询 2 | 目标 `6558db8db`；中间镜像 `e1a1d3f3` | 中间 rollout 已收敛，目标尚未部署 | rollout status 成功；deployment generation/observedGeneration 2729/2729，唯一 1/1 Ready/updated/available Pod 为 `e1a1d3f3`、0 restart。远端 tip 仍精确为 `6558db8db`，当前 Ready 是目标祖先但不含本轮修复 | 检查目标 8 位镜像 manifest 与后续 generation；目标或包含修复的后代成为唯一 Ready 前不运行生产 case |
| 2026-08-07 12:15 | Case 12 修复后 production 部署轮询 1 | 目标 `6558db8db`；deployment generation 2729 | 发布流水线推进中，目标尚未部署 | deployment 当前 spec 指向中间提交镜像 `e1a1d3f3`，replicas 2、updated 1、ready/available 1、unavailable 1；旧 `4389ecd3` 仍为唯一 Ready，新中间 Pod Running 但未 Ready、0 restart。远端 tip 仍精确为目标 `6558db8db`，因此没有在旧或未 Ready 镜像复验 | 低频等待中间 rollout 收敛并继续追踪目标或明确后代；只有唯一 1/1 Ready 包含修复后才修改 UserService |
| 2026-08-07 12:14 | Case 12 Aevatar 提交与远端回读 | `origin/feature/integrate`；提交 `6558db8db` | 通过，等待部署 | 仅 6 个目标文件提交为 `Allow shared sandbox bearer forwarding`；提交后再次 fetch 显示本地仅 ahead 1、远端未前进，普通 push 成功。`git ls-remote` 回读远端 tip 精确为 `6558db8db00bcc43d38d3c3e3781246d8079d5cc`；未 force push | 有界轮询 production deployment；只有唯一 1/1 Ready 镜像可追溯到该提交或明确包含修复的后代，才调整 UserService 并复验 Case 12/Assistant/Case 11 |
| 2026-08-07 12:14 | Case 12 Aevatar 最终远端竞态与安全复核 | 隔离 worktree；`HEAD` / `FETCH_HEAD` / `origin/feature/integrate` | 通过，可提交 | 最终 fetch 后三者精确等于 `b8a9d6908f727f0db7c179bdc6459ac99c1840dd`，ahead/behind 为 0/0；diff 仍精确为 6 个目标文件，`git diff --check` 通过，token/JWT/UUID 扫描 0 命中 | 只暂存这 6 个文件并提交 `Allow shared sandbox bearer forwarding`；随后普通推送和远端 tip 精确回读 |
| 2026-08-07 12:13 | Case 12 Aevatar rebase 后完整门禁 | 隔离 worktree；远端基线 `b8a9d6908` | 通过，待最终 fetch/提交 | 隔离分支已无冲突前移到最新 `origin/feature/integrate`；完整 `architecture_guards.sh` 最终通过，包含 projection activation、scripting runtime snapshot、runtime callback、channel card、Nyx relay、Lark agent path 与 87 份 docs lint。最终 diff 仍仅为 resolver、1 个回归测试和 4 份 managed Codex 文档，`git diff --check` 通过 | 再次 fetch 并确认远端未前进；只提交这 6 个 Aevatar 文件，普通推送到 `origin/feature/integrate` 并精确回读远端 tip |
| 2026-08-07 12:07 | Case 12 Aevatar 提交前远端与 diff 审核 | 隔离 worktree；`origin/feature/integrate` | diff 安全；远端前进 2 提交，暂不提交 | 最终 diff 仅含 resolver、1 个测试文件和 4 份 managed Codex 文档，敏感 token/JWT/UUID 扫描 0 命中，`git diff --check` 通过。fetch 后远端从基线 `4389ecd3` 前进到 `e1a1d3f36`，新增后代包含 MCP connector initialization timeout 修复；当前分支落后 2，不能直接以旧基线推送 | 使用 autostash 将隔离 worktree 前移到最新远端，检查冲突和最终 diff；重新运行聚焦/完整相关测试与必需门禁，全部通过后提交并普通推送 |
| 2026-08-07 12:06 | Case 12 Aevatar 修复完整本地门禁 | 隔离 worktree；6 个相关代码/测试/文档文件 | 通过，待提交推送 | ChronoSandbox 项目 213/213、managed transport 18/18、workflow saga architecture 15/15；`test_stability_guards.sh`、87 份 docs lint、完整 `architecture_guards.sh` 与 `git diff --check` 全部通过。Transport 测试继续断言 managed 请求精确使用 `_nyxid_via`、`X-API-Key` 且 `Authorization` 为空；输出仅含仓库既有 NU1510/NU1903、复杂度和 nullable warning，无本轮新增 error | 最终审核 6 文件 diff、敏感信息与工作树；fetch 最新 `origin/feature/integrate`，必要时 rebase 并重跑受影响测试，然后提交、普通推送和远端 tip 回读 |
| 2026-08-07 12:00 | Case 12 Aevatar 回归测试与最小修复 | 隔离分支 `fix/2026-08-07_managed-code-execute-compatibility`；基线 `4389ecd3` | 聚焦红测已复现并转绿；尚未提交部署 | 新增同一 provisioning 用例的 `forward=false/true` 双分支；旧 resolver 在 `true` 分支精确抛出 `chrono_sandbox_delegation_misconfigured`，修改后 2/2 通过。实现只移除 managed readiness 对 forwarding 值的限制，继续强制 `inject=true` 与 scope 精确 `proxy:*`；managed transport 的既有 `X-API-Key`/无 `Authorization` 契约未改。ADR、canon、runbook 与 design 已同步，不修改 NyxID 或 chrono-sandbox | 运行完整 ChronoSandbox 测试、transport 定向测试、test stability、docs lint、架构与 diff 门禁；全部通过后 fetch/rebase、提交并普通推送 `origin/feature/integrate` |
| 2026-08-07 11:55 | Case 12 普通执行鉴权回归根因与 Aevatar 修复边界 | Ready `4389ecd3`；NyxID `origin/main`；Aevatar `origin/feature/integrate` | 已定位 Aevatar readiness 过度约束；尚未修复部署 | 唯一 personal `chrono-sandbox` 当前为 `forward=false`、`inject=true`、scope=`proxy:*`；这满足 managed `/codex/execute` 的现行 resolver，却使普通 `/execute` 收不到 caller bearer 并稳定 401。Aevatar managed transport 已固定使用 `X-API-Key` 且不发送 `Authorization`；NyxID 只在请求实际携带 bearer 时才执行 `forward_access_token`，现有 Aevatar 测试也断言 managed 请求 `Authorization` 为空。因此允许该 UserService 使用 `forward=true` 可恢复普通 `code_execute`，不会把 managed Agent Key 转发到 chrono-sandbox；最新 `4389ecd3` 仅改 Admin Studio UI，不是本次回归来源 | 从最新 `origin/feature/integrate` 建隔离 worktree，先补 `forward=true` 仍可 managed provision 的回归测试，再放宽仅这一项 readiness 条件并同步 managed Codex 文档；门禁通过后普通推送，等待可追溯 Ready，再将 UserService 改为 `true/true/proxy:*`，依次复验 Case 12 preview/direct、`/api/chat` 与有新状态证据时的 Case 11 |
| 2026-08-07 11:39 | 新 Ready Case 11 证据/报告一致性最终门禁 | production JSON、Markdown、HTML、report validator、工作树 | 通过；blocker 如实保留 | JSON/Ruby syntax、`validate_report.rb` 与 `git diff --check` 全部通过；当前部署为 `4389ecd3`，Case 11 最新 run `210561efe2a0` 仍为 committed `failed`、state 31、4/4、`codex_execution_capacity_unavailable`，历史 transport 诊断与成功基线分层保留。验收仓库未暂存或提交任何既有脏改动 | 保留 heartbeat：Case 11 等待 deployment、远端或 managed execute capacity 新证据；Case 12 继续等待 NyxID identity assertion 与 chrono-sandbox 普通执行授权的协同修复；Risks 29/30/40/41 仍只在安全目标齐备时运行 |
| 2026-08-07 11:38 | 新 Ready Case 11 证据/报告一致性首轮门禁 | production JSON、Markdown、HTML、report validator | JSON 与 Ruby syntax 通过；report validator 命中陈旧精确文案断言 | 新 Ready `4389ecd3`、run hash `210561efe2a0`、committed 4/4 capacity failure 已同步；`git diff --check` 通过。报告校验器仍要求 README 把祖先 `4c0596c7` 称为“当前 Ready”，与新部署事实冲突，因此 fail closed，未改写为绿色 | 将该断言更新为不把历史 schedule 证据镜像冒充当前部署的精确文案，再重跑 report validator 与 `git diff --check` |
| 2026-08-07 11:37 | Case 11 fresh direct runtime | Ready `4389ecd3`；run hash `210561efe2a0` | committed `failed`；capacity blocker 未恢复 | 运行前 preview 再次通过并复用现有 revision；committed read model 为 `failed`、state 31、4/4，固定 `codex_exec` 仍返回 `codex_execution_capacity_unavailable`，无 final artifact、approval 或副作用。严格 typed artifact 契约未满足，故不能记为通过 | 将新 Ready/run 的脱敏失败证据同步到 production JSON、Markdown/HTML 报告并执行 report validator；保留 heartbeat，后续只在 deployment、远端或 managed execute capacity 有新证据时重跑 Case 11 |
| 2026-08-07 11:36 | Case 11 production explicit-request preview | Ready `4389ecd3`；scope hash `237314c29964` | 通过 | `/api/auth/me` 与 current scope team read model 在同一进程确认 configured team 唯一 active；固定 managed probe preview 为 `runEnabled=false`、0 inline workflow、0 external call site，未 provision、未启动 workflow、无副作用 | 在同一 Ready 部署启动 Case 11 fresh runtime；等待 committed terminal，并严格断言固定 `CODEX_EXEC_READY`、typed managed receipt、五项并行 gate、`parallel_check_count=5` 和 `side_effects=false` |
| 2026-08-07 11:35 | Case 11 production preview 首次启动 | 当前 shell；Ready `4389ecd3` | 未请求；运行前置缺失 | 验证器在任何 Aevatar 请求前因当前 shell 未设置 `AEVATAR_SCOPE_ID` 退出；未 preview、未 provision、未启动 workflow、无外部写入。不能把本地环境缺失解释为生产失败 | 经已认证 NyxID 只读恢复 `/api/auth/me` 的唯一 scope，并用当前 scope team read model 与 ignored config 中 team 做唯一 active 匹配；原始值仅在同一进程传给验证器 |
| 2026-08-07 11:34 | Production 本地物化与部署/远端只读复核 | `config.local.yaml`；Aevatar deployment；真实 `origin/feature/integrate` | 29/29 物化完成；Ready 状态已变化 | deployment generation/observedGeneration 均为 2727、1/1 Ready、updated/available 均为 1、0 restart；唯一 Ready 镜像 `4389ecd3`（digest `sha256:7e042661df96bbe2b1d137725f0f2338d7a54e18972cd6289ba63deb9c0b56c5`）可追溯到远端完整提交 `4389ecd3113c30ad74e466555c9e67d93e345843`。本地生产占位符已重新物化，输出未记录真实值 | 先在该 Ready 部署执行 Case 11 explicit-request preview；preview 通过后 fresh 运行固定 managed probe，并只以 committed terminal state 与严格 typed artifact 判定 |
| 2026-08-07 11:30 | 最终动态 inventory、安全与双端 skill 入口重扫 | 最终工作树、ignored/staged 状态、Codex/Claude skill | 清单一致；active 面仍有两个运行 blocker | 动态重算为 29 workflow、29 Ornn skill、29 runtime contract、29 production registry、3 channel、21 risk、1 regression、1 schedule；`.agents/skills/aevatar-acceptance-loop` 与 `.claude/skills/aevatar-acceptance-loop` 同源有效。`config.local.yaml`/`build/` 仍 ignored，staged=0；diff 扫描 JWT=0、bearer value=0、UUID=0，长标识仅为公开 SHA/commit 与稳定 identifier；report validator 和 `git diff --check` 通过 | 保留 heartbeat：Case 11 等待 managed execute capacity；Case 12 需要 NyxID identity assertion + chrono-sandbox 普通执行授权协同修复。Risks 29/30/40/41 继续等待明确安全目标，不使用真实资源制造绿色 |
| 2026-08-07 11:28 | 完整静态、打包与 CI 等价门禁 | 当前工作树；`config.example.yaml`；29 个 workflow/skill | 全部通过；运行 blocker 未改写 | workflow 29/29、channel 3/3、risk 21/21（15 passed、1 blocked、4 not-configured、1 skipped-expired）、regression 1/1；production/admission/Assistant/publish/catalog Ruby syntax、示例物化与 29/29 YAML parse、skill 29/29、29 包打包、report validator、HTML 内联脚本和 `git diff --check` 全部通过。未访问生产、未产生外部写入 | 最终重扫动态 inventory、Codex/Claude skill 入口、ignored 产物、敏感信息与 unstaged/staged 状态；保留两个明确外部 blocker |
| 2026-08-07 11:27 | Risk/report 聚焦一致性门禁 | Risk 26、production/risk JSON、Markdown/HTML、validators | 通过；当前 blocker 如实保留 | JSON/YAML/Ruby syntax 通过；`validate_risk_cases.rb` 为 21 项中的 15 passed、1 blocked、4 not-configured、1 skipped-expired；`validate_report.rb` 通过 29 workflow、20 个 01-20 当前行、21 risk、历史 5 个 Assistant 与当前 Cases 01/12 的 1 validated / 1 typed-failure 对照。报告现明确 Ornn 29/29 绿色、Lark channel 3/3 绿色，普通 `code_execute` 单独阻塞；另修正 README Case 20 定义步骤数 12 -> 13 | 执行 AGENTS.md 完整静态/打包/报告门禁并最终重扫；production 无新 Ready 或 execute capacity 前不重跑 Cases 11/12 |
| 2026-08-07 11:16 | Production/远端只读状态复核 | Aevatar deployment 与真实 `origin/feature/integrate` | Ready 无变化，未重跑 Case 11/12 | Production 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`、0 restart；远端已前进到 `4389ecd31`，但新增提交只改 Admin Studio 样式、静态页与对应测试，不含 Ornn、Assistant、NyxID 或执行链修复，尚无可追溯新 Ready 部署或 execute capacity 证据 | 保留 Case 11 capacity 与 Case 12 sandbox delegation blocker；继续源码对照，只接受不转发 caller bearer、不放宽 scope 的兼容修复 |
| 2026-08-07 11:09 | `/api/chat` 非 `code_execute` 对照 | Ready `6a656d75`；Case 01 `release-readiness-review` | 严格 `validated` | Fresh 回合完成 Ornn search、精确 skill presentation、typed mount approval、workflow start 与 committed observation；run hash `535e9029486f` 为 `completed`、state 80、13/13，artifact 精确命中 `ready_for_review=true`、`side_effects=false`，无 blocker | 将当前结论同步到机器摘要、Risk 26、Markdown/HTML 与 report validator：Ornn/chat 核心链绿色，Case 12 单独因 sandbox delegation 协议冲突阻塞 |
| 2026-08-07 11:05 | NyxID `chrono-sandbox` authority 最小对照 | 唯一 active/connected/personal UserService；ID hash `872833a52fc1` | health 通过，execute typed 拒绝 | 权威 inventory 为 `forward_access_token=false`、`inject_delegation_token=true`、scope=`proxy:*`；同一 `--via-service` 的 `/health` 返回 healthy/OpenSandbox connected，固定无副作用 JavaScript `/execute` 则返回 `success=false`、typed `UNAUTHENTICATED`。未输出服务 ID、token、URL 或原始响应 | 检查 NyxID delegation header 注入和 chrono-sandbox `/execute` verifier 当前契约；保持不转发 caller bearer，选择兼容的最小修复后回归 CLI、direct Case 12 与 `/api/chat` |
| 2026-08-07 11:03 | Direct runtime fresh committed 对照 | Ready `6a656d75`；Case 12；scope hash `237314c29964` | `failed`，复现共用 runtime 回归 | Preview 再次通过并复用既有 revision；run hash `bc734af0d857` committed `failed`、state 12、1/1，失败步骤仍为 `code_execute`，稳定码 `NYXID_PROXY_UNAUTHORIZED`，无 final artifact、approval 或副作用 | 对唯一 active personal `chrono-sandbox` 做脱敏 `/health` 与固定无副作用 `/execute` 对照；若 CLI execute 通过则修 Aevatar caller credential 传播，若同样失败则归因 NyxID/UserService authority |
| 2026-08-07 11:02 | Production explicit-request preview | Ready `6a656d75`；Case 12；scope hash `237314c29964` | 通过 | 经 `/api/auth/me` 与 team read model 在进程内恢复唯一 active scope/team；Case 12 preview 通过，`runEnabled=false`、0 inline workflow、0 external call site，未 provision、未启动 workflow、无副作用 | 在同一 Ready 部署运行 Case 12 direct runtime，等待 committed terminal artifact，与 fresh `/api/chat` typed failure 对照 |
| 2026-08-07 11:00 | `/api/chat` fresh committed 复验 | Case 12 `safe-code-execute-validation` 的 Ornn search、mount、workflow start 与 typed artifact | `typed-failure`，当前回归 | Ornn search、精确 skill presentation、typed mount approval 和 workflow start 均成功；唯一 fresh run hash `b7e7c285f4b3` committed `failed`、state 12，失败步骤 `calculate_settlement`，稳定码 `NYXID_PROXY_UNAUTHORIZED`，无 final artifact 或副作用；Assistant 文案未参与判定 | 拉取同一时间窗脱敏生产日志并检查 NyxID/Aevatar bearer、scope 与 `code_execute` authority 边界；确认归因后最小修复 |
| 2026-08-07 10:58 | Ornn 线上 verify-only | 当前 29 个打包 skill 的服务端格式、精确名称、版本与 public readback | 29/29 通过 | 仅经 `nyxid proxy request ornn-api` 调用服务端格式校验与名称回读；缺失、private、version mismatch 均为空，未上传或修改 skill | fresh 复验安全 `/api/chat` Case 12，确认 search、mount、workflow start 与 committed typed artifact 当前仍可用 |
| 2026-08-07 10:51 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 10:36 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 10:21 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 10:06 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 09:51 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 09:35 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 09:20 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 09:05 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 08:50 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 08:35 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 08:20 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 08:04 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 07:49 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 07:34 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；只在部署、远端或 capacity 证据变化时恢复 Case 11 preview/runtime |
| 2026-08-07 07:19 | Heartbeat 生产状态复核 | Case 11 deployment、真实远端与当前机器证据 | 无状态变化，未重跑 | deployment 仍为 generation 2725、1/1 Ready、镜像 `6a656d75`；`origin/feature/integrate` 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。Case 11 机器证据仍为 `codex_execution_capacity_unavailable` / `managed_proxy_unavailable` / HTTP 502，`executeCapacityProven=false`；Risk 仍为 16 passed、4 not-configured、1 skipped-expired | 保留 heartbeat；继续只在出现新部署、远端提交或 capacity 证据时恢复 Case 11 preview/runtime |
| 2026-08-07 07:05 | Heartbeat 生产状态复核 | Case 11 deployment、真实 `origin/feature/integrate` 与已保存 capacity 证据 | 无状态变化，未重跑 | deployment generation/observedGeneration 仍为 2725，1/1 Ready、updated/available 均为 1，镜像仍为 `6a656d75`；真实远端 tip 仍精确为 `6a656d7593c655ba565565824431802c85e2de46`。现有只读 health 证据仍只证明 healthy/OpenSandbox connected，不证明 execute capacity；无新部署、远端提交或 capacity 信号，因此不重复消耗相同 managed probe | 保留 heartbeat；下一次仍先比较部署/远端，只在出现新证据时从 preview 恢复 Case 11 |
| 2026-08-07 06:49 | 外部 blocker 恢复循环 | 当前 Codex 任务 heartbeat `aevatar` | 已启用 | `paseo-loop` CLI 因本机 daemon 要求密码而未擅自重启；按技能优先级改用当前任务 heartbeat，每 15 分钟低频检查。无新部署/capacity 证据时禁止重跑 Case 11，无明确 disposable 资源时禁止运行 Risks 29/30/40/41；全部 active 验证面严格通过后删除 heartbeat | 等待 heartbeat 在外部状态变化时回到本任务继续 preview/runtime/修复/部署闭环 |
| 2026-08-07 06:47 | Case 11 最终部署状态核对 | production deployment 与真实 `origin/feature/integrate` | 无状态变化，不重跑 | deployment generation/observedGeneration 均为 2725，workload 1/1 Ready、updated/available 均为 1，镜像仍为 `6a656d75`；对真实 `origin` fetch 后 `FETCH_HEAD` 与该完整修复提交精确相等。未出现新部署或 managed execute capacity 证据，因此不重复消耗相同 Case 11 探针 | 等待 capacity 状态变化后从 preview/runtime 恢复；其间保留 committed `codex_execution_capacity_unavailable` 为当前非绿事实 |
| 2026-08-07 06:46 | 最终 risk/report/安全收口 | 当前报告、机器证据与工作树 diff | 门禁通过；外部 blocker 保留 | Risk validator 通过 21/21：16 passed、4 not-configured、1 skipped-expired；report validator 通过 workflow=29、risk=21 与全部报告集合。`git diff --check` 通过，diff 中 bearer/JWT/UUID 扫描均 0 命中；`config.local.yaml` 与 `build/` 未跟踪，staged 文件为 0 | Case 11 等待 managed execute capacity 状态变化后重跑；Risks 29/30/40/41 等待明确 disposable Base、schedule authority、一次性发送目标和可清理的合成审批目标 |
| 2026-08-07 06:45 | 最终动态 inventory 与 registry 对齐 | workflow、Ornn skill、runtime/production registry、channel/risk/regression/schedule | 清单一致；active 面仍未全绿 | 从最终工作树重算为 29 workflow、29 Ornn skill、29 runtime contract、29 production case，case ID 01-29 唯一且集合完全一致；官方 workflow/skill validator 通过 29/29。另有 3 channel、21 risk、1 regression、1 schedule；Risk 为 16 passed、4 not-configured、1 skipped-expired。`config.local.yaml` 与 `build/` 均仍 ignored；Case 11 仍是唯一 direct-runtime 非绿 active workflow | 复验 risk/report validator、`git diff --check` 和最终工作树状态；无新 capacity 证据时不重复运行 Case 11 |
| 2026-08-07 06:43 | 验收闭环 Skill 双端发现复验 | `.agents/skills/aevatar-acceptance-loop` 与 `.claude/skills/aevatar-acceptance-loop` | 通过 | `skill-creator` 官方 `quick_validate.py` 对 Codex 实体目录和 Claude Code 软链各通过 1 次；Claude 链接精确指向同一 Skill，`agents/openai.yaml` 的 `$aevatar-acceptance-loop` 显式调用与 `allow_implicit_invocation=false` 一致。校验依赖仅安装到 `/tmp`，未修改全局 Python 或仓库 | 执行最终动态 inventory 与集合对齐，然后复验 risk/report validator 和 diff/status |
| 2026-08-07 06:39 | 最终完整静态与 CI 门禁 | 当前 29-workflow 工作树；`config.example.yaml` | 全部通过 | 29/29 workflow、29/29 skill、3/3 channel、21/21 risk、1/1 regression；production/Assistant/publish/admission/catalog/runtime/report Ruby syntax 全部通过。公开配置生成 29 个可解析 workflow、打包 29 个 skill，report validator、HTML 内联脚本和 `git diff --check` 通过；Risk 汇总诚实保留 4 not-configured、1 skipped-expired | 最终动态重扫 workflow/skill/registry/case/report 集合，检查敏感信息、ignored config/build、工作树与唯一剩余 active blocker；必要时只对 Case 11 检查外部 capacity 状态变化 |
| 2026-08-07 06:39 | Case 29 / Risk 42 机器证据与报告一致性 | production/risk JSON、Markdown、HTML、validators | 通过 | 新增 Case 29 脱敏摘要钉住 Ready `6a656d75`、2 个 read-only call site、run `a729912ee5d9` state 77 11/11、严格 artifact 与 Ornn public；Risk 42 保留源路径/SHA/5 步 legacy 事实和旧 required evidence，状态改为非绿色 `skipped-expired` 并指向 replacement 29。Risk validator 为 16 passed、4 not-configured、1 skipped-expired；report validator 通过 workflow=29、可靠性/替代行=9、risk=21，JSON/YAML/Ruby syntax 与 `git diff --check` 通过 | 执行 AGENTS.md 完整静态/CI 门禁；批次结束后回写 README，再最终重扫动态 inventory、状态冲突、敏感信息和 ignored 产物 |
| 2026-08-07 06:28 | Case 29 Ornn publish/readback | 29 个 local package；`invoice-approval-routing-preview` 1.0 | 通过 | 29/29 服务端格式预检通过；missing-only 发布 1、精确跳过 28，新增项设为 public 后按名称/版本回读一致。正式 verify-only 与独立 catalog 均为 29/29，missing/private/version mismatch 为空，后两次操作无上传或权限修改 | 扩展 risk/report schema 支持 `skipped-expired`；将 Risk 42 与 replacement Case 29 建立显式关系，保留旧定义/hash/原因，不删除历史证据 |
| 2026-08-07 06:26 | Case 29 direct runtime | `invoice_approval_routing_preview`；fresh run hash `a729912ee5d9` | 严格通过 | committed `completed`、`terminalSuccess=true`、state 77、11/11；`foreach(document_extract)` 子步骤使 committed 数比静态 10 步多 1。15 项 artifact 精确命中：附件/归一化/历史窗口/contact 路由/重复策略为 true，`approval_created=false`、`message_sent=false`、`external_writes=false`、`side_effects=false`；0 approval、0 resume | 打包并 missing-only 发布 Case 29 Ornn skill，做服务端 validator、public readback；通过后同步机器摘要/报告，再退休 Risk 42 |
| 2026-08-07 06:24 | Case 29 production preview | Ready 当前部署；scope hash `237314c29964`；Lark service hash `a623c5a302b9` | 通过；runtime 待验证 | `interactive` explicit-request preview 精确返回 2 个 call site：审批历史 GET 与 contact `batch_get_id` POST；两者均为 `effectiveRisk=read_only`、`approvalRequired=false`、`approvalEnforcement=none`、仅允许 `interactive`。未启动 run、未执行外部写入 | 在同一 ignored local materialization 上启动一次 fresh runtime；只接受 committed `completed`、完整父/子步骤和严格 no-submit typed artifact |
| 2026-08-07 06:23 | Case 29 公开/生产前物化 | `config.example.yaml` 隔离输出；ignored `config.local.yaml` | 通过 | 两套配置均生成 29 个可解析 workflow 且无未解析占位符；本地 16 个 replacement 完整，contact 邮箱不是公开示例值，Lark service hash 仍为 `a623c5a302b9`。`config.local.yaml`、`build/` 保持 ignored，未输出真实值 | 仅经已认证 NyxID 发现当前 scope/team；对 Case 29 执行 `interactive` explicit-request production preview，验证 GET 与 attested read-only POST 两个 call site |
| 2026-08-07 06:21 | Case 29 定向静态与 skill 同步 | `invoice_approval_routing_preview`；workflow/skill registry | 通过；production 尚未验证 | 29/29 workflow YAML、步骤图与安全契约通过；Case 29 固定 10 步、2 个外部调用（审批历史 GET + `batch_get_id` 语义只读 POST）、单附件 `document_extract`、无 `code_execute`、no-submit artifact；29/29 本地 skill 字节一致，Ruby syntax、YAML parse 和 `git diff --check` 通过。Risk 42 仍为 `not-configured`，未提前退休 | 用公开配置完成物化与完整静态门禁；同步报告计数/schema 后，用 ignored `config.local.yaml` 物化并执行 Case 29 production preview |
| 2026-08-07 06:15 | Risk 42 版本族 supersession 候选核对 | P1 v2 legacy、Risk 28 exact v5 current | current/legacy 契约明确；尚不能退休 | 源目录无独立迁移说明，但仓库 risk contract 明确称 42 为 v2 `legacy`，称 28 的 v5 为 `current`；Risk 28 已以 sanitized attachment、exact source、14/14 committed、submit/write 未执行严格通过。按验收 Skill，现有 Case 13/14 不能冒充“新创建 replacement”，所以 42 暂不改状态 | 新增下一个 workflow case，覆盖当前 attachment -> history/dedup -> contact routing -> no-submit artifact；完成静态、preview、committed runtime、Ornn 与报告后再将 42 标为 `skipped-expired` |
| 2026-08-07 06:14 | Risk 42 P1 v2 legacy integration 结构化审查 | exact source sha `e4b73d9f...` | 不可直接执行，保持隔离 | 定义 5 步，其中 1 个 `foreach(document_extract)`、4 个无 capability 的 `code_execute`；四段代码内部直接 fetch NyxID/Lark，包含固定 provider URL、审批 code、个人/部门邮箱映射和表单枚举，placeholder 数为 0。即使 submit=false 仍做 OCR、审批历史与 contact 外部调用；不能仅换 attachment 后执行，也不能把 direct fetch 当当前平台契约 | 核对同版本族 v5/v6 的正式 supersession 与 Risk 28；只有正式替代证据和新同类 replacement case 都严格通过后，才可把 42 记为 skipped-expired |
| 2026-08-07 06:13 | Risk 29/30 Base disposable 目标只读审查 | config Base；exact Lark service | 安全前置不满足，未创建/删除表 | 经同一 `api-lark-bot --via-service` 读取 app metadata 与 table catalog 均返回 code 0，现有表 6、无分页；但 Base 名称不含 acceptance/test/sandbox/probe/fixture 语义，不能判为 disposable。原始 app token、名称、table ID 均未输出并已从进程变量丢弃 | Risk 29/30 保持 `not-configured`，需要明确 disposable Base；转向无外部写的 Risk 42 legacy isolation 可行性审查 |
| 2026-08-07 06:12 | Risk 29/30 exact Lark UserService 只读前置 | config service hash `a623c5a302b9` | 通过，只读 | ignored config 的 Lark UserService 在 NyxID inventory 精确命中 1 条 `api-lark-bot`，active/connected、credential source 为 org、OpenAPI 已配置；未输出原始 service ID 或连接信息。该结果不证明目标 Base 可写/可删 | 经同一 `--via-service` 只读获取 Base app 元数据与 table catalog；仅输出响应成功、名称是否明确为 acceptance/test、table 数，不输出 app/table/name 原值 |
| 2026-08-07 06:12 | Risk 29/30 P2 fixture setup 结构化安全审查 | 源 schema 文档、setup/seed/no-send 定义 | 现有 setup 不满足 contract，未运行 | schema 文档要求同一 Base 内 4 张 disposable schema-equivalent 表；现有 setup 却只创建并 seed 1 张带额外“数据集”分区字段的表，且 route/UserService 硬编码、无 table DELETE 清理步骤。`budget_monitor_weekly.nosend.yaml` 保留精确四表六读，但仍硬编码 route。直接执行 setup 既不能满足 Risk 29，也无法证明 cleanup | 只读核对 ignored config 指向的 Base 是否为明确验收资源，以及 exact UserService catalog 是否包含 table create/read/delete；安全目标或清理任一不明则保持 blocker |
| 2026-08-07 06:10 | Risk 29/30/40/41/42 contract 与 validator 复核 | 五个 `not-configured` case | 安全前置要求确认，尚未运行 | 29 要求 disposable 四表、精确 6 个只读 call site 与固定行数/合计；30 要求 exact no-send source 的 typed durable 创建、自然触发、artifact、DELETE 和静默窗口；40 要求一次性收件目标与显式发送授权；41 要求合成审批目标及可验证清理；42 要求先移除硬编码目标并隔离 legacy integration。源码目录另有 P2 fixture setup/seed 定义，但尚未证明安全可清理 | 对 setup/seed 与 schema-preserving 文档做结构化脱敏审查；只有确认不触碰真实资源、目标可清理且 authority 可见后才考虑 Risk 29/30 |
| 2026-08-07 06:09 | Case 11 current machine/Markdown/HTML 同步验证 | Ready `6a656d75`；两次 fresh capacity failure | 通过一致性门禁；当前为 27/28 | production JSON 新增 `case11CurrentValidation` 并将 latest runtime Case 11 标为 platform blocked，保留 `f7f543c5` 历史 recovery；Markdown/HTML 当前部署、runtime 指标和 Case 11 行同步为 27/28 与红色已验证阻塞。JSON parse、Ruby syntax、risk/report validator 和 `git diff --check` 全部通过 | 继续处理五个 `not-configured` 的安全前置与可替代方案；同时低频检查 chrono-sandbox execute capacity 是否发生状态变化 |
| 2026-08-07 06:04 | Case 11 current machine/report 一致性核对 | production JSON、Markdown、HTML | 确认陈旧绿色状态 | 机器 runtime 仍只保存历史 run `22111c344c15` 的 committed completed/state 179/30 步，Markdown/HTML 仍显示 `f7f543c5` 已恢复；均未同步 Ready `6a656d75` 上 run `705a69901de5`、`4caa726585f6` 的 committed failed/state 31/4/4/HTTP 502 capacity。当前 README 与报告存在冲突 | 保留历史成功为 recovery 对照；将 current Case 11 更新为两次 capacity failure，并同步 JSON、Markdown、HTML 与 report validator，当前失败必须使用非绿色状态 |
| 2026-08-07 06:03 | Case 19 canary 摘要一致性修复验证 | production/risk/report machine evidence | 通过；旧 `unproven` 已消除 | JSON parse、`validate_report.rb` syntax、21 项 risk validator、report validator 与 `git diff --check` 全部通过；canary 摘要现在以同一 run hash 和 committed/ingress/脱敏字段对照后段 Case 19 证据。validation 中仅余 Risk 29/30/40/41/42 五个 `not-configured` | 核对本轮 Case 11 两次 capacity committed failure 是否已同步到 production JSON、Markdown 与 HTML；状态变化不能只停留在 README |
| 2026-08-07 06:02 | Case 19 Lark canary 机器证据交叉核对 | run hash `03c3f4ded68e`；Risk 24 | 证据完整，确认摘要漂移 | production 摘要后段与 Risk 24 均记录唯一新增 run、committed `completed`、state 32、4/4、typed artifact `lark_bot_ingress_validated=true`、文件/SHA 断言、脱敏与无副作用；Markdown/HTML 报告同样引用该 run。前部 `larkChannelCanary=unproven` 与同文件后段冲突，属于验收实现缺陷，不是 case 过期 | 仅把前部 canary 摘要改为 `validated` 并引用相同脱敏事实；不新增运行证据、不重发 Lark，随后执行 JSON/report/risk validator |
| 2026-08-07 06:02 | 非绿机器状态逐项归因 | production/risk validation 摘要 | 5 个外部配置阻塞；1 个证据冲突待核对 | Risk 29/30/40/41/42 分别缺合成四表、精确 source schedule authority、一次性发送目标、安全审批目标/清理、legacy 硬编码隔离，均不得直接执行真实源定义。唯一 `unproven` 来自旧 production 摘要的 Lark attachment canary 字段，但 README 与 Risk 24 声称已有 fresh committed Case 19 证据，当前存在摘要一致性疑点 | 交叉核对 Case 19 fresh run hash、committed artifact 和 Risk 24；证据完整则修验收机器摘要并重跑 report validator，不重发 Lark、不冒充 case 过期 |
| 2026-08-07 06:01 | Case 11 capacity 后动态 inventory 重扫 | 当前最终工作树与 validation 摘要 | 清单一致；active 面未全绿 | 动态发现 28 workflow、28 Ornn skill、28 production registry、3 channel、21 risk、1 regression、1 schedule、7 fixture（其中 1 个 inline workflow）；全部 validation JSON 可解析。机器摘要仍检出 5 个 `not-configured` 与 1 个 `unproven`；`build/`、`config.local.yaml` 保持 ignored，既有脏工作树未暂存/提交 | 逐项读取 6 个非绿状态的脱敏 case/feature/blocker，区分当前 capacity、可立即执行项与缺少 disposable target/authority 的外部阻塞 |
| 2026-08-07 06:00 | Case 11 health 后唯一 fresh 重试 | Ready `6a656d75`；run hash `4caa726585f6` | committed 同边界失败，停止重复 | Preview 通过并复用 revision；fresh run committed `failed`、state 31、4/4，仍为 `codex_execution_capacity_unavailable` / `CodexExecutionException`，无 final artifact、approval 或副作用。结合前一 run 的 NyxID HTTP 502 与同 UserService health 绿色，当前为 execute capacity 外部阻塞，不再用相同输入循环 | 继续动态重扫和所有不依赖 managed execution capacity 的 active case；Case 11 保留 strict failure，待 capacity 状态变化后再从 preview/runtime 恢复 |
| 2026-08-07 05:59 | Case 11 同 UserService health 边界 | 唯一 personal `chrono-sandbox`；ID hash `872833a52fc1` | health 通过，允许一次有界重试 | 调用前再次要求唯一 active/connected/personal 且策略精确 `false/true/proxy:*`；通过该 UserService 的只读 `/health` 返回 `status=healthy`、`opensandbox_connected=true`。该证据只覆盖路由/OpenSandbox 连接，不外推 execute capacity | fresh 重试 Case 11 一次；若仍为 502/capacity，停止重复并按外部 capacity blocker 处理，继续其他可推进 case |
| 2026-08-07 05:59 | Case 11 capacity 失败 HTTP 上下文 | run hash `705a69901de5`；NyxID bounded proxy | 确认为 HTTP 502 | transport failure 前后有限上下文显示 NyxID POST 约 11116ms 收到 HTTP 502，`NyxID bounded proxy request failed: POST -> 502`，随后 Aevatar 映射为 `managed_proxy_unavailable` / CapacityUnavailable。认证 401 已消失；日志在输出前脱敏且未落盘 | 仅经同一 personal UserService 调用无副作用 `/health`；若 route/OpenSandbox 明确健康，仅 fresh 重试 Case 11 一次，否则保留外部 capacity blocker |
| 2026-08-07 05:58 | Case 11 capacity 失败首个有界日志批次 | run hash `705a69901de5`；Ready `6a656d75` | 命中 transport capacity 分类 | 最近 5 分钟固定 selector 日志在脱敏后精确命中 `Managed Codex transport failed with code managed_proxy_unavailable`，同一 run 映射为 `failureKind=CapacityUnavailable`，`execute_probe` 无 retry/on_error 后失败；未保存 Pod、actor、run 或 UUID 原值。本批尚未取得下游 HTTP status | 读取该 transport failure 的有限上下文，确认 HTTP 429/502/503 或 typed downstream code；没有新增证据前不重复 Case 11 |
| 2026-08-07 05:58 | Case 11 兼容修正后 fresh runtime | Ready `6a656d75`；run hash `705a69901de5` | committed `failed`，capacity 边界 | Preview 再次通过并复用现有 revision；运行 committed `failed`、state 31、4/4，稳定错误已从原认证拒绝变为 `codex_execution_capacity_unavailable` / `CodexExecutionException`，无 final artifact、approval 或副作用。不能以 scope 修正或 preview 宣称 Case 11 通过 | 读取本次时间窗的有界 Aevatar production logs，区分 NyxID proxy HTTP、chrono-sandbox capacity/provider 和 Aevatar error mapping；只在新增证据后决定是否重试 |
| 2026-08-07 05:57 | Case 11 兼容修正后 fresh production preview | Ready `6a656d75`；scope hash `237314c29964` | 通过 | 使用 `build/workflows-local` 和权威恢复的唯一 active scope/team 执行 explicit-request preview；`runEnabled=false`、`inlineWorkflowCount=0`、`callSiteCount=0`、callSites 为空，未 provision、未启动 workflow、无外部写入 | 同一部署与 production local 定义启动 Case 11 fresh runtime；等待 committed terminal 并严格断言固定 output、五项 gate、parallel=5、side effects=false |
| 2026-08-07 05:56 | Case 11 production scope/team 权威前置 | scope hash `237314c29964`；team hash `db2102f24ba6` | 通过，唯一 active 匹配 | `/api/auth/me` authenticated/enabled、top/session scope 一致；team root scope 与 auth scope 一致，5 个 team 中 ignored config 指定目标在 `teamId/scopeId` 双字段上精确匹配 1 条且 lifecycle active，member count 113。原始值只在进程内使用 | 在新进程重复同一前置并将 scope/team 仅作为参数传给 `production_validate.rb --cases 11 --workflow-dir build/workflows-local`；先 preview 不运行 |
| 2026-08-07 05:56 | Case 11 scope team read model schema 预检 | `/api/scopes/{scopeId}/teams` | 通过，只读 | 先要求 `/api/auth/me` authenticated/enabled 且 top/session scope 精确相等；team 响应为对象根，包含 `scopeId`、`teams`、`nextPageToken`，当前 5 项，成员字段含 `teamId/scopeId/lifecycleStage/memberCount`。本批未输出任何 ID 或名称 | 从 ignored config 读取 `__STUDIO_TEAM_ID__`，要求响应 root scope、team scope 与 auth scope 一致且目标唯一 active；仅输出 scope/team hash、计数和 member count |
| 2026-08-07 05:55 | Case 11 preview 身份只读 schema 预检 | NyxID `/api/auth/me` | 通过，只读 | 响应包含 authenticated/enabled、profile、top-level scope 与 session scope；两个 scope 字段类型均为 string。本批仅输出 root/session/profile 字段名和类型，未输出 scope、subject、email 或 token | 要求 authenticated/enabled 且 top/session scope 精确相等；用该 scope 调用 `/api/scopes/{scopeId}/teams`，与本地 team 配置做唯一 active 匹配 |
| 2026-08-07 05:54 | Case 11 production preview shell 前置 | 当前桌面 shell | scope/team 环境缺失，尚未请求 preview | `AEVATAR_SCOPE_ID` 与 `AEVATAR_TEAM_ID` 均未设置；验证器已确认可显式使用 `--workflow-dir build/workflows-local`。本批未输出变量值、未调用 production preview，也不能由环境缺失推断资源不存在 | 仅经 NyxID proxy 读取 `/api/auth/me` 与 scope team read model，在进程内恢复唯一 scope/team；只输出哈希和计数后执行 Case 11 preview |
| 2026-08-07 05:54 | Case 11 固定 probe 附加静态复核 | 公开源与 `build/workflows-local` | 通过 | 纠正 selector 为 `type=tool_call` 且 `parameters.tool=codex_exec` 后，公开源精确为 32 步、唯一 probe、固定 target/workspace/prompt/180 秒 arguments 完全一致、`timeout_ms=360000`；production local 物化文件与公开源逐字一致 | 使用 production validator 对 Case 11 单独执行 explicit-request preview；通过后再启动 fresh runtime |
| 2026-08-07 05:53 | Case 11 production 物化与 workflow 静态批次 | `config.local.yaml` -> `build/workflows-local`；28 个 workflow | 官方静态校验通过；附加探针 selector 无效 | 隔离 local 目录生成 28/28，`YAML.safe_load` 28/28；官方 `validate_workflows.rb` 28/28 通过并确认 Case 11 为 32 步、0 external call、唯一 `codex_exec`。末尾自定义 Ruby 探针错误假设 `type=codex_exec`，以 `expected one codex_exec` 本地退出，故该附加结论无效；未输出 replacement 值，产物均 ignored | 只读取公开源定义的 step 结构纠正附加 selector并复核固定参数/360000 timeout；通过后进入 Case 11 production preview |
| 2026-08-07 05:53 | Case 11 production materialization 入口预检 | `scripts/materialize_workflows.rb` | 首次参数探测本地退出，未生成文件 | 物化器不实现 `--help`，首次探测将其当作配置路径并以本地 `ENOENT` 退出；源码确认正确契约为 `<配置.yaml> [输出目录]`，第二个位置参数可用于隔离 local 产物。`config.local.yaml` 与 `build/` 均由 gitignore 覆盖，本批未读取配置值 | 使用 `config.local.yaml build/workflows-local` 正确物化；随后解析 28 个 YAML 并运行完整 workflow validator |
| 2026-08-07 05:52 | Case 11 NyxID delegation 策略独立回读 | 唯一 personal `chrono-sandbox`；ID hash `872833a52fc1` | 通过，生产配置已恢复 | 新进程重新执行 `service list` 后精确命中同一 ID hash 的唯一目标；active/connected/personal 保持 true，三项策略精确为 `forward_access_token=false`、`inject_delegation_token=true`、scope=`proxy:*`。未输出原始 ID、URL、header 或 token | 使用 ignored `config.local.yaml` 重新物化到隔离 local build 目录；完成 YAML 与完整 workflow 静态校验后单独执行 Case 11 preview |
| 2026-08-07 05:52 | Case 11 NyxID delegation 策略更新请求 | 唯一 personal `chrono-sandbox`；ID hash `872833a52fc1` | CLI 命令成功，等待独立回读 | 命令发送前在同一进程再次要求唯一 active/connected/personal 目标且旧值精确为 `false/true/proxy:* sandbox:execute`；正式 `service update` 仅显式设置 `false/true/proxy:*` 并退出 0，原始 update 响应已丢弃。CLI ack 本身不作为最终配置证据 | 另起 `service list` 独立脱敏回读同一 ID hash、lifecycle/owner 和三项策略；精确通过后再物化 production workflow |
| 2026-08-07 05:50 | Case 11 NyxID 配置更新最终前置断言 | 唯一 personal `chrono-sandbox`；ID hash `872833a52fc1` | 通过，只读 | 修正 selector 为 `credential_source.type` 后精确命中 1 条 active/connected/personal 目标；当前策略权威回读为 `forward_access_token=false`、`inject_delegation_token=true`、scope=`proxy:* sandbox:execute`，与预期迁移前状态一致 | 使用正式 typed CLI 显式设置 `false/true/proxy:*`，丢弃原始 update 响应；随后另起 `service list` 独立脱敏回读 |
| 2026-08-07 05:50 | Case 11 NyxID 候选分层只读归因 | `chrono-sandbox` inventory；ID hash `872833a52fc1` | 唯一目标存在；选择器字段已纠正 | 仅按 slug 精确命中 1 条，active/connected true，scope 仍为 `proxy:* sandbox:execute`；`credential_source` 对象仅有 `type` 键，旧谓词错误使用 `kind`。本批 jq 的 `// null` 还会把合法 false 折叠成 null，因此 forward 值没有有效结论；未发送 update | 使用 `.credential_source.type` 和保留 false 的读取方式完成最终唯一性/当前值断言；通过后仅更新三项 identity 策略 |
| 2026-08-07 05:50 | Case 11 NyxID 配置更新前唯一性断言 1 | `chrono-sandbox` inventory | 本地前置断言失败，未发送 PUT | 将 slug、active、connected、`credential_source.kind=personal` 合并筛选后候选数为 0；命令在任何 update 前退出，只输出计数。该结果不能证明服务缺失，可能是当前 JSON 的 owner/source 表达与选择器假设不一致 | 分层只读统计 slug 命中及其 active/connected/credential source 安全字段；修正选择器后仍要求精确唯一才更新 |
| 2026-08-07 05:50 | Case 11 NyxID inventory schema 预检 | NyxID #1337 正式 CLI | 通过，只读 | `service list --output json` 返回对象根且服务数组位于 `keys`；目标选择和 identity 更新所需的 slug、credential source、active/connected、forward/inject/scope 字段均存在。本批只输出结构与字段名，未输出服务值、ID、URL、header 或 token | 从 `keys` 精确筛选唯一 active/connected/personal `chrono-sandbox`，仅输出 ID hash 和三项策略；唯一性通过后才更新 |
| 2026-08-07 05:49 | Case 11 兼容修正 Pod 收敛复核 | production image `6a656d75` | 通过 | 固定 selector 权威回读精确 1 个 Pod，phase 为 Running、全部 container Ready、restart 总数 0、deletion pending 0，唯一镜像为 `6a656d75`；未保存 Pod 名称 | 使用 NyxID #1337 构建的正式 CLI 先只读断言唯一 active/connected/personal `chrono-sandbox` 当前仍为双 scope，再更新为 `false/true/proxy:*` 并独立脱敏回读 |
| 2026-08-07 05:49 | Case 11 兼容修正目标 Ready 部署 | target `6a656d759`；production image `6a656d75` | Ready，可追溯包含修正 | 目标 8 位 tag manifest 已可读取；deployment generation/observed `2725/2725`，desired/updated/ready/available 1/1、unavailable 0，Available/Progressing true，现役镜像精确为 `6a656d75`。远端 tip 已在上一批精确回读为完整提交 `6a656d7593c655ba565565824431802c85e2de46` | 独立复核唯一 Ready Pod 与 restart；通过后使用 NyxID 正式 CLI 将唯一 personal `chrono-sandbox` 恢复为 `false/true/proxy:*` 并脱敏回读 |
| 2026-08-07 05:47 | Case 11 manifest/workload 联合轮询 6 | 目标 tag `6a656d75`；Aevatar production workload | 等待构建与部署，无状态变化 | 又一轮 55 秒后目标 manifest 仍不存在；deployment 继续为 generation/observed `2723/2723`、1/1 Ready、unavailable 0、镜像 `a44f849e`，没有新 rollout 或失败条件 | 继续低频有界轮询；不重复旧镜像 Case 11，不提前修改 delegation scope |
| 2026-08-07 05:46 | Case 11 manifest/workload 联合轮询 5 | 目标 tag `6a656d75`；Aevatar production workload | 等待构建与部署，无状态变化 | 间隔 55 秒后目标 manifest 仍不存在；deployment 保持 generation/observed `2723/2723`、desired/updated/ready/available 1/1、unavailable 0、镜像 `a44f849e`。Progressing 仍指向既有 ReplicaSet，未出现新 rollout 或失败 | 继续 60 秒级低频有界轮询；目标或明确后代 Ready 前保持 UserService 和 Case 11 不变 |
| 2026-08-07 05:44 | Case 11 manifest/workload 联合轮询 4 | 目标 tag `6a656d75`；Aevatar production workload | 等待构建与部署，无状态变化 | 间隔 45 秒后目标 manifest 仍不存在；deployment 仍为 generation/observed `2723/2723`、1/1 Ready、unavailable 0、镜像 `a44f849e`，Available/Progressing true，无新 rollout 或失败信号 | 将轮询间隔拉长到 60 秒；继续等待 manifest 和可追溯 Ready workload |
| 2026-08-07 05:43 | Case 11 目标镜像 manifest 修正探针 | `aevatar-console-backend:6a656d75` | 等待构建 | 改用非保留变量后，同一 8 位提交 tag 的只读 registry manifest 探针正常完成并返回不存在；结合远端 tip 已精确推送、production 仍为 `a44f849e`，当前尚无可部署目标镜像，也无 rollout failure 可归因 | 保持低频有界轮询 manifest 与 authoritative workload；目标镜像 Ready 前不修改 UserService |
| 2026-08-07 05:43 | Case 11 发布链只读复核 | `origin/feature/integrate`、GitHub Actions、目标镜像 tag | 远端稳定；manifest 首次探针无有效结论 | fetch 后远端 tip 仍精确为 `6a656d759` 且包含目标；分支最近 workflow run 不含该提交，符合该路径由仓库外发布链驱动的既有事实。目标 8 位 tag 的 manifest 命令已执行，但本地 zsh 在用只读变量 `status` 收集退出码时中断，故本次没有可采信的 manifest 存在/缺失结论 | 改用非保留变量重试同一只读 manifest 探针；随后继续权威 workload 轮询 |
| 2026-08-07 05:42 | Case 11 兼容修正部署轮询 3 | Aevatar production workload | 等待部署，无状态变化 | 间隔 45 秒后再次权威回读；deployment 继续为 generation/observed `2723/2723`、desired/updated/ready/available 1/1、unavailable 0，现役镜像 `a44f849e`，Available/Progressing true，尚无目标 rollout 或失败条件 | 只读复核远端 tip、目标镜像构建迹象与 workload；继续有界低频等待，不在旧部署运行 Case 11 |
| 2026-08-07 05:41 | Case 11 兼容修正部署轮询 2 | Aevatar production workload | 等待部署，无状态变化 | 使用固定 production kubeconfig 权威回读；deployment generation/observed `2723/2723`，desired/updated/ready/available 1/1、unavailable 0，Available/Progressing 均为 true，现役镜像仍为 `a44f849e`；尚未包含 `6a656d759`，没有 rollout 失败 | 继续低频有界轮询；Ready 镜像可追溯包含修正前不修改 UserService、不运行 Case 11 |
| 2026-08-07 05:39 | Case 11 兼容修正部署轮询 1 | Aevatar production workload | 等待部署，无状态变化 | deployment generation/observed `2723/2723`，desired/updated/ready/available 1/1、unavailable 空，现役镜像仍为 `a44f849e`；尚未包含已推送的 `6a656d759`，没有 rollout 失败 | 保持低频有界轮询；新 Ready 镜像可追溯包含修正前不修改 UserService、不运行 Case 11 |
| 2026-08-07 05:38 | Case 11 Aevatar 兼容修正提交与普通推送 | `origin/feature/integrate` | 已推送 `6a656d7593c655ba565565824431802c85e2de46` | 提交 `Restore managed Codex canary delegation scope` 只包含 resolver、lifecycle/readiness fixture 和四份 managed-codex 文档；提交前远端仍为 `a44f849e`，无需 rebase。普通 push 成功后 `git ls-remote` 回读远端 tip 与本地提交精确一致；隔离 worktree 干净，未 force push | 有界轮询 Aevatar production workload；只有 Ready 镜像可追溯包含 `6a656d759` 后，才把唯一 personal `chrono-sandbox` 恢复为 `false/true/proxy:*` 并 fresh 执行 Case 11 preview/runtime |
| 2026-08-07 05:36 | Case 11 Aevatar 下游契约兼容修正 | 隔离 worktree；基线/远端 `a44f849e` | 修正完成，门禁通过；尚未提交部署 | chrono-sandbox PR #8 head `1e8134d` 的专用 verifier 明确要求唯一 `X-NyxID-Delegation-Token` 且 scope 精确为 `proxy:*`，双 scope 会以 `CODEX_DELEGATION_SCOPE_INVALID`/401 fail closed。Aevatar resolver、lifecycle/readiness fixture 与四份 managed-codex 文档已恢复该当前发布契约，7 个文件与 `a44f849e` 父提交逐字一致。完整 ChronoSandbox 项目 213/213、workflow saga architecture 15/15、test stability、87 份 docs lint、architecture guards 与 `git diff --check` 全部通过；仅有仓库既有 warning | 最终审核 7 文件逆向 diff 与敏感信息，fetch 远端后提交并普通推送 `origin/feature/integrate`；等待可追溯 Ready 部署，再将唯一 personal UserService 恢复为 `false/true/proxy:*` 并 fresh 复验 Case 11 |
| 2026-08-07 05:27 | Case 11 下游认证归因 | NyxID #1337、chrono-sandbox PR #8 head `1e8134d`、Aevatar Ready `a44f849e` | 已定位跨仓 scope 迁移缺口，未重跑 | 分支源码纠正了仅检查 chrono-sandbox `main` 得出的初步判断：专用 `POST /codex/execute` 会绕过通用 bearer/introspection 中间件，精确读取 `X-NyxID-Delegation-Token` 并在本地验签。其 verifier 仍只接受单 scope `proxy:*`，所有 claim 错误均返回 401；NyxID #1337 已正式允许 service-issued `sandbox:execute`，当前 Aevatar resolver 与 UserService 则要求精确双 scope `proxy:* sandbox:execute`。生产 managed key 的 `last_used_at` 已命中失败请求，排除 Aevatar `X-API-Key` 未认证。故当前 401 是跨仓 scope 契约不同步，不是 header 缺失、capacity 或 workflow 编排问题 | 以实际部署的 chrono-sandbox PR #8 契约和 Aevatar 既有 canon/runbook 为准核对漂移归属；只修错误一侧，不重复运行制造绿色 |
| 2026-08-07 05:21 | Case 11 双 scope 修复后 fresh runtime | Ready `a44f849e`；run hash `0bd906f3f32e` | committed `failed`，尚未闭环 | Preview 通过并复用 revision；run committed `failed`、state 31、4/4，稳定错误 `codex_execution_admission_denied`，无 final artifact、approval 或副作用。该结果说明 Aevatar 新 resolver 与 UserService 双 scope 配置已被接受，但执行链仍在下游 authorization 边界拒绝，不能以本地修复或 preview 宣称通过 | 读取本次有界 production log，区分 NyxID delegation token minting、chrono-sandbox `sandbox:execute` introspection、Agent Key grant 或旧 credential descriptor；只在新增证据后决定修复 |
| 2026-08-07 05:20 | Case 11 双 scope 修复后 fresh preview | Ready `a44f849e`；scope hash `237314c29964` | 通过 | 固定 managed `codex_exec` 定义通过 explicit-request preview；`inlineWorkflowCount=0`、`callSiteCount=0`，`runEnabled=false`，未 provision、未启动 workflow、未产生副作用。scope/team/Lark service 原值只在进程内使用，公开输出仅保留 hash | 使用同一 production materialization 和 Ready 部署启动 Case 11 fresh runtime；等待 committed terminal 并精确断言固定输出、五项 gate、parallel=5、side effects=false |
| 2026-08-07 05:19 | Case 11 部署后 production 配置物化与静态复核 | `config.local.yaml`；动态 workflow inventory | 通过，28/28 | 16 个本地 replacement 重新生成 28 个 ignored workflow，28/28 通过 `YAML.safe_load` 与完整 `validate_workflows.rb`；Case 11 保持 32 步、0 external call、唯一固定 `codex_exec`。`config.local.yaml` 与 `build/` 均为 ignored，命令未输出 replacement 值 | 经已认证 NyxID 入口在 Ready `a44f849e` 上单独执行 Case 11 fresh explicit-request preview |
| 2026-08-07 05:18 | Case 11 NyxID 双 scope 配置更新 | 唯一 personal `chrono-sandbox`；ID hash `872833a52fc1` | 通过，生产配置已回读 | 更新前选择器精确命中 1 条 active/connected/personal 目标且策略为 `false/true/proxy:*`；使用 NyxID #1337 正式 CLI identity-only update，仅将 delegation scope 改为 `proxy:* sandbox:execute`。丢弃原始 update payload 后独立 `service list` 确认同一 ID hash、active/connected/personal 与 `false/true` 均保持，scope 精确为双 token；未输出原始 ID 或 token | 在 Ready `a44f849e` 上 fresh 执行 Case 11 explicit-request preview；通过后再运行 committed runtime |
| 2026-08-07 05:16 | Case 11 Aevatar 修复部署收敛 | production image `a44f849e`；commit `a44f849e06321fb777db38a5d815a16e790507b0` | Ready，可追溯包含修复 | rollout 从 generation `2723/2723` 的 surge 状态收敛为 desired/updated/ready/available 1/1、unavailable 空；唯一新 Pod Running/Ready、0 restart，deployment conditions 为 Available/Progressing true。镜像短 tag `a44f849e` 与 `origin/feature/integrate` 精确 tip 前缀一致 | 使用 NyxID #1337 正式 CLI 将唯一 personal `chrono-sandbox` 更新为 `false/true` 与精确双 scope；脱敏回读后 fresh 运行 Case 11 preview/runtime |
| 2026-08-07 05:14 | Case 11 修复后部署轮询 5 | Aevatar production deployment | 等待部署，无状态变化 | 完整 60 秒间隔后 authoritative get 仍为 generation/observed `2721/2721`、desired/updated/ready/available 1/1、镜像 `4c0596c7` | 继续下一次 60 秒低频轮询，不在旧镜像上修改双 scope |
| 2026-08-07 05:12 | Case 11 修复后 deployment watch 与权威回读 | Aevatar production deployment/Pod/ReplicaSet | 仍等待部署；watch 终止不代表对象消失 | watch 在无中间变更后输出一条字段全空事件并结束；立即按同名 deployment、固定 selector Pod 和 ReplicaSet 三面回读，均确认 `2721/2721`、1/1 Ready、Pod Running/ready、0 restart，现役 ReplicaSet 仍为 `4c0596c7` 且无新镜像。未把 watch transport 终止误报为 rollout | 恢复 60 秒级显式只读轮询；出现新 generation/image 后再检查 Ready 和 commit 可追溯性 |
| 2026-08-07 05:11 | Case 11 修复后部署轮询 4 | Aevatar production workload | 等待部署，无状态变化 | GitHub trigger 归因后复核 production 仍为 generation/observed `2721/2721`、1/1 Ready、镜像 `4c0596c7`；没有失败 rollout 可读取日志，NyxID scope 保持旧值以避免旧 resolver 拒绝 | 继续 60 秒级低频轮询，等待发布链更新 workload |
| 2026-08-07 05:10 | Case 11 修复提交 CI trigger 归因 | Aevatar GitHub workflows 与 commit status | 无 check-run 为预期，不是构建失败 | commit check-runs 为 0、legacy status 无 context；源码确认 `ci.yml` push 只监听 `main`/`dev`，`codex-runner-image` 对 `feature/integrate` 仅监听 runner/包版本路径，本提交均不命中。未手动 dispatch；production 发布由仓库外链路负责 | 返回 production workload 的低频有界轮询，等待 generation/image 更新并验证 Ready 可追溯性 |
| 2026-08-07 05:09 | Case 11 修复提交 CI 只读发现 | GitHub Actions；`feature/integrate` | 当前无 `a44f849e0` 对应 run | `gh run list` 返回的 branch run 最新 head 仍为前一日提交，没有当前修复 commit；未触发、重跑或取消任何 workflow。该空结果不能外推为 CI 成功或失败，也不改变生产仍为 `4c0596c7` 的事实 | 读取 workflow trigger 与 commit check/status API，确认 direct push 的实际构建/部署入口；继续低频生产轮询 |
| 2026-08-07 05:08 | Case 11 修复后部署轮询 3 | Aevatar production workload | 等待部署，无状态变化 | 约 45 秒后仍为 `2721/2721`、1/1 Ready、镜像 `4c0596c7`；无 rollout 事件进入生产 | 只读检查 `feature/integrate` GitHub Actions 状态，区分排队、构建、发布失败或部署等待 |
| 2026-08-07 05:07 | Case 11 修复后部署轮询 2 | Aevatar production workload | 等待部署，无状态变化 | 间隔复核仍为 generation/observed `2721/2721`、desired/updated/ready/available 1/1，镜像 `4c0596c7`；未出现 rollout 失败，也未提前运行 Case 11 | 延长有界轮询间隔，继续等待包含 `a44f849e0` 的 Ready 镜像 |
| 2026-08-07 05:06 | Case 11 修复后部署轮询 1 | Aevatar production workload | 等待部署，无状态变化 | deployment generation/observed `2721/2721`，desired/updated/ready/available 均 1，当前镜像仍为 `4c0596c7`；尚不包含已推送的 `a44f849e0`，未调用生产 workflow 或修改 NyxID 配置 | 保持有界低频轮询；只有新 Ready 镜像可追溯包含修复后才进入 UserService scope 更新与 Case 11 fresh preview/runtime |
| 2026-08-07 05:05 | Case 11 Aevatar 修复提交与普通推送 | `origin/feature/integrate` | 已推送 `a44f849e06321fb777db38a5d815a16e790507b0` | 提交 `Fix managed Codex delegation scope contract` 只包含 resolver、两组回归测试和四份 managed-codex 文档；提交前再次 fetch，远端仍为基线 `4c0596c7`，无需 rebase。普通 `git push origin HEAD:feature/integrate` 成功，随后 `git ls-remote` 回读远端 tip 与本地提交精确一致；隔离 worktree 干净 | 有界轮询生产 workload；只有 Ready 镜像可追溯包含 `a44f849e0` 后，才更新唯一 personal `chrono-sandbox` scope 并 fresh 复验 Case 11 |
| 2026-08-07 05:03 | Case 11 Aevatar 扩展静态门禁 | 同一隔离修复 worktree | 通过 | 完整 `Aevatar.AI.Infrastructure.ChronoSandbox.Tests` 219/219；`test_stability_guards.sh` 通过；docs lint 检查 87 个文件、0 error；`architecture_guards.sh` 全部通过，其中 workflow saga architecture tests 15/15。输出包含仓库既有 package/analyzer warning，不含本轮新增 error，`git diff --check` 保持通过 | 复核最终 diff 和敏感信息，fetch 最新 `origin/feature/integrate`；若远端未前进则提交，否则 rebase 后重跑受影响门禁 |
| 2026-08-07 05:00 | Case 11 Aevatar scope 契约修复与聚焦单测 | 隔离分支 `codex/fix-managed-codex-delegation-scope-20260807`；基线 `4c0596c7` | 修复完成，聚焦测试 149/149 通过；尚未部署 | NyxID #1337 与本地 chrono-sandbox 源码共同确认执行链需要精确 scope 集合 `proxy:* sandbox:execute`：前者供固定 LLM REST proxy，后者供 chrono-sandbox execution admission。Aevatar resolver 已改为按空白 token set 无序比较，缺项或额外 scope 均 fail closed；lifecycle/readiness fixture、正反序/空白/负例测试及 managed-codex ADR、canon、runbook、design 已同步。`dotnet test` 聚焦 149 项全部通过，仅出现既有编译 warning | 运行 test stability、文档与架构门禁及完整 ChronoSandbox 测试；通过后 fetch/rebase、提交并普通推送 `origin/feature/integrate` |
| 2026-08-07 04:53 | Case 11 第二次请求完整 HTTP 窗口与归因纠正 | run hash `bf2d79dcc55e`；NyxID proxy | 确认为 HTTP 502，不是无 status 异常 | 从 request start 到 typed failure 的 271 行有界窗口确认：Aevatar POST `/proxy/s/chrono-sandbox/codex/execute` 后 10803 ms 收到 HTTP 502。源码 `ProxyFailure` 将 429/502/503 映射为 `managed_proxy_unavailable`；因此 04:50 的 `HttpStatus=0` 推断被证伪并已原位修正。同期 Aevatar/NyxID health 均 200，问题位于 execute 上游/runner capacity，不是 Aevatar workflow 或 HTTP client 丢失 status | 获取 NyxID/chrono-sandbox 对 502 的稳定下游诊断；不得把 health 绿色外推为 execution capacity 绿色，也不再重复相同 Aevatar run |
| 2026-08-07 04:52 | Case 11 managed proxy fresh 重试 | 当前生产；run hash `bf2d79dcc55e` | committed 同边界失败，不再盲重试 | Preview 通过、复用 revision 后 committed `failed`、state 31、4/4，仍为 `codex_execution_capacity_unavailable`，无 artifact、approval 或副作用。完整日志随后确认本次为 NyxID proxy HTTP 502；同 UserService health 绿色只能证明路由和 OpenSandbox 连接存活，不能证明 execute capacity | 读取 NyxID/chrono-sandbox 的 502 typed body 或脱敏日志，区分 provider、runner 分配和队列容量；没有新增证据前不继续重跑 |
| 2026-08-07 04:51 | Case 11 chrono-sandbox health 与 catalog 边界 | 同一 UserService；catalog `GET /health` | health 通过，支持一次 transport 重试 | NyxID catalog 精确声明 9 个 endpoint，包含 `POST /codex/execute` 与无副作用 `GET /health`；通过同一 UserService 的 health 返回 `status=healthy`、`opensandbox_connected=true`。该证据只证明路由与 OpenSandbox 连接存活，不覆盖 execute runner/provider capacity | 在 health 新证据下仅 fresh 重试 Case 11 一次；若仍失败，读取完整 HTTP window，不做无证据循环 |
| 2026-08-07 04:50 | Case 11 transport typed mapping 源码复核 | Aevatar `NyxIdManagedCodexChronoTransport` | code 单独不足以区分 status 0 与 429/502/503 | 第二次 10 分钟筛选未再返回目标上下文，未把空窗口当恢复证据。源码确认 `managed_proxy_unavailable` 既用于 bounded client `HttpStatus=0`，也由 `ProxyFailure` 对 429/502/503 生成；必须读取完整 HTTP client window 才能归因 | 读取 chrono-sandbox catalog/readiness并在 fresh 重试后保留完整 request/response status 窗口 |
| 2026-08-07 04:49 | Case 11 有界生产日志与 Ready 归因 | deployment `4c0596c7`；run hash `9ef41164b0b9` | 定位到 NyxID managed proxy unavailable | Aevatar deployment generation 2721/2721、desired/updated/ready/available 均 1，Pod Running/Ready、0 restart。10 分钟有界日志确认 `execute_probe` 已 POST NyxID `chrono-sandbox /codex/execute`，随后 transport code=`managed_proxy_unavailable`，Aevatar 将其 typed 映射为 `CapacityUnavailable`；日志与 committed run hash 一致并已脱敏 | 读取同请求前后有界日志中的 HTTP status/异常边界；只读检查 chrono-sandbox UserService proxy/readiness，区分 NyxID 503、下游不可达或 managed runner capacity |
| 2026-08-07 04:48 | Case 11 delegation 修复后 fresh runtime | 当前生产；run hash `9ef41164b0b9` | committed 失败，断点已推进 | Preview 继续通过；复用现有 revision 后运行 committed `failed`、state 31、4/4，失败步骤仍为 `execute_probe`，但稳定错误已从 `codex_execution_admission_denied` 变为 `codex_execution_capacity_unavailable`，无 final artifact、无 approval、无副作用。说明 NyxID `false/true/proxy:*` 已越过原 admission gate，当前失败属于 managed capacity/transport 新边界 | 使用有界 Aevatar production logs 和 managed readiness/transport 只读接口关联本次时间窗；区分 Aevatar capacity 映射、NyxID proxy、chrono-sandbox availability 或 runner capacity |
| 2026-08-07 04:47 | Case 11 delegation 修复后 fresh preview | 当前生产；scope hash `237314c29964` | 通过 | 固定 managed `codex_exec` 定义通过 explicit-request preview；`inlineWorkflowCount=0`、`callSiteCount=0`，无 NyxID 外部业务 call site。`runEnabled=false`，未 provision、未启动 workflow、未产生外部写入 | 使用同一 production materialization 与 scope/team 运行 Case 11，等待 committed terminal；精确检查 `CODEX_EXEC_READY`、五项 gate、parallel=5、side effects=false |
| 2026-08-07 04:47 | Case 11 production 配置物化 | `config.local.yaml`；当前动态 inventory | 通过，28/28 | 16 个本地 replacement 重新生成 28 个 ignored workflow，28/28 均通过 `YAML.safe_load`；命令输出不含 replacement 值，`config.local.yaml` 与 `build/` 不进入跟踪 | 使用已认证 scope/team 在 Case 11 单独执行 explicit-request preview；仅 preview 契约通过后进入 fresh runtime |
| 2026-08-07 04:46 | Case 11 scope/team 权威只读发现 | scope hash `237314c29964`；team hash `db2102f24ba6` | 通过，唯一 active 匹配 | Aevatar 源码确认 roster 路由为 `/api/scopes/{scopeId}/teams`；经 NyxID 回读当前 scope 的 5 个 team，`config.local.yaml` 指定 team 在 scope/team 双字段上匹配 1 条且 lifecycle 为 active，member count 113。全程只在进程内使用原始 scope/team，公开日志只保留 12 位 hash | 用本地配置重新物化 28 个定义；运行 Case 11 fresh preview，通过后立即运行并等待 committed terminal artifact |
| 2026-08-07 04:45 | Case 11 scope/team 只读发现首次尝试 | NyxID `/api/auth/me` 与猜测 `/api/studio/teams` | scope 可读；team 路由 404 | `/api/auth/me` 经 NyxID 返回 authenticated profile/session/scope 结构，未输出真实 scope；猜测的 `/api/studio/teams` 为不存在路由并返回 HTTP 404，未创建或修改 team/member。不能用该 404 推断 team 缺失 | 从 Aevatar `origin/feature/integrate` 路由和前端 API helper 精确读取 team catalog 路径，再通过 NyxID 只读请求恢复唯一 team |
| 2026-08-07 04:45 | Case 11 production 运行前置检查 | 当前桌面 shell | 本地上下文缺失，未请求生产 case | `config.local.yaml` 存在，但本 shell 未设置 `AEVATAR_SCOPE_ID` / `AEVATAR_TEAM_ID`；`kubectl` 没有 current-context 并只尝试 localhost，不能用作 Ready 证据。上述检查未调用 Aevatar case、未改变生产状态，也不能推断部署异常 | 仅通过已认证 `nyxid proxy request aevatar` 读取 `/api/auth/me` 与 Studio team read model，在内存中恢复唯一 scope/team；随后物化并运行 Case 11 |
| 2026-08-07 04:44 | Case 11 `chrono-sandbox` delegation 策略修复 | 唯一 personal UserService，ID hash `872833a52fc1` | 通过，生产配置已修正 | 使用 NyxID #1337 正式 CLI identity-only 更新路径，只设置 `forward_access_token=false`、`inject_delegation_token=true`、scope=`proxy:*`；update ack 与随后独立 `service list` 的 ID hash 一致。回读同时确认目标仍 active、connected、personal；未输出或持久化原始 ID、token 或完整服务响应 | 用 `config.local.yaml` 恢复生产定义，确认当前 Ready 部署后运行 Case 11 fresh preview 与 committed runtime；以固定 output、五项 gate 和无副作用 artifact 判定 |
| 2026-08-07 04:43 | Case 11 NyxID 策略更新首次尝试 | 唯一 `chrono-sandbox` 候选 | 本地前置断言失败，未发送 PUT | 更新命令在解析 inventory 时把结构化 `credential_source` 误按字符串比较，因“唯一 active personal”计数不为 1而立即退出；没有调用新 CLI 的 update 子命令，生产三项策略保持原值。该失败属于本地选择器缺陷，不是 NyxID 或 Aevatar 失败 | 将 personal 判定改为读取 `credential_source.kind`，再次要求 slug/count/active/connected 精确命中后执行同一三字段更新 |
| 2026-08-07 04:43 | NyxID #1337 CLI 聚焦测试与隔离构建 | NyxID `origin/main` `047dc0b4` 临时 worktree | 通过，2/2 + build | `service_update_accepts_identity_delegation_flags` 1/1、`identity_only_update_resolves_and_updates_user_service` 1/1；后者精确证明只读解析 key 后仅向目标 `/user-services/{id}` PUT 三项 identity 策略。`cargo build --locked -p nyxid-cli` 成功；未修改 NyxID 主工作树或生产配置 | 使用构建产物和 inventory 中唯一 `chrono-sandbox` ID 执行 `false/true/proxy:*` 更新；立即脱敏回读 count、owner、active 与三项策略，不记录原始 ID |
| 2026-08-07 04:37 | Case 11 NyxID 配置入口与当前策略只读复核 | 唯一 personal `chrono-sandbox`；NyxID #1337；Aevatar `origin/feature/integrate` | 已定位正式修复入口，尚未修改 | 已认证 inventory 恰有 1 个 active、connected、personal 目标，当前策略为 `forward_access_token=true`、`inject_delegation_token=true`、scope=`llm:proxy`，仅保留 12 位 ID hash。Aevatar 当前 canon、resolver 与测试精确要求 `false/true/proxy:*`；NyxID 提交 `5c89e104` 已为 `service update` 增加三项 typed CLI 参数、identity-only `PUT /user-services/{id}` 路径和单元测试，不需要临时 HTTP 或放宽 Aevatar | 在 NyxID 最新远端源码的隔离构建中运行 CLI 参数/identity update 测试；通过后仅更新这一条 UserService，并立即脱敏回读三项策略 |
| 2026-08-07 04:35 | 验收闭环恢复与三仓边界复核 | 当前验收工作树、Aevatar、NyxID | 已恢复，未执行生产 case | 验收仓库保留上一轮全部未提交证据；Aevatar 主工作树仍含用户修改，平台修复继续要求最新 `origin/feature/integrate` 隔离 worktree；本地 NyxID `main` 落后远端 80 个提交，不能再用旧 CLI 能力断言 Case 11 无配置入口。本批未读取或修改凭据，未产生外部写入 | 基于 NyxID 最新远端源码核对 UserService 更新契约与已认证 CLI 能力；若存在安全管理入口，精确修正 `chrono-sandbox` 后 fresh 重跑 Case 11 |
| 2026-08-07 03:52 | 最终 inventory、脱敏与工作树扫描 | 当前最终工作树 | 通过扫描；active 运行面未全绿 | 最终重算仍为 workflow/Ornn skill/production registry 28/28/28、channel 3、risk 21、regression 1、schedule 1；`config.local.yaml` 与 `build/` 均 ignored 且未跟踪。变更文件未发现 bearer、JWT、UUID 或原始 Lark/Base ID；初筛候选均确认为 `receiptStatus`、`records...`、`received...` 等契约字段名。report validator 与 `git diff --check` 通过；保留既有脏工作树，不暂存、不提交验收仓库 | 外部管理员将 NyxID `chrono-sandbox` UserService 调整为不转发 access token 且授予 `proxy:*` 后重跑 Case 11；Case 06 与 Risk 29/30/40/41/42 仅在提供 disposable target 和安全清理契约后 fresh 执行 |
| 2026-08-07 03:50 | 仓库级 Skill 双端官方校验 | `.agents/skills/aevatar-acceptance-loop` 与 `.claude/skills/aevatar-acceptance-loop` | 通过，2/2 | 使用 `skill-creator/scripts/quick_validate.py` 的一次性 PyYAML 环境分别校验实体目录和 Claude Code 软链接，均输出 `Skill is valid!`；`agents/openai.yaml` 可解析且默认提示显式引用 `$aevatar-acceptance-loop`。Skill 的批次 README 回写要求已在本轮逐批执行 | 重扫敏感信息、ignored 生成物、最终 inventory、报告门禁与工作树状态；保留无法安全执行项目的诚实 blocker |
| 2026-08-07 03:49 | 最终完整静态与 CI 补充门禁 | 当前工作树；`config.example.yaml` | 通过；运行阻塞状态未改写 | workflow 28/28、Ornn skill 28/28、channel 3/3、regression 1/1 通过；risk 为 16 passed、5 not-configured。Production/Assistant/publish/admission/catalog Ruby 语法、示例物化与 28/28 YAML 解析、28 包打包、报告契约、HTML 内联脚本和 `git diff --check` 全部通过；report validator 输出 workflow=28、风险案例=21、能力矩阵=19、自然语言=5、阻塞=3 | 运行官方 Skill `quick_validate.py` 并回写 README；随后重扫敏感信息、ignored 生成物、最终 inventory 与工作树状态 |
| 2026-08-07 03:47 | 最终动态 inventory 批次 | 当前工作树；Codex/Claude `aevatar-acceptance-loop` | 通过，运行状态仍有外部/安全阻塞 | 从最终工作树重新发现 28 个 workflow、28 个一一对应 Ornn skill、production registry 28 项、3 个 channel case、21 个 risk case、1 个 regression case和 1 个 schedule；Codex `.agents/skills` 与 Claude `.claude/skills` 入口均可读且指向同一 Skill。Skill 不保存 case 编号或数量，并明确要求每个验证批次后先更新 README | 执行 AGENTS.md 完整静态提交前检查、CI 补充门禁和官方 Skill validator；每个批次结束后先回写 README，再做最终敏感信息与工作树扫描 |
| 2026-08-07 03:45 | Fresh schedule 机器/报告一致性复验 | 当前 `4c0596c7` 证据 + 历史 `b010ba61` 证据 | 通过 | 新增 `validation/production-validation-2026-08-07-schedule.json`，强制检查 current Ready 部署、typed confirmation/admission、state 7/11 provisioning、owner-scoped schedule detail、1 次自然 cron、`b9859494e2a9` 11/11 artifact、typed DELETE、`1 -> 1` 静默窗口，以及前三轮诊断 schedule 全部清理。JSON parse、Ruby 语法、定向 diff 均通过；`validate_report.rb` 正式输出 workflow=28、历史直接案例=20、新增探针=5、风险案例=21、能力矩阵=19、自然语言=5，全部通过 | 执行最终动态 inventory，再跑 AGENTS.md 完整静态提交前检查、CI 补充门禁、敏感信息与误跟踪生成物扫描 |
| 2026-08-07 03:44 | Fresh schedule 证据与报告定向门禁 | 机器 JSON、README、Markdown、HTML、`validate_report.rb` | 部分通过；report validator 失败 | 新增的当前 schedule 机器证据 JSON 可解析，`validate_report.rb` Ruby 语法通过，五个相关文件的 `git diff --check` 通过。正式 report validator 在“文字报告缺少 Durable schedule 分阶段生产闭环”处失败，说明 Markdown 文案与新旧双证据强断言尚有一项字符串不一致；未改写为通过 | 逐项回读该组断言的布尔值，仅修正实际漂移的报告/门禁文案，重跑 report validator；通过后再执行最终全量静态批次 |
| 2026-08-07 03:36 | Case 15 schedule fresh 完整闭环 | Ready `4c0596c7`；`weekly-budget-variance-digest` | 通过；已清理 | Confirmation 为 1 workflow / 6 GET / read-only / durable，token 未持久化；typed admission 为 `accepted/pending_binding`，binding state 7、provisioning state 11 / attempt 2 committed `succeeded`。Owner-scoped detail 确认 schedule enabled、`* * * * *`、`Asia/Singapore`；1 次自然 cron 的 run hash `b9859494e2a9` 为 state 73、11/11 committed `completed`，周实际/ 预算 2340/2400、月投影/ 预算 9360/9600、over/watch 1/1、六路 live read 和无副作用全部命中。Typed DELETE 为 `accepted/pending`，detail 消失、owner list 0，跨下一分钟 run `1 -> 1`；仅保留 12 位哈希 | 将 fresh schedule 证据同步到机器摘要、Markdown/HTML 报告和报告 validator；随后执行最终动态 inventory 与全部静态门禁 |
| 2026-08-07 03:32 | Case 15 schedule fresh 第三轮、detail schema 诊断与清理 | Ready `4c0596c7`；owner-scoped `/api/schedules/{id}` | 验收附加断言失败；未进入 cron；副作用已清理 | Fresh confirmation/admission 与 committed binding state 7 / provisioning state 11 继续通过；owner-scoped detail 已成功读到 schedule，但本地脚本又将 `displayName` 保留与否作为 Case 15 硬门禁，触发 `schedule detail contract mismatch`。该字段不属于案例运行契约，本轮不记通过；typed failure cleanup 返回 `accepted/pending`，删除后 owner list 为 0，跨下一分钟 run 保持 `0 -> 0`。随后用现有只读 schedule 脱敏核对 detail schema，确认权威字段为 `schedule.scheduleId/enabled/cronExpression/timezone/deleted` | 第四轮仅断言 Case 15 真正契约：schedule ID、enabled、cron、timezone、自然 cron、committed 11/11 artifact、typed DELETE 和静默窗口；`displayName` 只用于合成输入标记，不参与通过判定 |
| 2026-08-07 03:28 | Case 15 schedule fresh 第二轮与精确清理 | Ready `4c0596c7`；owner-scoped catalog | 验收可见性断言仍失败；平台 provisioning 成功；副作用已清理 | Fresh confirmation 再次精确为 1 workflow / 6 GET / read-only / durable；typed admission 为 `accepted/pending_binding`，binding state 7 与 provisioning state 11 / attempt 2 均 committed `succeeded`。修正后的 owner list 在 120 秒内仍不返回该 active schedule，但 committed member 已有 schedule/operation ID，且用该 ID + owner 的 failure cleanup 收到 typed `accepted/pending`，说明本轮仍是验收读路径选择错误，不记 Case 15 通过。清理后 owner list 为 0，跨下一分钟 run catalog 保持 `2 -> 2`，仅保留脱敏哈希 | 改用 committed schedule ID 的 owner-scoped `/api/schedules/{id}` detail 作为可见性权威；cron 以 observatory `schedule=` 过滤和 committed run detail 判定，删除后再用 owner list 0 + 静默窗口关闭 |
| 2026-08-07 03:21 | Case 15 schedule fresh 首轮与精确清理 | Ready `4c0596c7`；`weekly-budget-variance-digest` | 验收编排失败；平台 provisioning 成功；副作用已清理 | 首次返回 1 个 workflow / 6 个 GET，全部 read-only 且允许 durable；确认后收到 typed `accepted/pending_binding`，binding committed `succeeded` state 7，provisioning committed `succeeded` state 11，attempt 2。本地编排误用无 owner 的 schedule catalog 轮询 team-owned schedule，因此超时，本轮不记 Case 15 通过。随后按 scope/team/member owner 精确定位唯一合成 schedule，typed DELETE 返回 `accepted/pending`；owner-scoped list 为 0，跨下一分钟 run catalog 保持 `3 -> 3`，未留下调度。仅保留 member/schedule/operation/command 的 12 位哈希 | 将轮询修正为 `ownerKind=studio_member_automation` 的 owner-scoped catalog，重跑唯一一次 confirmation -> provisioning -> 自然 cron -> committed 11/11 artifact -> typed DELETE -> 静默窗口 |
| 2026-08-07 03:08 | Ornn 全目录 fresh verify-only | `ornn-api`；28 个本地 skill 包 | 通过，28/28；无服务端变更 | 用 `config.local.yaml` 重新物化 28 个 workflow、同步 28 个 skill asset 并打包；正式 `publish_skills.rb --verify-only` 与独立 `verify_public_catalog.rb` 均确认 server format 28、exact name 28、public 28、version match 28，missing/private/version mismatch 集合为空。`uploadsPerformed=false`、`permissionsChanged=false` | 执行 Case 15 schedule 的 typed confirmation -> provisioning -> cron -> DELETE 可清理闭环；若当前 durable authority 不可用则保留 fresh 稳定 blocker，不用历史证据伪造当前通过 |
| 2026-08-07 03:03 | Regression R01 fresh Lark `/init` production proof | Ready `4c0596c7`；required commit `f5e51e99f` | 通过；local 1/1、production 1/1 | Git ancestry 确认 Ready 提交包含所需修复；deployment generation/observedGeneration `2721/2721`、1/1 updated/ready/available，Pod 0 restart，digest `sha256:7cdca8d5…`。精确 Aevatar Bot 私聊只发送 1 次 `/init`，15 秒后 DOM 仍为 1 条用户消息、1 张授权更新回复卡，duplicate=0；未点击授权链接。发送后 production log 窗口的 `Activation access violation` 与 `CommittedStatePublicationException` 均为 0。case、脱敏机器摘要、专用报告和 validator 已同步，`validate_regression_cases.rb`、JSON parse、Ruby syntax、定向 `git diff --check` 均通过 | 运行 schedule 与 Ornn 服务端验证；随后统一 machine evidence/report/README 并做最终全量静态复扫 |
| 2026-08-07 03:01 | Risk / channel / regression 机器证据门禁 | 当前工作树；Ready `4c0596c7` | Risk/channel 通过；regression 仍 1 项 pending-deployment | `validate_risk_cases.rb`：21 项为 16 passed、0 blocked、0 failed、0 pending-execution、5 not-configured；`validate_channel_cases.rb`：3/3 passed；`validate_regression_cases.rb`：1 项 local 通过、production 0、`pending-deployment=1`。未把缺配置或待部署状态改写成通过 | 核对唯一 regression case 与 `4c0596c7` 的 deployed commit/production proof，能用已有 fresh 证据关闭则同步机器摘要；否则保留精确 blocker。随后验证 schedule 与 Ornn |
| 2026-08-07 03:00 | Admission 33-36 fresh 探针 | Ready `4c0596c7`；scope hash `237314c29964` | 通过，4/4；无副作用 | 33 缺 capability 在 provision 以 `INVALID_PROVISION_WORKFLOW_REQUEST` fail closed；34 固定 path slot 正例真实到达 provider，模板化 selector 在 preview 被 `INVALID_USER_WORKFLOW_REQUEST` 拒绝，slot escape 在 run 前以 `NYXID_OPERATION_PATH_PARAMETER_INVALID` 拒绝，组合契约确认；35 n8n `nodes` 根字段在 preview 拒绝；36 durable 写因 `DURABLE_AUTHORIZATION_UNAVAILABLE` 在 preview 拒绝。每轮使用新 nonce，durable 未创建 schedule，未写 Base | 运行 risk/channel validator，核对 fresh admission 语义与现有脱敏机器摘要没有状态冲突；随后处理 schedule 与 Ornn 层 |
| 2026-08-07 02:59 | `/api/chat` Assistant fresh 批次 | Ready `4c0596c7`；01/12/13/14/15 | 通过，5/5 `caseValidated` | 五项均 `chatCompleted=true`、搜索 Ornn、加载精确 skill presentation、启动 workflow、读取 committed typed artifact，`workflowValidationStatus=validated`，duplicate / mixed-presentation tool start 集合均为空。01/12/14/15 committed run hash 为 `501d0c470f28` / `ef5efb446ff7` / `20e637b9bca2` / `7d418ec8d541`，分别 13/13、4/4、3/3、11/11；13 的 PDF OCR、归一化、历史重复与无副作用 contract 同样严格命中。14 的 Assistant 最终文案仍写 `AwaitingToolApproval`，但 committed state 已 completed、批准 identity 存在且 artifact 成功，文案不覆盖 authoritative 结果 | 执行 admission/risk、channel、schedule、Ornn 与报告一致性层；需要真实 Lark 交互或 durable mutation 的入口继续按各自副作用边界处理 |
| 2026-08-07 02:50 | Base 写探针精确清理 | 本轮 05/24/25 创建的 4 条合成记录 | 通过 | 删除前只读列表共 5 条，4 条目标记录分别且唯一命中完整 key、Control、Owner、Status、Sequence、Reviewed At 契约；逐条 DELETE 均返回 `code=0`。清理后目标 key 匹配数为 0、`has_more=false`，原有 1 条未匹配记录保持存在且未触碰；未输出或保存 record ID，可恢复性取决于目标 Base 回收站策略 | 执行 `/api/chat` Assistant 动态验收；按 committed typed artifact 判定，不用 Assistant 文案替代 workflow 终态 |
| 2026-08-07 02:49 | Direct runtime 可清理 Base 写探针 fresh 批次 | Ready `4c0596c7`；05/24/25 | 通过，3/3；已写入 4 条，待清理 | Case 05/24/25 run hash 分别为 `281a4299062f` / `57e7da79bea1` / `5ffe6ecca8c4`，均 committed `completed`，state 46 6/6、state 40 5/5、state 61 8/8，strict artifact 全部命中。三项均观察到完整 typed pending identity，resume 次数精确为 1/1/2；05 写入 1 条固定资产确认，24 写入 1 条 runtime probe，25 顺序写入 2 条 probe，共 4 条合成记录；未创建审批或发送消息 | 只读列出目标表并按 key + Control/Owner/Status/Sequence/Reviewed At 全字段匹配；仅删除精确匹配记录并回读目标集合为 0，未匹配记录不触碰 |
| 2026-08-07 02:48 | Direct runtime 写分支安全与清理复核 | Cases 05/06/24/25 | 05/24/25 可执行；06 不在当前授权边界 | 05/24/25 只向固定资产盘点表写入 1/1/2 条合成记录，key、Control、Owner、Status、Sequence、Reviewed At 均可精确匹配，并已有逐条 DELETE 后回读为 0 的清理路径。06 会创建真实 Lark 审批，当前 workflow/validator 未声明取消或删除清理步骤，不能把“已创建待审批实例”视作可清理探针；本批未产生外部写入 | 显式 allowlist 运行 05/24/25 并按完整 typed identity 批准 1/1/2 次；完成后先精确清理并回读，再进行下一验证层。06 保持需要额外非清理副作用授权 |
| 2026-08-07 02:46 | Direct runtime 12/14/17 typed approval fresh 批次 | Ready `4c0596c7`；scope hash `237314c29964` | 通过，3/3；无业务写入 | Case 12 run hash `ab9b9e78759f` committed `completed`、state 31、4/4，固定结算 `total_cents=16623`、`side_effects=false`。Case 14/17 分别为 run hash `ee14c277b7af` / `6fccf910d03b`，均观察到完整 typed pending identity 并各 resume 1 次，随后 committed `completed`、state 28 3/3 / state 34 4/4；联系人标识已脱敏，Base search 返回 0 项，两项 artifact 均 `side_effects=false`。三项 strict contract 全部命中 | 复核 06/24/25 的合成目标、typed approval 与精确清理路径；只执行本 Skill 授权的可清理写探针 |
| 2026-08-07 02:45 | Direct runtime 12/14/17 第二次批准批次 | Ready `4c0596c7`；12/14/17；scope 已认证恢复 | 未启动；本地 team 前置缺失 | `/api/auth/me` 顶层与 session scope 一致并已仅注入当前进程；验证器随后在任何 workflow 调用前因未设置 `AEVATAR_TEAM_ID` 退出。未创建 member、未启动 workflow、未发送 typed approval、无外部写入 | 用 `config.local.yaml` 的团队配置与当前 scope team read model 做唯一 active 匹配，不回显 ID；在同一进程传入 scope/team 后重跑 |
| 2026-08-07 02:44 | Direct runtime 12/14/17 首次批准批次 | Ready `4c0596c7`；12/14/17 | 未启动；本地前置缺失 | 验证器在任何生产调用前因当前 shell 未设置 `AEVATAR_SCOPE_ID` 退出；未创建 member、未启动 workflow、未发送 typed approval、未产生外部写入 | 从已认证 Aevatar `/api/auth/me` 在同一进程恢复 scope 且不回显原值，再执行同一 12/14/17 精确批准批次 |
| 2026-08-07 02:43 | Case 11 managed credential / NyxID UserService 只读归因 | Ready `4c0596c7`；scope hash `237314c29964` | 外部配置 blocker 已确认；无生产变更 | `nyxid whoami` 确认当前 native 用户 active；Aevatar credential GET 仍为 enabled/eligible/active、`execution_ready=true`、reason=`ready`、state 3、cleanup 0。NyxID 精确回读 `chrono-sandbox` 为 active/personal，但仍是 `forward_access_token=true`、`inject_delegation_token=true`、scope=`llm:proxy`，不满足当前 internal-canary 固定前置 `false/true/proxy:*`；这与 fresh Case 11 的 `codex_execution_admission_denied` 及历史内部 `managed_proxy_authentication_failed` / lifecycle `chrono_sandbox_delegation_misconfigured` 一致。当前 `nyxid 0.9.0 service update` 没有三项策略参数，未用临时 HTTP 绕过 CLI，也未 reconcile/rotate credential | 继续运行不依赖 managed sandbox 的 12/14/17 typed approval 批次；Case 11 等待受支持的 UserService 策略修复入口或运维变更后，再执行 credential reconcile 与 fixed probe |
| 2026-08-07 02:38 | Direct runtime 无副作用 fresh 批次 | Ready `4c0596c7`；01,02,03,04,05,07,08,09,10,11,13,15,16,18-23,26-28 | 21/22 通过；Case 11 failed；无副作用 | 除 11 外的 21 项均 committed `completed`、步骤齐全且 strict artifact 命中；其中 26/27/28 fresh run hash 为 `32c69d2e3083` / `aa0ba5f43822` / `83f3757f81c6`，分别为 state 44 5/5、state 86 14/14、state 38 6/6。Case 11 run hash `fa312176b745` committed `failed`、state 31、4/4，失败为 `codex_execution_admission_denied`，无 final artifact。批次未批准任何调用、未执行外部写入 | 拉取 Case 11 对应时间窗的脱敏 production logs 与 managed readiness，先区分 Aevatar regression 和 NyxID/账号配置；完成归因后再运行 12/14/17 批准批次 |
| 2026-08-07 02:27 | Production explicit-request preview fresh 批次 | Ready `4c0596c7`；scope hash `237314c29964`；动态 registry 01-28 | 通过，28/28 | 先用 `config.local.yaml` 恢复 28 个 ignored workflow；NyxID `/api/auth/me` 已认证、session/top-level scope 一致，配置 team 在该 scope 唯一 active 匹配。28/28 preview 全部通过；call-site method、path template、effective risk、approval enforcement 与 interactive mode 均命中；Case 26 `inlineWorkflowCount=1`，其余为 0。未 provision、未启动 workflow、无外部写入 | 先运行所有无副作用 direct workflow；再按 case allowlist 执行只读批准和可清理写探针，每批后更新 README |
| 2026-08-07 02:24 | 完整静态提交前检查与公开模板物化 | 当前工作树；`config.example.yaml` | 通过；独立 regression 状态未变化 | 28/28 workflow、28/28 Ornn skill、3/3 channel 定义、21/21 risk 定义、production/Assistant/admission Ruby 语法、risk/channel/report validator、示例物化后的 28/28 YAML parse 和 `git diff --check` 全部通过；`validate_regression_cases.rb` 诚实保留 1 个 `pending-deployment`，未伪报为通过。`build/` 与 `config.local.yaml` 均保持 ignored | 用 `config.local.yaml` 恢复生产物化并动态运行 28/28 explicit-request preview；preview 批次后先更新 README，再分风险执行 direct runtime |
| 2026-08-07 02:23 | Risk/channel/report 一致性批次 | 当前工作树；Ready `4c0596c7` | 通过 | `validate_risk_cases.rb`：21 项为 16 passed、0 blocked、0 failed、0 pending、5 not-configured；`validate_channel_cases.rb`：3/3 passed；`validate_report.rb`：28 workflow、20 历史直接案例、21 risk、5 Assistant、3 channel 的 README/Markdown/HTML/机器证据一致；`git diff --check` 通过。报告已补 26-28 行、3/3 channel 当前状态、Risk 23/37/38/39 fresh 证据和 26-28 机器摘要链接 | 执行 AGENTS.md 完整静态提交前检查与 `config.example.yaml` 隔离物化；批次结束后先回写 README，再开始 production 全量 preview |
| 2026-08-07 02:13 | Risk 23 Lark workflow 运行期拒绝 fresh 复验 | Ready `4c0596c7`；run hash `3af9fd937d24` | 通过，无副作用 | 发送前目标 run 基线 9；只发送 1 条“使用已挂载 skill”提示，新卡片与 target catalog 均只增 1，唯一 run 先 committed `awaiting_tool_approval` state 10。新增 Reject 只分发 1 次后，同一 run committed `failed`、state 17、1/1，失败步骤 `resolve_contact`；committed 与 Lark 回传均含稳定 `approval_denied`，下游未执行、无 final artifact、未回显 raw run ID。审批按钮仍显示，不外推为卡片失效已修复 | 将 Risk 23 required evidence 和机器摘要更新为 passed；同步 16 passed/0 blocked/0 failed/5 not-configured 的当前风险汇总与 Markdown/HTML 报告 |
| 2026-08-07 02:05 | Risk 37/38/39 机器证据与 validator 定向批次 | Ready `4c0596c7`；`validation/production-validation-2026-08-07-cases-26-28.json` | 运行证据已同步，报告一致性待更新 | 37/38/39 均改为 passed，machine evidence 保存 12 位 run hash、state/step 计数与 YAML required evidence 的精确布尔事实；新 26-28 生产摘要 JSON 语法和脱敏门禁结构有效。Risk validator 已通过三项 fresh 契约，随后只因 Markdown 报告仍写 25 个 workflow 被 Risk 32 拦下 | 将 README、Markdown、HTML 和 report validator 统一为 28 workflow/skill/contract，并重跑 risk/report 门禁 |
| 2026-08-07 02:02 | Cases 26/27/28 修复部署 committed runtime | Ready `4c0596c7`；run hash `07f1fc930866` / `5b0e99a928f5` / `ebe447a8479b` | 通过，3/3，无副作用 | Case 26 committed `completed`、state 44、5/5，`inline_definition_bound`、child resolved/completed 全真；27 committed `completed`、state 86、14/14，3 个 worker receipt 与 merge order 全真；28 committed `completed`、state 38、6/6，winner、first success 与 later completions ignored 全真。三项严格 artifact contract 均命中，无 approval、无外部写入 | 将 fresh 证据同步到 Risk 37/38/39 与 26-28 生产摘要，重跑 risk/report validator；随后执行动态全量 preview/runtime 回归 |
| 2026-08-07 02:00 | Cases 26/27/28 修复部署 explicit-request preview | Ready `4c0596c7`；scope hash `237314c29964` | 通过，3/3 | 使用 `config.local.yaml` 在新临时目录隔离物化；26/27/28 均 preview 通过且 0 external call site，Case 26 精确记录 `inlineWorkflowCount=1`，27/28 为 0；未 provision、未启动 workflow、无外部写入 | 在同一 Ready 部署真实运行三项无副作用案例，检查 inline child、三路 parallel receipt/order 和 race winner/迟到完成 typed artifact |
| 2026-08-07 01:58 | Cases 26/27/28 修复 Ready 部署确认 | 提交/镜像 `4c0596c7`；digest `sha256:7cdca8d5…` | 通过，可进入复验 | deployment generation/observedGeneration 为 2721/2721，rollout status 成功；旧 Pod 已退出，目标镜像成为唯一副本且 replicas/updated/ready/available 均为 1，Pod 1/1 Ready、0 restart。远端 tip 精确为完整提交 `4c0596c764b45abc36d00e27577cd5a949796f79` | 在该 Ready 部署重跑 26/27/28 explicit-request preview；通过后再真实运行并读取 committed typed artifact |
| 2026-08-07 01:58 | Cases 26/27/28 修复部署第四次轮询 | 目标 `4c0596c7`；Ready `a2b8b379` | 等待构建，无状态变化 | registry 仍无目标 manifest；deployment generation 2719 已 observed，旧镜像保持唯一 1/1 Ready 副本，无失败 rollout 信号 | 延长到约 45 秒轮询；同步不依赖部署的 28-case catalog、contract 和报告数量事实 |
| 2026-08-07 01:55 | Cases 26/27/28 修复部署第三次轮询 | 目标 `4c0596c7`；Ready `a2b8b379` | 等待构建，无状态变化 | 目标 8 位 tag 仍无 registry manifest；deployment generation/observedGeneration 保持 2719/2719，旧镜像 updated/ready/available 仍为 1/1/1，没有 rollout 失败迹象 | 在构建窗口修复验收报告的 28-case 数量与部署语义漂移；随后继续低频轮询 |
| 2026-08-07 01:53 | 验收仓库静态与报告一致性批次 | 当前工作树；动态 inventory 28 workflow/skill | 部分通过，发现旧证据漂移 | workflow 28/28、skill 28/28、channel 3/3、production/admission/Assistant Ruby 语法和 `git diff --check` 通过；Risk validator 因 Risk 27 机器摘要仍写 public catalog 25 失败，report validator 因 21-25 证据目标提交与风险摘要目标提交不一致失败。两项都是验收证据同步缺口，不是 Aevatar runtime 失败 | 将 Risk 27 catalog 数量同步为 28，核对并统一当前部署/目标提交语义，更新 Markdown/HTML 报告后重跑 risk/report validator；随后完成 example materialize 批次 |
| 2026-08-07 01:51 | Cases 26/27/28 修复部署第二次轮询 | 目标 `4c0596c7`；Ready `a2b8b379` | 等待构建，无状态变化 | 约 30 秒后目标 manifest 仍不存在；deployment 继续为 generation 2719/2719，旧镜像唯一副本 1/1 Ready、updated/available 均为 1、0 restart，没有失败 rollout 信号。一次聚合轮询未返回输出后已拆分只读命令复核，本行只记录复核结果 | 继续低频等待 registry manifest 与目标 Ready rollout，不在旧镜像复验 |
| 2026-08-07 01:50 | Cases 26/27/28 修复 registry 与远端检查 | 目标 tag `4c0596c7`；`origin/feature/integrate` | 等待构建 | registry 尚无目标 8 位 tag manifest；`git ls-remote` 仍精确回读 `4c0596c764b45abc36d00e27577cd5a949796f79`，没有需要重新对齐的远端后代，也没有 rollout 失败证据 | 拉长轮询间隔，等待目标 manifest 和唯一 Ready workload |
| 2026-08-07 01:49 | Cases 26/27/28 修复部署首次轮询 | 目标 `4c0596c7`；Ready `a2b8b379` | 等待部署，无状态变化 | deployment generation/observedGeneration 仍为 2719/2719，旧镜像是唯一副本且 updated/ready/available 均为 1，Pod 1/1 Ready、0 restart；当前运行 digest 仍为 `sha256:bde917a9…`，尚未包含目标修复 | 继续有界低频轮询目标或明确后代镜像；部署前不在旧镜像重复运行 26/27/28 |
| 2026-08-07 01:47 | Cases 26/27/28 Aevatar 修复提交与远端回读 | `origin/feature/integrate`；`4c0596c764b45abc36d00e27577cd5a949796f79` | 推送通过，等待可追溯 Ready 部署 | 只提交 execution kernel、Studio provisioning 及两处回归测试共 4 个文件，79 insertions/5 deletions；fetch 后远端仍为直接父提交 `a2b8b379`，普通 push 成功，`git ls-remote` 回读远端 tip 与本地完整 SHA 精确一致，未使用 force push；验收仓库未提交 | 有界低频轮询 registry 与唯一 Ready workload；只有目标提交或明确包含它的后代部署 Ready 后，才重跑 26/27/28 preview 与 committed runtime |
| 2026-08-07 01:44 | Cases 26/27/28 Aevatar 根因修复与扩大门禁 | 隔离 worktree；基线 `a2b8b379` | 本地通过，待提交、推送与 Ready 部署 | execution kernel 现对 `parallel` / `race` 的 `sub_param_*` 延迟解析，保留 worker 阶段才可用的 `${index}`；Studio provision 对 root 与 inline YAML 使用同一 canonical trim，避免 bundle 重建后 definition digest 漂移。定向 18/18 与 75/75，Core 946/946，Studio 1753/1753，Integration 485 passed/4 skipped，Architecture 15/15，binding/稳定性/架构/proto/文档 87/87/脱敏 diff 门禁全部通过；仅有既有 NU1510/NU1903 与复杂度警告 | 只提交 4 个相关文件；再次 fetch 并基于最新远端对齐后普通推送，回读远端 tip，等待可追溯 Ready 部署 |
| 2026-08-07 01:34 | Cases 27/28 committed step 定向诊断 | Ready `a2b8b379`；run hash `6cbbde97b648` / `97f18c9a654d` | 确认 Aevatar 参数延迟解析缺陷，无外部写入 | 两项均真实分发 3 个 deterministic worker 且全部 success，但 committed output 分别为 `evidence-` 和 `"candidate-"`，`${index}` 在进入 `parallel` / `race` 模块前已被执行内核解析为空。Ready 提交的 canon 和单模块测试都明确要求 `sub_param_*` 支持 `${index}`，不是 case 过期或 artifact 误判 | 从最新 `origin/feature/integrate` 建立隔离 worktree，先增加经过完整 execution kernel 的回归测试，再把 `parallel` / `race` 纳入 `sub_param_*` 延迟解析并普通推送 |
| 2026-08-07 01:30 | 26/27/28 committed runtime 定向复验 | Ready `a2b8b379` | 0/3 严格通过，无外部写入 | 26 在 provision 阶段以 `INVALID_PROVISION_WORKFLOW_REQUEST` 拒绝，稳定信息为 capability admission definition digest 与 bound definition 不一致，未创建 run。27 的 run hash `6cbbde97b648` committed `completed`、state 50、8/8，但走失败 artifact，三路 receipt/order 布尔均为 false；28 的 run hash `97f18c9a654d` committed `completed`、state 38、6/6，但 winner/首成功/迟到完成布尔均为 false。两项 terminal completed 均因严格 artifact 不匹配判失败 | 读取 27/28 committed step output 确认验收定义问题；最小复现 26 的 definition digest 边界，若证实平台缺陷则在最新 Aevatar 隔离 worktree 修复并普通推送 |
| 2026-08-07 01:29 | 26/27/28 production explicit-request preview | Ready `a2b8b379`；scope hash `237314c29964` | 通过 | 使用 `config.local.yaml` 在 `/tmp` 隔离物化；3/3 preview 通过且均为 0 external call site。Case 26 的定义 bundle 记录 `inlineWorkflowCount=1`，27/28 为 0；未 provision、未启动 workflow、无外部写入 | 在同一 Ready 部署真实运行 26/27/28，检查 inline child output、三路 parallel receipt/order 和 race 单一父终态 |
| 2026-08-07 01:29 | 26/27/28 production preview 启动 | 本地隔离物化命令 | 未执行，生产零请求 | 命令因包含退出时清理 `mktemp` 目录的 `rm -rf` 被本地执行安全层拒绝，进程未创建，未读取生产 case、未 provision、未启动 workflow、无副作用 | 移除删除动作并保留 `/tmp` 隔离目录；继续只输出 scope hash，重新执行同一 preview |
| 2026-08-07 01:27 | Aevatar 修复 Ready 部署确认 | production generation 2719；`a2b8b379` | 通过，可进入复验 | 唯一 workload 已切换为 `a2b8b379`，精确对应远端 `feature/integrate` tip `a2b8b379057ab401da9fcafe4760887ecc7bcad5`；digest `sha256:bde917a9…`，deployment generation/observedGeneration 均为 2719，updated/ready/available 均 1，Pod 1/1 Ready、0 restart。`gh run list` 仍无该 head 的 CI 记录，但 Ready 镜像与远端完整提交已可精确追溯 | 使用 `config.local.yaml` 隔离物化，先跑 26/27/28 preview 与 committed runtime；随后 fresh 复验 Risk 23/37/38/39 |
| 2026-08-07 01:26 | Ornn missing-only 发布与双重公开回读 | `ornn-api`；当前 28 个重新打包 skill | 通过 | 28/28 先通过服务端格式与发布预检；既有 25 项名称/version/public 精确匹配并跳过，只创建并公开新增 26/27/28。随后正式 `publish_skills.rb --verify-only` 与独立 `verify_public_catalog.rb` 均回读 28/28，missing/private/version mismatch 为空；复验阶段 `uploadsPerformed=false`、`permissionsChanged=false`，README 未保存 GUID | 将 28/28 脱敏事实同步到 Risk 27/31/32 机器摘要和 Markdown/HTML 报告；完成报告一致性门禁后继续等待目标 Ready 部署 |
| 2026-08-07 01:22 | 新增组合原语案例与双端 Skill 静态批次 | 当前工作树；26/27/28；`aevatar-acceptance-loop` | 定义层通过，生产层待验证 | 28/28 workflow 的 YAML、步骤图、外部调用计数和专用契约全部通过；28/28 Ornn skill 与主定义一致，Case 26 额外 asset 与固定 inline 子定义逐字一致；`production_validate.rb`、runtime contract 和相关生成/校验脚本语法通过。Codex `.agents/skills` 与 Claude `.claude/skills` 软链接可读，Skill 明确要求每个验证批次后先更新 README，并通过 `skill-creator` 的 `quick_validate.py`。新增三项尚未 preview/runtime，Ornn 服务端尚未发布 | 更新风险/报告数量契约与能力矩阵，完成本地全门禁后发布新增 Ornn skill；继续等待 `a2b8b379` Ready，再跑 26/27/28 与 Risk 23/37/38/39 |
| 2026-08-07 01:14 | Aevatar 修复部署再次轮询 | 目标 `a2b8b379`；生产 generation 2717 | 等待部署，无状态变化 | 唯一 Ready workload 仍为 `49244090`、digest `sha256:89462e…`，deployment generation/observedGeneration 均为 2717，updated/ready/available 均 1，Pod 1/1 Ready、0 restart；`gh run list` 仍无目标 head 的 CI 记录，`git ls-remote` 则继续精确回读远端 tip 为 `a2b8b379057ab401da9fcafe4760887ecc7bcad5` | 部署等待期间新增 `workflow_call`、确定性 `parallel` 与确定性 `race` 的正式无副作用 workflow/Ornn skill 覆盖；完成每个静态批次后先回写 README，再继续低频轮询 |
| 2026-08-07 01:11 | Aevatar 修复部署首次轮询 | 目标 `a2b8b379`；生产 generation 2717 | 等待部署，无状态变化 | 唯一 workload 仍为 `49244090`、digest `sha256:89462e…`，deployment generation/observedGeneration 均为 2717，updated/ready/available 均 1，Pod 1/1 Ready、0 restart；目标镜像 tag 尚无 registry manifest，`gh run list` 尚未返回该 head 的 CI 记录，远端 tip 仍精确等于目标提交；尚不能执行修复后复验 | 低频检查目标镜像构建与远端后代；只有目标提交或明确包含它的后代成为唯一 Ready workload 后才运行 Risk 23/37/38/39 |
| 2026-08-07 01:10 | Aevatar Risk 23/37/38/39 修复、扩大门禁与远端回读 | `origin/feature/integrate`；`a2b8b379057ab401da9fcafe4760887ecc7bcad5` | 本地通过，等待可追溯部署 | Risk 23 拒绝路径新增稳定 `approval_denied`；Studio provision、binding、durable revalidation 与 revision identity 全链携带 inline workflow definitions；`parallel`/`race` 支持确定性 `sub_step_type`、参数展开和可判定 fan-in/winner 语义。Workflow Core 944/944、Studio 1752/1752、Workflow Host API 985/985、Workflow Application 599/599、Integration 485 passed/4 skipped，架构、文档、binding、稳定性与 diff 门禁均通过；另将远端基线中真实 1ms 超时测试改为 `FakeTimeProvider` 确定性探针。普通 push 后 `git ls-remote` 精确回读远端 tip 等于该提交，未使用 force push；尚无生产 Ready 证据 | 有界轮询 Ready workload，要求部署提交为该提交或可证明包含它的后代；随后先 fresh 复验 Risk 23/37/38/39，再新增正式无副作用 workflow replacement coverage 并执行完整动态清单 |
| 2026-08-07 00:31 | Risk 23 workflow 运行期拒绝 fresh 复验 | Ready `49244090`；Lark `Aevatar Agent` | 失败，确认 Aevatar 缺陷 | 发送“使用已挂载 skill”精确提示后只新增 1 个目标 run；Reject 只点击/分发 1 次，run hash `1d55d55c374c` committed `failed`、state 17、1/1，`resolve_contact` 下游未执行且无 final artifact。但 committed step 与 Lark relay 仍只有 `approval rejected` / `workflow_run_error`，缺少 typed `approval_denied`，审批按钮也未失效；未保存 raw identity | 从最新 `origin/feature/integrate` 创建隔离 worktree，补拒绝 stable code/read model 与卡片失效回归测试，最小修复后普通推送并等待部署复验 |
| 2026-08-07 00:20 | Channel post-fix committed 验证 | Ready `49244090`（直接包含 `de801ca70`）；20/21/22 | 通过 | Case 20 的 fresh `use_skill` mount 批准只分发 1 次并恢复同一 AgentRun，mount `Completed` 后只新增 1 个 `service-invoke` run；hash `9f476bbd79c1` committed `completed`、state 28、3/3，artifact 精确命中。Case 21 的 fresh mount 拒绝只分发 1 次，typed `Denied` 对应 `approval_denied`，未 mount、未 start、目标 run 增量 0。Case 22 既有同部署祖先证据继续有效；channel validator 为 3/3 passed。底部重复文本仍未发送 | 同步 Markdown/HTML 报告与风险 Case 23 的拒绝语义；继续 37-43 归因和 schedule 批次 |
| 2026-08-07 00:19 | Risk 机器摘要与后代部署契约 | `validation/risk-validation-2026-08-06.json`；Ready `49244090` | 通过，状态未全绿 | fresh 33-36 已落到当前部署时间；验收 validator 修正“包含目标基线”字段却强制 SHA 相等的矛盾，现要求目标基线固定、当前 SHA/镜像对应、`requiredSourceCommitPresent=true`、1/1 Ready。risk validator 通过，仍为 12 passed、2 blocked、2 failed、5 not-configured | 用 fresh channel 拒绝证据重算 23；分别处理 37 的 inline definition 与 38/39 的确定性 worker 能力 |
| 2026-08-07 00:17 | Risk/admission fresh 探针 | Ready `49244090`（直接包含 `de801ca70`）；33-36 | 通过 | 新 nonce 下 33 在 provision 以 `NYXID_OPERATION_SELECTION_REQUIRED` fail-closed；34 的合法 slot value 准入并只在 provider 侧返回合成 token 的 `NYXID_PROXY_HTTP_400`，模板化 selector 与越段 slot 分别以 `NYXID_OPERATION_SELECTION_REQUIRED`、`NYXID_OPERATION_PATH_PARAMETER_INVALID` 拒绝；35 拒绝 n8n root `nodes`；36 durable preview 以 `DURABLE_AUTHORIZATION_UNAVAILABLE` 拒绝。全部未产生外部写入，输出已脱敏 | 更新 risk 机器摘要的部署与 fresh probe 时间，再重算 23/37-43 可执行状态 |
| 2026-08-07 00:10 | Assistant `/api/chat` fresh 批次 | Ready `49244090`（直接包含 `de801ca70`）；01/12/13/14/15 | 通过 | 5/5 均完成 Ornn search、精确 skill 加载、typed mount approval、workflow 启动与 committed artifact 校验；分别为 13/13、4/4、12/12、3/3、11/11，严格状态均为 `validated`。14 从 committed current state 取得完整 typed identity 并 resume 1 次，run hash `d526f930cc8a`；Assistant 最终文案仍写 Awaiting，只保留为展示层陈旧证据，不参与通过判定。所有 case 均 `side_effects=false` | 执行 admission/risk fresh 探针；随后同步 channel 20/21 的 post-fix Lark 证据与报告 |
| 2026-08-07 00:02 | Base 写探针精确清理 | 本轮 05/24/25 创建的 4 条合成记录 | 通过 | 删除前再次验证 4/4 全字段契约；逐条 DELETE 均返回 code 0，重新 list 后四类验收 key 匹配数为 0，唯一未匹配记录保持存在且未触碰。仓库未保存 record ID；可恢复性取决于目标 Base 回收站策略 | 执行 Assistant、risk/admission、schedule 与报告一致性批次；channel Cases 20/21 等待浏览器输入控制恢复 |
| 2026-08-07 00:01 | Base 写探针清理只读盘点 | 资产盘点合成表；固定验收 key/字段契约 | 通过盘点，待清理 | 表内 5 条记录中恰有 4 条命中本轮验收模式：manual/runtime/sequential-a/sequential-b 各 1；四条的 Control、Owner、Status、Sequence、Reviewed At 全字段精确匹配。另 1 条不匹配并排除，未读取或保存其业务字段 | 仅删除这 4 条完整匹配记录；逐条要求 Lark code 0，随后重新 list 并要求四类匹配总数为 0 |
| 2026-08-07 00:00 | Direct runtime 可清理 Base 写探针 | Ready `de801ca7`；05/24/25 | 通过，待清理 | 三项均 committed `completed` 且严格 artifact 命中，分别为 6/6、5/5、8/8；按完整 typed identity resume 1/1/2 次，共创建 4 条固定合成 key 的资产盘点记录。未执行消息或审批写分支 | 只读列出固定 key 模式并校验 Control/Owner/Status/Sequence/Reviewed At；仅删除全字段精确匹配记录并回读为 0 |
| 2026-08-06 23:58 | Direct runtime 无副作用业务分支 | Ready `de801ca7`；05/07/08/09/10 | 通过 | 五项均 committed `completed` 且严格 artifact 命中；05 `mutation_executed=false`，07/08 `message_sent=false`，09/10 `approval_created=false`，10 同时 `message_sent=false`；未观察 approval pending，未产生外部写入 | 执行可清理的 05/24/25 写探针并在完成后精确清理；06 与发送/审批 submit 分支保持不可清理写边界 |
| 2026-08-06 23:55 | Direct runtime 外部只读/受保护 POST | Ready `de801ca7`；03/04/13/14/15/16/17 | 通过 | 7/7 均 committed `completed` 且严格 artifact 命中；14/17 分别观察并按完整 typed identity resume 1 次，最终 3/3、4/4；16 为 fresh 4/4，未再出现 HTTP 502。所有结果均 `side_effects=false`，联系人标识已脱敏 | 执行 05/07/08/09/10 的无副作用业务分支；06 保留不可清理审批写边界，不在本轮重复创建 |
| 2026-08-06 23:51 | Case 20 fresh 定向复验 | Ready `de801ca7` | 通过，但保留竞态风险 | 新 run hash `e33a66b08f60` committed `completed`、15/15、`stateVersion=89`，map_reduce/cache 严格 artifact 命中且无副作用。前一 run 的 8/10 无错误失败未再次出现，但不能用单次绿色删除该异常证据 | 继续其余 direct runtime；源码核对动态 child terminalization，必要时增加可稳定复现的并发/冷启动测试 |
| 2026-08-06 23:50 | Case 20 committed/log 诊断 | run hash `49a1e3d04b0a`；Ready `de801ca7` | 收敛到 cache 动态 child | authoritative detail 已推进到 `stateVersion=90`、8/10；map_reduce、计数和 shape switch 全部成功，`cache_probe_primary` miss 后派发的动态 transform child 与 parent 都未完成，run 无 `lastError` 即 failed。最近 500 行生产日志共约 107 KB，但无 cache 或 error/fail 记录；未保存原始 run/actor/动态 child 标识 | 单独 fresh 重跑 Case 20；若稳定复现则在 Aevatar 隔离 worktree 添加 runtime 回归测试并修复 cache continuation/terminalization |
| 2026-08-06 23:48 | Direct runtime 基础/文件/确定性原语 | Ready `de801ca7`；01/02/11/12/18/19/20/21/22/23 | 部分通过 | 01、02、12、18、19、21、22、23 均 committed `completed` 且严格 artifact 命中；12 固定结算为 `total_cents=16623`、`side_effects=false`。11 在 4/4 已观测步骤后 committed `failed`，稳定错误 `codex_execution_admission_denied`。20 新 run hash `49a1e3d04b0a` 在 8/9 后 committed `failed`，无 final artifact，验证器有界摘要未取得稳定错误；本批无业务写入 | 读取 Case 20 committed step detail 与脱敏生产日志做根因归因；Case 11 继续按 managed delegation 配置 blocker 处理，不放宽策略 |
| 2026-08-06 23:43 | Production explicit-request preview | Ready `de801ca7`；动态默认 01-25 | 通过 | 从已认证 observatory 唯一验收 scope 恢复参数，仅保留 scope hash `237314c29964`；25/25 preview 通过，全部 method、path template、effective risk、approval enforcement 与 `interactive` execution mode 契约匹配；未启动 workflow、未产生外部写入 | 先执行无副作用和只读批准的 direct runtime；写探针仅按 allowlist 与 typed approval 精确放行 |
| 2026-08-06 23:42 | Production 全量 preview 启动 | Ready `de801ca7`；动态默认 01-25 | 配置阻塞，未请求生产 case | `production_validate.rb` 在参数检查阶段因当前 shell 缺少 `AEVATAR_SCOPE_ID` 立即退出；未创建 member、未绑定、未 preview、未启动 workflow。`config.local.yaml` 已正确物化但不承载该 CLI scope 参数 | 只读发现当前已登录 NyxID/Aevatar scope 配置来源，恢复参数后重新执行完整 preview |
| 2026-08-06 23:40 | Case 20 fresh Lark 发送尝试 | 已登录 Aevatar 私聊；Ready `de801ca7` | UI 控制阻塞，未执行 | `2026-08-06T15:36:04Z` 基线为 7 个目标 run；精确 mount prompt 已填入编辑器，但 Playwright、CUA 与 DOM-CUA 的受支持点击/Enter 都在浏览器输入桥超时，文本仍留在编辑器。`15:39:31Z` 回读仍为同一 7 个脱敏 run hash，增量 0；未点击旧审批卡、未发送消息、未产生 workflow 或其他副作用 | 恢复 in-app browser 输入控制后，仅按一次 Enter 发送已填入的 Case 20 prompt；出现新 skill mount 卡后批准并继续 committed 验证 |
| 2026-08-06 23:36 | Cases 20/21 验收触发契约修正 | channel case 定义与 validator | 静态通过，运行状态未变 | 两条 mount 批准/拒绝 case 的 prompt 从普通“使用”改为显式“挂载 … skill”，并新增精确 prompt 防漂移断言；Case 22 保持“使用已挂载”只读语义。channel/report/`git diff --check` 通过，机器证据仍诚实保留 1 passed/2 failed | 使用修正后的精确 prompt 执行 fresh Lark Case 20 批准与 Case 21 拒绝 |
| 2026-08-06 23:33 | Aevatar Ready 部署确认 | 生产 revision 1392 | 通过，可进入复验 | Ready workload 已切换为 `de801ca7`，精确对应 `de801ca70a37db624b27155c1870d0c99ad93b7c`；image digest `cd66bf29c8a5…`，rollout complete，Pod 1/1 Ready、0 restart | 在已登录 Lark 会话执行 fresh Cases 20/21，分别验证 mount 批准和拒绝的 typed continuation |
| 2026-08-06 23:32 | Ornn 服务端与公开 catalog | `ornn-api`；当前 25 个重新打包 skill | 通过 | `publish_skills.rb --verify-only` 与独立 `verify_public_catalog.rb` 均确认服务端格式、精确名称、版本和 public 状态 25/25；missing/private/version mismatch 均为空，`uploadsPerformed=false`、`permissionsChanged=false`。首次误传不受支持的 `--help` 后中止只读回读，正式完整重跑已通过 | 等待 Aevatar 修复部署；部署后执行 channel fresh 复验 |
| 2026-08-06 23:29 | Aevatar 部署轮询 | 生产 revision 1391 | 无变化，等待部署 | Ready workload 仍为 `1de55eab`、digest `04e7ce8fa6cb…`，1/1 Ready、0 restart；已用被忽略的 `config.local.yaml` 恢复生产物化，未产生 tracked 配置或 build 变更 | 继续有界轮询；部署等待期间核对动态验证入口和默认 case 集合 |
| 2026-08-06 23:47 | Case 20/21 真实 Lark 首次挂载闭环 | Ready `de801ca7`；前滚 `49244090` 包含目标提交 | 2/2 严格通过 | Case 20：mount 卡/callback=1、同一 AgentRun 恢复、workflow 卡/callback=1、唯一新 run `e18081f2211b` committed 3/3 state 30、artifact 与 observatory 脱敏通过；Case 21：mount 卡/callback=1、typed `Denied` / `approval_denied`、mount/start/run delta=0；前滚期间无身份猜测或重复回调 | 同步机器摘要与报告，门禁通过后关闭 #3210 |
| 2026-08-06 23:27 | 静态全量与双端 Skill 校验 | 当前工作树；`config.example.yaml` | 门禁通过，运行状态未收敛 | 25/25 workflow、25/25 Ornn skill、3 个 channel case、21 个 risk case、1 个 regression case、示例物化 YAML、相关 Ruby 语法、报告、Codex `.agents/skills` 与 Claude `.claude/skills` 软链接入口全部校验通过；channel 运行证据仍为 1 passed/2 failed，risk 仍为 12 passed/2 blocked/2 failed/5 not-configured，regression 仍为 `pending-deployment` | 用 `config.local.yaml` 恢复生产物化；等待修复部署后先复验 Cases 20/21，再继续剩余动态清单 |
| 2026-08-06 23:25 | Case 20/21 首次挂载契约补强 | `de801ca70`；验收定义与机器摘要 | 等待部署 | “使用/use/加载/load”与“挂载/mount”已分流；Case 20 明确要求 mount 卡与 workflow 工具卡各 1 张、两次 callback 各 1 次；Case 21 只拒绝 mount 卡并要求 workflow start/run delta 均为 0；旧 `ee031038` / `InvalidWorkflowYaml` 记录转入历史 | 推送验收定义，核验 Ready 部署后执行真实 Lark 批准与拒绝路径 |
| 2026-08-06 23:25 | Aevatar 推送回读与部署轮询 | 远端 `feature/integrate`；生产 revision 1391 | 已推送，等待部署 | `git ls-remote` 确认远端 tip 精确为 `de801ca70a37db624b27155c1870d0c99ad93b7c`；生产仍为 Ready `1de55eab`、digest `04e7ce8fa6cb…`，Pod 1/1 Ready、0 restart，尚不能复验修复 | 有界轮询至 Ready workload 可追溯到 `de801ca70` 或包含它的后代提交 |
| 2026-08-06 23:24 | Cases 20/21 Aevatar 修复扩大门禁 | Aevatar `de801ca70` | 通过 | 根因修复保持明确 `挂载/mount` 的 typed workflow mount intent，普通 `使用/use/load/加载` 继续只读；完整 `Aevatar.AI.Tests` 2469/2469、`Aevatar.GAgents.ChannelRuntime.Tests` 1950/1950、稳定性门禁、架构门禁（含 15/15 架构测试）、87 份文档 lint 与 `git diff --check` 全部通过，仅有既有 NU1510/NU1903 警告 | 回读远端 tip，等待 Ready workload 可追溯到 `de801ca70`，再执行 fresh Lark 批准/拒绝复验 |
| 2026-08-06 23:06 | Cases 20/21 channel 源码归因 | Ready `ee031038`；Aevatar `origin/feature/integrate` | 已定位 Aevatar 缺陷 | 两个 fresh Lark 回合均未产生 `ornn_search_skills`、`use_skill` 或新 mount 审批卡，而是由模型直接调用 `aevatar_start_workflow` 并以 `InvalidWorkflowYaml` 失败；同一会话 Case 22 可进入 workflow 运行期审批，排除 YAML、Ornn catalog、transport 和 UserService grant。源码确认 `SkillRecovery` 未在 Conversation/AgentRun 序列化链丢失；真正缺陷是自然语言 parser 把明确“挂载 skill”与普通“使用/load skill”都降为只读加载，workflow 未挂载后模型仍尝试启动 | 以 typed `MountWorkflowsRequested` 保留显式 mount 意图，确定性执行 search、mount preview、confirmation 与 durable approval，并在目标 Ready 部署复验 |
| 2026-08-06 22:55 | Risk/channel/report 一致性门禁 | 当前工作树 | 通过 | `validate_risk_cases.rb`：21 项为 12 passed、2 blocked、2 failed、5 not-configured；`validate_channel_cases.rb`：1 passed、2 failed；`validate_report.rb`、JSON 解析与 `git diff --check` 全部通过 | 定位 Cases 20/21 在 AgentRun fallback 中产生 `InvalidWorkflowYaml` 的源码根因 |
| 2026-08-06 22:54 | Risk 25/26 证据重算与 README/report 回写 | Ready `ee031038`；fresh Lark Case 22 与 Assistant Case 12 | 通过证据已收敛，待一致性门禁 | Risk 25 的 sender binding、精确 Lark UserService grant、同 run approval resume 与 contact artifact 全部由 Case 22 命中；Risk 26 按原 `/api/chat` 入口 4/4 completed，`total_cents=16623`、`side_effects=false`；汇总更新为 12 passed、2 blocked、2 failed、5 not-configured | 运行 risk、channel 与 report validator；通过后定位 Cases 20/21 的 `InvalidWorkflowYaml` |
| 2026-08-06 17:32 | 本地静态、打包与报告 | 当前工作树 | 部分通过 | workflow、channel、risk、Ornn skill、脚本语法、Agent skill 双入口、HTML 与 `git diff --check` 通过；`validate_report.rb` 以“分析页缺少新增 workflow 21 的严格通过状态”失败；`config.example.yaml` materialize 后发现 `build/workflows/` 留有一个旧生成文件 | 修复报告覆盖与 materialize 的 stale-output 清理，再重跑完整静态批次 |
| 2026-08-06 17:34 | materialize 定向复验 | `config.example.yaml` / 当前工作树 | 通过 | 已清理不再对应源定义的旧 `*.workflow.yaml`；source/build 文件集合逐字匹配，全部生成 YAML 可解析，脚本语法与 diff 检查通过 | 用 `config.local.yaml` 重新物化后执行全量 production preview；报告状态随 fresh 证据统一更新 |
| 2026-08-06 17:36 | Production explicit-request preview | Aevatar 主网；scope hash `237314c29964` | 通过 | 使用 `config.local.yaml` 物化；动态 registry 中全部 workflow preview 通过，method、path template、risk、approval enforcement 与 execution mode 契约均匹配；未启动 workflow、未产生外部写入 | 执行无副作用及只读批准的 direct runtime 批次 |
| 2026-08-06 17:38 | Direct runtime 定向复验 | Aevatar 主网；21/22/23 | 通过 | 三项分别 committed 7/7、8/8、4/4，`stateVersion` 49/55/31，strict artifact contract 全部命中且无副作用；旧 HTTP 400 未复现 | 更新脱敏机器摘要并继续其余 direct runtime；报告统一撤销该环境假回归 |
| 2026-08-06 17:43 | Direct runtime 基础/文件/执行原语 | Aevatar 主网；01/02/11/12/18/19/20 | 部分通过 | 01/02/12/18/19/20 committed completed 且 strict artifact 命中；11 committed failed，`codex_execution_admission_denied`，无 final artifact；本批无业务写入 | 完成其余 direct runtime 后，核对 case 11 readiness、当前部署和 Aevatar admission/log 证据 |
| 2026-08-06 17:46 | Direct runtime 外部只读与受保护只读 POST | Aevatar 主网；03/04/13/14/15/16/17 | 部分通过 | 03/04/13/14/15/17 committed completed 且 strict artifact 命中；14/17 各取得 1 次 typed pending/resume；16 在创建可判定 run 前遇到 HTTP 502 transport failure | 单独重试 16；不得把 502 记为 workflow typed failure |
| 2026-08-06 17:47 | Direct runtime transport 重试 | Aevatar 主网；16 | 通过 | fresh request committed 4/4，`stateVersion=31`，`provider_response_verified=true`、`side_effects=false`；前次 HTTP 502 未复现 | 将 502 归为已恢复的瞬时 transport 故障，继续 preview-mode direct 分支 |
| 2026-08-06 17:50 | Direct runtime 无副作用业务分支 | Aevatar 主网；05/07/08/09/10 | 通过 | 五项均 committed completed 且 strict artifact 命中；`mutation_executed=false`、`message_sent=false`、`approval_created=false`，未产生外部写入 | 保留 06 的既有 committed 审批证据，避免重复创建不可清理对象；评估并执行可清理写探针 |
| 2026-08-06 17:53 | Direct runtime 写探针证据复核 | Aevatar 主网；24/25 | 通过 | 最近一轮分别 committed 5/5、8/8，typed resume=1/2；三轮成功复验累计新增 9 条固定前缀合成 Base 记录，尚未清理 | 保留脱敏运行事实；未获清理授权前不删除，也不重复写入 |
| 2026-08-06 17:55 | Ornn public catalog verify-only | `ornn-api`；本地打包集合 | 阻塞 | 全部 ZIP 通过服务端格式校验；19 个名称/public/version 精确回读，6 个线上不存在；`uploadsPerformed=false`、`permissionsChanged=false` | 使用正式发布器的 missing-only 模式只补缺失项，再做全量精确回读 |
| 2026-08-06 17:58 | 动态 case 盘点 | 当前工作树与三个仓库边界 | 通过 | 动态发现 25 个 workflow、25 个一一对应 Ornn skill、3 个 channel case、21 个 risk case；新增 Risk 43 已纳入范围；Aevatar 既有工作树为脏，平台修复必须使用最新 `origin/feature/integrate` 的隔离 worktree | 补充正式发布器的 missing-only 模式并只发布 6 个缺失 skill |
| 2026-08-06 18:01 | Ornn missing-only 发布 | `ornn-api`；25 个本地包 | 通过 | 25/25 服务端格式通过；19 个线上名称/version/public 精确匹配并跳过；仅创建并公开原缺失的 6 个 skill，逐项回读一致；README 未保存 GUID | 独立执行全目录 verify-only，确认 25/25 最终 catalog 状态 |
| 2026-08-06 18:02 | Ornn 全目录 verify-only | `ornn-api`；25 个本地包 | 通过 | 正式发布器与独立 catalog 验证器均确认 25/25 服务端格式、名称、version、public 精确一致；missing/private/version mismatch 均为空；未上传、未改权限 | 同步 Risk 27 机器证据和 Markdown/HTML 报告，再诊断 Case 11 |
| 2026-08-06 18:06 | Ornn 风险证据与报告一致性 | 当前工作树 | 部分通过 | `validate_risk_cases.rb` 通过：21 项为 9 passed、2 blocked、4 failed、1 pending-execution、5 not-configured；`validate_report.rb` 因仍要求历史 Case 19 `start-blocked` 固定表述而失败 | 更新报告校验器为 catalog 已修复、fresh Lark `pending-execution` 的当前契约并重跑 |
| 2026-08-06 18:08 | 风险证据与报告定向复验 | 当前工作树 | 通过 | Risk YAML/JSON 汇总为 21 项中的 9 passed、2 blocked、4 failed、1 pending-execution、5 not-configured；`validate_risk_cases.rb`、`validate_report.rb`、脚本语法与 `git diff --check` 全部通过 | 诊断并闭环 Case 11 managed `codex_exec` admission 回归 |
| 2026-08-06 18:11 | Case 11 fresh 定向启动 | Ready 镜像 `2046dd5c` | 未启动 | 账号 managed readiness 为 enabled/eligible/active/ready 且无 cleanup pending；验证器在生产调用前因当前 shell 未设置 `AEVATAR_SCOPE_ID` 退出，没有创建 member 或 workflow run | 从本地 team 配置和已认证 Aevatar read model 恢复 scope，再重跑同一隔离定义 |
| 2026-08-06 18:13 | Case 11 fresh run 与线上日志诊断 | Ready 镜像 `2046dd5c`；scope hash `237314c29964` | 失败 | scope/team read model 匹配且 readiness 全部 ready；隔离定义请求以 HTTP 404 结束，未取得可判定 run。最近 15 分钟有界日志无可唯一关联的 404；部署源码仍注册 preview 与 provision 路由 | 单独执行 Case 11 preview，区分 preview 与 provision/binding/invoke 边界 |
| 2026-08-06 18:14 | Case 11 定向 preview | Ready 镜像 `2046dd5c`；隔离 local materialization | 通过 | fixed managed probe preview 通过，0 个外部 call site；HTTP 404 已排除 parser 与 explicit-request preview 边界 | 增加脱敏阶段诊断后重跑 run，定位 binding/provision/invoke 的具体失败点 |
| 2026-08-06 18:15 | Case 11 阶段化 run 诊断 | Ready 镜像 `2046dd5c` | 失败 | 脱敏阶段诊断确认失败位于 `invoke_workflow`，typed HTTP 503 `WORKFLOW_PROJECTION_UNAVAILABLE`；未创建可判定 run，`codex_exec` 尚未执行 | 检查 projection 可用性判定、Pod 状态与部署恢复窗口，必要时有界重试 |
| 2026-08-06 18:16 | Projection 健康与恢复窗口诊断 | Ready 镜像 `2046dd5c` | 部分通过 | Pod Ready、0 restart；最近日志持续成功写入 Elasticsearch workflow/projection read model，未见全局 projection outage；新 member 首次 invoke 仍可能早于投影可见 | 复用同一确定性 member 有界重试 Case 11；复现则进入 Aevatar 根因修复 |
| 2026-08-06 18:16 | Case 11 committed 重试 | Ready 镜像 `2046dd5c`；run hash `e75366e09494` | 失败 | 复用现有 revision 后取得 committed `failed`，state 31，4/4 已观测步骤；`codex_exec` 为 `codex_execution_admission_denied`，无 final artifact；readiness 仍为 ready | 拉取对应时间窗 codex 日志并定位 Aevatar admission 根因 |
| 2026-08-06 18:18 | Case 11 admission 源码/日志归因 | Ready 镜像 `2046dd5c` | 未收敛 | 对应时间窗仅记录 managed tool 注册，未暴露内部 failure code；`f7f543c51..2046dd5c` 的 managed coordinator/transport 无代码变化，历史认证修复未被回退；外层错误仍可能折叠 identity、Vault 或下游 401/403 | 查询 typed audit/read model 的内部 code；若诊断丢失则纳入 Aevatar 最小修复 |
| 2026-08-06 18:28 | Case 11 NyxID 身份传播源码归因 | Ready 镜像 `2046dd5c`；Aevatar/NyxID 本地源码 | 已定位 Aevatar 缺陷 | NyxID transitional 契约允许同时转发 `Authorization` 与已签名 identity assertion；主站 selector 当前在 bearer 存在时忽略 assertion，导致 bearer principal 未提供 native NyxID user claim 时 workflow authority 为空；固定 probe、readiness 与下游 transport 已排除 | 从最新 `origin/feature/integrate` 创建隔离 worktree，补双凭据认证及 workflow caller authority 回归测试后最小修复 |
| 2026-08-06 18:33 | Aevatar 身份断言优先级本地修复 | 基线 `fe752839d`；隔离 worktree | 通过 | 新增测试先稳定复现 2/2 失败：双凭据主体错误与无效 assertion 回退；最小 selector 修复后认证及 workflow caller credential 套件 7/7 通过，assertion 主体和 authority 一致，bearer 仍保留 | 运行完整受影响项目测试、稳定性/架构门禁和 diff 检查，再提交并普通推送 `origin/feature/integrate` |
| 2026-08-06 18:36 | Aevatar 修复扩大测试与门禁 | 隔离 worktree；本机 Redis `8.6.2` | 部分通过 | `architecture_guards.sh`、`test_stability_guards.sh`、`git diff --check` 通过；Capabilities 643 项中 640 通过，3 项仅因固定要求 Redis `7.2.3` 的环境前置失败，与本轮身份变更无关 | 精确排除这 3 个 pinned-Redis 环境用例重跑其余项目测试，复核 diff 后提交 |
| 2026-08-06 18:38 | Aevatar 修复环境隔离复验 | 隔离 worktree | 通过 | 精确排除 3 个 pinned-Redis 版本前置用例后，Capabilities 其余 640/640 通过；结合定向 7/7、架构与稳定性门禁，未发现本轮修复引入回归 | 重新 fetch/rebase 最新 `origin/feature/integrate`，复核并提交修复 |
| 2026-08-06 18:41 | Aevatar 修复 rebase 后复验 | 提交 `cd007b657`；基线 `25a84a44b` | 通过 | 远端前进 1 个不重叠的 Admin Studio 提交后 rebase 成功；身份断言定向套件 7/7、稳定性门禁和 `origin/feature/integrate...HEAD` 完整架构门禁再次通过 | 最后回读远端 tip，普通推送修复到 `origin/feature/integrate` 并等待 Ready 部署 |
| 2026-08-06 18:42 | Aevatar 修复推送与远端回读 | `origin/feature/integrate`；提交 `cd007b657` | 通过 | 普通推送成功，`git ls-remote` 回读的远端 tip 与本地完整提交精确一致；未使用 force push | 有界轮询 Ready workload，确认部署可追溯包含该修复后重跑 Case 11 |
| 2026-08-06 18:43 | Aevatar 修复部署首次轮询 | Ready 镜像 `fe752839` | 等待部署 | Pod Ready、0 restart，已从旧 `2046dd5c` 前滚但尚未包含 `cd007b657`；未执行 Case 11，避免在错误部署上复验 | 低频轮询 CI 与 Ready 镜像，直到可追溯包含修复提交 |
| 2026-08-06 18:47 | 验收闭环 skill 结构复验 | 当前工作树；临时隔离 Python 环境 | 通过 | `skill-creator` 官方 `quick_validate.py` 通过；Codex `agents/openai.yaml` 与 Claude Code 同源 skill 链接有效，批次 README 回写契约已生效 | 继续等待 `cd007b657` 或其后代 Ready 部署 |
| 2026-08-06 18:49 | Aevatar 修复部署第二次前滚 | Ready 镜像 `25a84a44` | 等待部署 | 新 Pod 1/1 Ready、0 restart；该提交是 `cd007b657` 的直接父提交，仍未包含身份修复；远端最新 `32c731992` 已确认是修复后代 | 继续低频轮询下一个 Ready 镜像，不在父提交上重跑 Case 11 |
| 2026-08-06 18:59 | Aevatar 身份修复 Ready 部署 | Ready 镜像 `32c73199` | 通过 | 新 Pod 1/1 Ready、0 restart；git ancestry 明确证明该部署包含 `cd007b657`，满足可追溯部署门槛 | 在该 Ready 镜像上先重跑 Case 11 preview，再运行 fixed managed probe 并读取 committed typed artifact |
| 2026-08-06 19:03 | Case 11 修复部署定向 preview | Ready 镜像 `32c73199`；scope hash `237314c29964` | 通过 | 通过 `/api/auth/me` 与 Studio team roster 在单进程内恢复并交叉校验 scope/team；fixed managed probe fresh preview 通过，0 个外部 call site；未启动 workflow、未产生外部写入 | 在同一 Ready 部署上真实运行 Case 11，并以 committed terminal state 与严格 typed artifact 判定 |
| 2026-08-06 19:04 | Case 11 修复部署首次 runtime | Ready 镜像 `32c73199` | 未启动 | preview 与 endpoint readiness 后，invoke 返回 typed HTTP 503 `WORKFLOW_PROJECTION_UNAVAILABLE`；未取得 run ID，`codex_exec` 尚未执行，不能据此判定身份修复失败 | 复用同一确定性 member 有界重试；若仍失败则重新检查 projection/read model 健康与部署日志 |
| 2026-08-06 19:05 | Case 11 修复部署 committed 重试 | Ready 镜像 `32c73199`；run hash `181874564935` | 失败 | 复用现有 revision 后取得 committed `failed`，state 31，4/4 已观测步骤；`codex_exec` 仍为 `codex_execution_admission_denied`，无 final artifact；已排除首次 invoke 的 projection 窗口 | 拉取该 run 时间窗的脱敏生产日志，确认身份断言修复后的剩余 admission 拒绝边界并继续修复 Aevatar |
| 2026-08-06 19:10 | Case 11 committed run 生产日志诊断 | Ready 镜像 `32c73199`；run hash `181874564935` | 未收敛 | 相关窗口未出现 `codex`、managed admission 或可关联的内部稳定错误码；同窗 workflow/projection read model 写入持续成功，排除全局 projection outage，但外层 `codex_execution_admission_denied` 仍折叠内部原因 | 查询 committed run detail/audit；若仍丢失内部 code，则在 Aevatar 增加无敏感信息的稳定诊断传播与回归测试 |
| 2026-08-06 19:11 | Case 11 managed readiness 与请求身份边界 | Ready 镜像 `32c73199`；scope hash `237314c29964` | 通过边界检查 | readiness 为 enabled/eligible/active、`execution_ready=true`、reason=`ready`、state 3、cleanup 0；`/api/auth/me` 为 NyxID 且 session/top-level scope 一致；interactive invoke 会从当前 HTTP 请求重新提取 caller credential，排除复用 revision 中陈旧凭据的假设 | 读取同一 committed run 的 detail/audit，只输出稳定 code、布尔值和哈希，以区分 identity 与 Vault credential resolution 边界 |
| 2026-08-06 19:13 | Case 11 committed detail/audit 交叉查询 | Ready 镜像 `32c73199`；run hash `181874564935` | 已定位诊断缺口 | observatory detail、member-scoped summary 与 audit 均确认 committed `failed`、state 31、4 个实际步骤，失败点为 `execute_probe`；三个面都只保留 `codex_execution_admission_denied`，未保留任何 `managed_*` 内部 code，无法从 committed 证据区分 identity、descriptor 与 Vault resolution | 在 Aevatar 执行/审计传播链补安全稳定的内部 failure code 与回归测试；部署后先用 fresh run 精确归因，再修实际 managed 执行缺陷 |
| 2026-08-06 19:17 | Case 11 fresh runtime 复现 | Ready 镜像 `09612d0b`；run hash `6f408aa3d421` | 失败 | 当前部署是身份修复后代；fresh preview 通过且 0 外部 call site，复用 revision 后 committed `failed`、state 31、4/4，仍为 `codex_execution_admission_denied` 且无 final artifact | 立即读取本次运行窗口的内部日志；若稳定 detail 仍不可见，则修复内部可观测性但不泄露 provider-owned detail 到公开审计分类 |
| 2026-08-06 19:18 | Case 11 fresh run 日志关联 | Ready 镜像 `09612d0b`；run hash `6f408aa3d421` | 阻塞于内部可观测性 | Ready Pod 单容器、0 restart，stdout 日志持续可读；fresh run 后 5 分钟窗口仍没有 coordinator/transport 的 `Managed Codex` Warning 或任何 `managed_*` code，无法使用现有生产日志区分根因 | 在 Aevatar 内部日志链增加无凭据、可关联且有测试保护的稳定 failure code；保持公开 receipt/audit 的封闭分类不变 |
| 2026-08-06 19:20 | Aevatar managed failure 内部诊断首轮静态批次 | 基线 `bb80c4dea`；隔离 worktree | 未进入测试 | 最小实现仅记录受限 stable code、failure kind 与 12 位 run hash；`git diff --check` 通过；定向 `--no-restore` 因隔离 worktree 缺少 `project.assets.json` 在构建前退出，独立 SHA-256 对照另发现测试预期 hash 需修正 | 修正测试预期，并允许 restore 后重跑 Codex failure audit 定向套件 |
| 2026-08-06 19:20 | Aevatar managed failure 内部诊断定向测试 | 基线 `bb80c4dea`；隔离 worktree | 通过 | restore/build 后 Codex failure audit 13/13 通过；内部 Warning 仅包含 failure kind、受限 stable provider code 与 12 位 run hash，原始 run ID 不出现；公开 receipt/audit 继续只保留封闭 `codex_execution_*` 分类且不含 provider detail | 无构建复验并运行完整 Aevatar.AI.Tests、稳定性/架构门禁与 diff 检查 |
| 2026-08-06 19:21 | Aevatar managed failure 扩大测试首轮 | 基线 `bb80c4dea`；隔离 worktree | 无有效测试结论 | 完整项目 build 与定向 `--no-build` 被错误并行到同一输出目录，最终因 `Aevatar.AI.Tests.runtimeconfig.json` 文件锁退出；两个门禁命令引用了不存在的 `scripts/` 根路径。均属验证命令编排错误，不是用例失败 | 查明实际门禁脚本路径，停止共享输出并发，串行重跑完整项目测试与门禁 |
| 2026-08-06 19:23 | Aevatar managed failure 完整相关项目测试 | 基线 `bb80c4dea`；隔离 worktree | 通过 | 停止共享输出并发后，`Aevatar.AI.Tests` 串行 2459/2459 通过、0 skipped；覆盖 DI 构造、工具执行、审计及相关 AI 路径 | 运行 `tools/ci` 下稳定性与架构门禁，复核 diff 后提交 |
| 2026-08-06 19:24 | Aevatar managed failure 提交前门禁 | 基线 `bb80c4dea`；隔离 worktree | 通过 | Codex failure audit 无构建复验 13/13；stability、architecture、proto lint、audit trail、NyxID chat semantics 等门禁和 `git diff --check` 全部通过 | 增加 unsafe provider code 日志降级负例，复验后 fetch/rebase 最新远端并提交 |
| 2026-08-06 19:25 | Aevatar managed failure 日志注入负例 | 基线 `bb80c4dea`；隔离 worktree | 通过 | 新增包含换行的 provider code 负例并通过；只有有界小写字母、数字、下划线 code 可进入内部日志，其余统一为 `unclassified`；架构门禁与 diff 检查继续通过 | fetch 最新 `origin/feature/integrate`；如远端前进则 rebase 并复验，否则提交当前最小修复 |
| 2026-08-06 19:25 | Aevatar 远端前进检查 | 本地基线 `bb80c4dea`；远端 `92bf13670` | 需要 rebase | `origin/feature/integrate` 已前进，包含 workflow catalogue 与失败终态相关变更；本轮改动仍仅限 executor 与对应测试，diff 无冲突迹象 | 暂存本轮两文件修改，将隔离分支 rebase 到 `92bf13670` 后恢复并重跑定向/完整相关测试与门禁 |
| 2026-08-06 19:26 | Aevatar managed failure rebase | 最新远端 `92bf13670`；隔离 worktree | 通过 | 本轮两文件修改经命名 stash 暂存后 fast-forward/rebase 到最新远端，恢复无冲突，stash 已删除，`git diff --check` 通过；主 Aevatar 脏工作树未改动 | 在新基线上串行重跑完整 `Aevatar.AI.Tests`，再运行稳定性与架构门禁 |
| 2026-08-06 19:33 | Aevatar managed failure rebase 后完整测试 | 最新远端 `92bf13670`；隔离 worktree | 通过 | `Aevatar.AI.Tests` 2460/2460 通过、0 skipped；新增 unsafe provider code 负例已计入，内部诊断与公开审计边界在最新 workflow 变更后仍成立 | 运行最终 stability/architecture 门禁，复核两文件 diff 后提交 |
| 2026-08-06 19:34 | Aevatar managed failure 最终提交前门禁 | 最新远端 `92bf13670`；隔离 worktree | 通过 | stability、architecture、proto lint、audit trail、channel/NyxID 语义门禁与 `git diff --check` 全部通过；diff 仅 executor 和对应 audit test 两文件，不含生产身份或凭据 | 提交内部诊断修复；再次 fetch/rebase 后普通推送到 `origin/feature/integrate` |
| 2026-08-06 19:34 | Aevatar managed failure 诊断修复提交 | 提交 `f0337b80d`；父提交 `92bf13670` | 通过 | 仅提交 executor 内部 Warning 与对应安全/审计回归测试，共两文件；隔离 worktree 提交后干净，验收仓库未提交 | fetch 远端并核对 tip；无前进则普通推送，有前进则 rebase、复验后推送 |
| 2026-08-06 19:35 | Aevatar 推送前远端一致性 | 本地 `f0337b80d`；远端 `92bf13670` | 通过 | 再次 fetch 后远端未前进，本地提交父节点精确等于当前 `origin/feature/integrate`，无需 rebase | 普通推送 `HEAD:feature/integrate`，随后回读远端完整 SHA |
| 2026-08-06 19:35 | Aevatar managed failure 诊断修复推送 | `origin/feature/integrate`；提交 `f0337b80d` | 通过 | 普通推送成功，`git ls-remote` 回读远端 tip 与本地完整 SHA 精确一致；未使用 force push | 有界轮询 Ready workload，只有部署包含该提交或明确后代时才 fresh 复跑 Case 11 并读取安全内部 code |
| 2026-08-06 19:35 | Aevatar 诊断修复部署首次轮询 | Ready 镜像 `5240e518` | 等待部署 | workload 为 1/1 Ready、updated/available 均为 1、0 已知 rollout 故障；该镜像来自推送前远端历史，不含 `f0337b80d` | 继续低频轮询；等待期间执行验收仓库本地静态批次，不在旧镜像复跑 Case 11 |
| 2026-08-06 19:36 | 验收仓库静态与报告一致性复验 | 当前工作树 | 通过 validator | 25/25 workflow、25/25 skill、3/3 channel 定义、21/21 risk 定义均纳入校验；生产/Assistant/admission 脚本语法、报告契约和 `git diff --check` 通过。运行状态仍诚实保留 channel 0/3 严格通过、risk 9 passed/2 blocked/4 failed/1 pending/5 not-configured | 用 `config.example.yaml` 物化并解析全部生成 YAML；随后继续轮询诊断修复部署 |
| 2026-08-06 19:36 | 公开模板 materialize 与 YAML 解析 | `config.example.yaml`；当前工作树 | 通过 | materializer 干净生成动态 inventory 的 25 个 workflow，全部 `YAML.safe_load` 成功，未发现 stale output 或文件集合漂移；`build/` 保持忽略 | 重新用不提交的 `config.local.yaml` 物化生产定义，再检查 `f0337b80d` Ready 部署 |
| 2026-08-06 19:37 | 生产配置 materialize | `config.local.yaml`；当前工作树 | 通过 | 16 个本地 replacement 已重新物化到 25 个 ignored build workflow；命令输出未包含真实值，配置与 build 均未纳入提交 | 检查 Ready image；包含 `f0337b80d` 后 fresh 运行 Case 11 并关联内部 Warning |
| 2026-08-06 19:37 | Aevatar 诊断修复部署第二次轮询 | Ready 镜像 `5240e518` | 等待部署 | generation 与 observedGeneration 均为 2691，workload 1/1 Ready 且 updated/available 为 1；镜像无变化，尚未包含 `f0337b80d`，未在旧镜像重复运行 Case 11 | 查询 `feature/integrate` 对应 CI/build 状态；继续有界低频轮询 |
| 2026-08-06 19:38 | Aevatar 诊断修复 CI 列表检查 | `feature/integrate`；提交 `f0337b80d` | 尚无对应 run | 最近可见 GitHub `ci` 仍是 `09612d0ba` completed/success，列表尚未出现诊断提交；生产已独立前滚到 `5240e518`，不能假设该 Actions 列表与部署一一同步 | 查询 `f0337b80d` 精确 check-runs/commit status，并继续观察 Ready image |
| 2026-08-06 19:38 | Aevatar 诊断提交精确 checks/status | 提交 `f0337b80d` | 符合分支契约 | GitHub 返回 0 check-runs、0 commit statuses；`.github/workflows/ci.yml` 的 push 触发只包含 `main`/`dev`，不包含 `feature/integrate`，所以无 Actions run 是预期而非失败 | 继续以外部构建链产生的 Ready image 与 git ancestry 作为部署证据 |
| 2026-08-06 19:39 | Aevatar 诊断修复部署第三次轮询 | Ready 镜像 `5240e518` | 等待部署，无状态变化 | 30 秒后 deployment 仍为 generation 2691、1/1 Ready、updated/available 为 1；没有 failed rollout 信号，目标提交仍未部署 | 只读检查 registry 的 `f0337b80` 镜像 tag，区分构建等待与部署等待 |
| 2026-08-06 19:39 | Aevatar 诊断镜像 registry 检查 | 镜像 tag `f0337b80` | 等待构建 | Docker registry 返回 `no such manifest`，说明目标提交镜像尚未发布；当前不是 rollout 卡死，也不能在旧 Ready 镜像复验诊断日志 | fetch 远端检查是否已有包含修复的后代提交；继续低频检查后代 tag/Ready image |
| 2026-08-06 19:40 | Aevatar 诊断提交远端后代检查 | `origin/feature/integrate` | 无后代、等待构建 | fetch 后远端 tip 仍精确为 `f0337b80d`，ancestry 检查通过但没有更新提交可替代目标 tag；registry 仍需构建当前 tip | 低频等待镜像；同时盘点 risk/channel 未通过项的精确入口，为 Case 11 恢复后的全量闭环排序 |
| 2026-08-06 19:44 | Aevatar 诊断镜像发布与 rollout 启动 | 镜像 `f0337b80`；digest `sha256:c15b43abc85255d9b7312e271126f5c97e4c35328bad30cb60d21d8bb75019f8` | 等待 Ready | registry manifest 已发布；新 ReplicaSet `aevatar-console-backend-57c874fbdd` 已创建并请求 1 个副本，但本批结束时尚无 Ready 副本，旧 `5240e518` workload 仍承担 Ready 流量 | 低频轮询 deployment 与新 Pod；只有 `f0337b80` 达到 1/1 Ready 后才重跑 Case 11 |
| 2026-08-06 19:49 | Aevatar 诊断修复 Ready 部署 | Ready 镜像 `f0337b80`；digest `sha256:c15b43abc85255d9b7312e271126f5c97e4c35328bad30cb60d21d8bb75019f8` | 通过 | deployment generation/observedGeneration 均为 2693，目标提交对应镜像为唯一副本且 1/1 Ready、updated/available 均为 1、0 restart；运行 digest 与 registry manifest 精确一致 | 在该 Ready 部署 fresh 运行 Case 11，随后按 run hash 读取内部受限 failure kind/code |
| 2026-08-06 19:51 | Case 11 运行前身份预检 | Ready 镜像 `f0337b80`；scope hash `237314c29964` | 通过 | `/api/auth/me` 已认证，顶层 scope、session scope 与 profile subject 哈希一致；只检查脱敏字段结构和布尔状态，未输出真实 scope 或凭据 | 从忽略的本地配置读取 team/service，单进程执行 Case 11 preview 与 fresh runtime |
| 2026-08-06 19:51 | Case 11 诊断部署 fresh runtime | Ready 镜像 `f0337b80`；run hash `557edd506461` | 失败 | fresh preview 通过且 0 个外部 call site；复用 revision 后 committed `failed`、state 31、4/4，`execute_probe` 仍为 `codex_execution_admission_denied`，无 final artifact | 立即按同一 run hash 查询诊断部署的受限内部 Warning，提取 stable failure kind/code |
| 2026-08-06 19:52 | Case 11 诊断部署日志关联 | Ready 镜像 `f0337b80`；run hash `557edd506461` | 未取得内部 code | fresh run 后 5 分钟精确匹配及 10 分钟 `codex`/`managed`/`admitted` 扩大搜索均为空；新 Warning 未出现在当前 Pod stdout | 核对 executor 调用边界与生产日志级别，判断失败是否在进入 `AdmittedAgentToolExecutor` 前已被折叠 |
| 2026-08-06 19:54 | Case 11 managed failure 源码边界复核 | 部署源码 `f0337b80` | 缩小范围，未最终归因 | `CodexExecutionException` 会经过 executor catch；生产没有日志级别覆盖。managed coordinator 的 transport/readiness 失败已有 Warning，只有 request validation/identity 的 `ManagedCodexRequestException` 直接返回且不记录；这使 `managed_identity_unavailable` 成为待证假设，但尚不能写成确认根因 | 在 required logger 的 coordinator failure 生成边界补受限 code 日志，或在后代 Ready 部署上取得等价 typed 证据后再修实际缺陷 |
| 2026-08-06 19:55 | Aevatar 后代镜像 rollout | 镜像 `3f62ff62`；远端 tip `3981c1e1d` | 等待 Ready | `3f62ff62` 是 `f0337b80` 的直接后代，明确包含诊断修复；deployment generation 2695 已启动新 Pod，但本批结束时新副本未 Ready，旧 `f0337b80` 仍 1/1 Ready | 等待后代镜像 Ready；不跨两个 deployment 重跑或混合日志证据 |
| 2026-08-06 19:56 | Aevatar 诊断后代 Ready 部署 | Ready 镜像 `3f62ff62`；digest `sha256:578d292d1c71f56a50e550dcaf3b8f6485ecfa9ac316f2774e5a901195aa2b8e` | 通过 | deployment generation/observedGeneration 均为 2695，目标后代镜像已成为唯一副本且 1/1 Ready、updated/available 均为 1、0 restart；git ancestry 包含 `f0337b80` | fresh 重跑 Case 11，并立即按单 Pod、run hash 查询受限日志 |
| 2026-08-06 19:57 | Case 11 后代部署首次 invoke | Ready 镜像 `3f62ff62` | 未启动 | preview/既有 binding 后 invoke 返回 typed HTTP 503 `WORKFLOW_PROJECTION_UNAVAILABLE`；未取得 run ID，`codex_exec` 尚未执行，不能据此判断诊断或功能状态 | 复用同一确定性 member 有界重试，取得 committed run 后再关联单 Pod 日志 |
| 2026-08-06 19:57 | Case 11 projection 有界重试 | Ready 镜像 `3f62ff62` | 持续未启动 | 第二次复用同一确定性 member 仍在 invoke 返回 typed HTTP 503 `WORKFLOW_PROJECTION_UNAVAILABLE`；连续两次均未创建 run，重复执行不再提供新证据 | 检查新 Pod projection 初始化、Elasticsearch 写入与 workload readiness；区分暂时恢复窗口和 Ready 判定缺陷 |
| 2026-08-06 19:58 | Projection/Ready 生产诊断 | Ready 镜像 `3f62ff62` | 全局链路健康，member 可见性待重试 | 新 Pod 持续成功提交 projection scope 事件并写入 Elasticsearch 的 workflow binding、execution board 与 status read model；Pod/Deployment Ready、0 restart。启动期仅一次端口未监听的 readiness 过渡，无持续错误 | 在 projection 持续写入后对同一 member 做最后一次有界 invoke；成功创建 run 后立即关联日志，仍 503 则升级为独立 Aevatar activation 缺陷 |
| 2026-08-06 19:59 | Case 11 projection 恢复后 runtime | Ready 镜像 `3f62ff62`；run hash `ee61879a2f4a` | 失败 | invoke 已恢复并取得 committed `failed`，state 31，4/4；fixed probe 仍为外层 `codex_execution_admission_denied`，无 final artifact | 固定单 Pod 查询同一时间窗，读取诊断提交新增的受限 failure kind/code |
| 2026-08-06 20:00 | Case 11 内部 failure code 关联 | Ready 镜像 `3f62ff62`；run hash `ee61879a2f4a` | 已定位根因边界 | coordinator 与 executor 两层日志精确一致：`failureKind=AdmissionDenied`、`failureCode=managed_proxy_authorization_denied`；工具上下文有 caller bearer/NyxID access token，排除 identity、readiness 与 Vault credential unavailable | 核对 Aevatar managed transport 的 Agent Key header、service binding 与 NyxID 已发布代理认证契约，修复 Aevatar 下游授权调用 |
| 2026-08-06 20:04 | Managed Agent Key / NyxID 契约对照 | Aevatar `3f62ff62`；本地 NyxID 源码 `bf484f395` | 已定位分类缺口 | 唯一 managed key active、未过期、`proxy`/`codex`，严格只允许当前 sandbox+LLM 两个 UserService，service/node scope 均精确；Aevatar 已使用 `X-API-Key` 与 exact `_nyxid_via`。但 bounded transport 将 HTTP 401 与 403 折叠为同一 code，无法区分 Vault 中陈旧 secret 与真实授权拒绝 | 在 Aevatar 内将 401/403 映射为不同的稳定内部 failure code并补测试；部署后 fresh run 精确归因 |
| 2026-08-06 20:06 | Aevatar managed proxy auth 分类定向测试 | 基线 `3981c1e1d`；隔离 worktree | 通过 | transport 定向套件 18/18 通过；真实 HTTP 401 映射为 `managed_proxy_authentication_failed`，403 保持 `managed_proxy_authorization_denied`，两条路径均断言不泄露响应体中的 Agent Key；`git diff --check` 通过且仅两文件改动 | 运行完整 ChronoSandbox 测试项目、稳定性/架构门禁和最终 diff 检查 |
| 2026-08-06 20:06 | Aevatar managed proxy auth 分类扩大测试 | 基线 `3981c1e1d`；隔离 worktree | 通过 | 完整 `Aevatar.AI.Infrastructure.ChronoSandbox.Tests` 211/211 通过、0 skipped；credential lifecycle、readiness、transport 与 coordinator 相关路径未见回归 | 运行仓库 stability/architecture 及相关语义门禁，复核 diff 后提交 |
| 2026-08-06 20:10 | Aevatar managed proxy auth 分类提交前门禁 | 基线 `3981c1e1d`；隔离 worktree | 通过 | stability、architecture、proto、workflow saga/binding、audit、channel、NyxID 与 docs lint 全部通过；Architecture tests 15/15，`git diff --check` 通过，diff 仍仅 transport 与对应测试两文件 | fetch 最新 `origin/feature/integrate`；如远端前进则 rebase/复验，否则提交并普通推送 |
| 2026-08-06 20:11 | Aevatar managed proxy auth 分类远端一致性 | 本地/远端基线 `3981c1e1d` | 通过 | fetch 后 `origin/feature/integrate` 未前进，本地分支父节点与远端完整 SHA 精确一致；无需 rebase，工作区仅两处预期修改 | 只提交 transport 401/403 分类与对应回归测试，然后再次 fetch 并普通推送 |
| 2026-08-06 20:11 | Aevatar managed proxy auth 分类提交 | 提交 `48261819b`；父提交 `3981c1e1d` | 通过 | 两文件提交完成：401/403 稳定分类与真实 HTTP boundary 回归测试；隔离 worktree 提交后干净，验收仓库未提交 | 再次 fetch 远端；无前进则普通推送 `HEAD:feature/integrate` 并回读完整 SHA |
| 2026-08-06 20:12 | Aevatar managed proxy auth 分类推送 | `origin/feature/integrate`；提交 `48261819b` | 通过 | 普通推送成功，`git ls-remote` 回读远端 tip 与本地完整 SHA 精确一致；未使用 force push | 等待 registry manifest 与可追溯 Ready workload，随后 fresh Case 11 区分 401 stale secret 与 403 authorization |
| 2026-08-06 20:12 | Aevatar auth 分类部署首次轮询 | Ready 镜像 `3981c1e1` | 等待构建 | 生产已前滚到目标提交的直接父节点并 1/1 Ready；registry 尚无 `48261819` manifest，远端 tip 仍精确为目标提交 | 低频等待目标镜像构建；不在父节点上重复 Case 11 |
| 2026-08-06 20:14 | Aevatar auth 分类镜像有界等待 | 镜像 tag `48261819` | 等待构建，无状态变化 | 两次间隔 20 秒的 registry 检查均无 manifest；未观察到 rollout 失败，因为 deployment 尚未引用目标 tag | 继续低频检查 registry 与 Ready workload，manifest 发布后再进入 rollout 验证 |
| 2026-08-06 20:14 | Aevatar auth 分类部署复查 | Ready 镜像 `3981c1e1` | 等待构建，无状态变化 | 目标 registry tag 仍不存在；deployment generation 2697 稳定、父提交镜像 1/1 Ready，尚未开始目标 rollout | 延长轮询间隔，等待 `48261819` manifest |
| 2026-08-06 20:15 | Aevatar auth 分类镜像延长等待 | 镜像 tag `48261819` | 等待构建，无状态变化 | 40 秒后 registry 仍无目标 manifest；未触发 production runtime 重试 | 检查远端是否已有包含修复的后代提交，并继续等待目标或后代镜像 |
| 2026-08-06 20:16 | Aevatar auth 分类远端后代检查 | `origin/feature/integrate` | 无后代、等待构建 | fetch 后远端 tip 仍精确为 `48261819b`，目标 ancestry 正常但没有后代镜像可替代 | 继续低频等待当前 tip 的 registry manifest |
| 2026-08-06 20:17 | Aevatar auth 分类镜像第二次延长等待 | 镜像 tag `48261819` | 等待构建，无状态变化 | 再过 40 秒 registry 仍无 manifest；未观察到 deployment 更新或可判定构建失败 | 继续低频等待并复查 deployment/registry |
| 2026-08-06 20:19 | Aevatar auth 分类镜像第三次延长等待 | 镜像 tag `48261819` | 等待构建，无状态变化 | 再过 50 秒 registry 仍无 manifest；推送后约 7 分钟，仍接近既有外部构建窗口 | 继续等待目标 manifest，父镜像保持 Ready |
| 2026-08-06 20:20 | Aevatar auth 分类镜像第四次延长等待 | 镜像 tag `48261819` | 等待构建，无状态变化 | 再过 50 秒 registry 仍无 manifest；推送后约 8 分钟，deployment 尚未引用目标 tag | 继续低频等待，接近既有约 9 分钟构建时长 |
| 2026-08-06 20:21 | Aevatar auth 分类镜像第五次延长等待 | 镜像 tag `48261819` | 等待构建，无状态变化 | 再过 45 秒 registry 仍无 manifest；推送后约 9 分钟，尚无可判定失败信号 | 下一轮同时检查目标/后代 registry 与 deployment image |
| 2026-08-06 20:23 | Aevatar auth 分类镜像发布与 rollout 启动 | 镜像 `48261819`；digest config `sha256:89166692c836ba4cafabd976af3368fcacbd85843efca48f720c00c3fc68a8be` | 等待 Ready | registry manifest 已发布；deployment generation/observedGeneration 为 2699/2699，目标镜像已开始 rollout；本批结束时 2 个副本中仅 1 个 updated、1 个 Ready/available，尚未证明目标镜像为唯一 1/1 Ready | 低频轮询 deployment 与目标 Pod；只有 `48261819` 成为唯一 1/1 Ready、0 restart 后才 fresh 运行 Case 11 |
| 2026-08-06 20:24 | Aevatar auth 分类 Ready 部署 | Ready 镜像 `48261819`；运行 digest `sha256:6365e07ddb8291b2c5a27df7158ca2e25d4b7061390ff4ccc66a13b92a808384` | 通过 | deployment generation/observedGeneration 均为 2699，目标提交对应镜像为唯一副本且 1/1 Ready、updated/available 均为 1、0 restart；rollout status 成功 | 在该 Ready 部署单进程 fresh 运行 Case 11，并立即按 run hash 查询同一 Pod 的受限 stable failure code |
| 2026-08-06 20:25 | Case 11 auth 分类部署 fresh runtime | Ready 镜像 `48261819`；run hash `28dff88dcde0` | 失败 | 身份预检三处 scope hash 一致；fresh preview 通过且 0 个外部 call site；复用 revision 后 committed `failed`、state 31、4/4，`execute_probe` 仍为外层 `codex_execution_admission_denied`，无 final artifact | 固定唯一目标 Pod 查询同一 run hash 的受限内部日志，用新分类区分 401 stale Agent Key secret 与 403 authorization |
| 2026-08-06 20:26 | Case 11 auth 分类日志关联 | Ready 镜像 `48261819`；run hash `28dff88dcde0` | 已定位 authentication 边界 | 唯一目标 Pod 的 transport 与 admitted executor 两层受限日志一致返回 `managed_proxy_authentication_failed`；新 401/403 分类证明下游是 HTTP 401，排除真实授权范围 403 | 对照 NyxID Agent Key rotation 与 Aevatar Vault secret 生命周期，确认 active 元数据和 raw secret 是否漂移并修复根因 |
| 2026-08-06 20:29 | Managed credential 轮换前一致性核对 | Aevatar status / NyxID key metadata | 已确认 raw secret 漂移 | Aevatar read model 仍为 `active`、`execution_ready=true`、state 3、cleanup 0；NyxID 仅有 1 个同名 active key，未过期，`proxy`/`codex`、两个精确 service grant、无 allow-all。结构元数据全部正确但真实 transport 401，只能由当前 Vault raw secret 无效解释 | 使用当前已认证用户调用正式 `/api/managed-codex/credential/rotate`，有界等待新 authoritative state 后复验 Case 11 |
| 2026-08-06 20:30 | Managed credential 首次 rotate 请求 | 当前已认证 NyxID 用户 | 结果不明，已停止重试 | NyxID CLI 未返回可接受的成功状态；命令未输出或保存原始错误和任何 credential 标识。由于 lifecycle 写可能已被受理，不能把 CLI 非零等同于“未执行”，也不能盲目重复轮换 | 只读回查 authoritative state version、active managed key 数量及脱敏 stable error，确认是否已有状态变化后再决定恢复动作 |
| 2026-08-06 20:31 | Managed credential rotate 后只读回查 | Aevatar state / NyxID key metadata / 目标 Pod | 明确未写入 | state 仍为 3、cleanup 0；同名 key 仍为原 active hash `3cffd111e4a0`，inactive 数为 0；服务端 rotate endpoint 在约 162 ms 内返回 HTTP 502。没有新 key、旧 key 撤销或 Actor commit，排除“已受理但 CLI 非零” | 再做一次有界复现并只提取响应 stable code/脱敏 message，定位 lifecycle 前置失败，不盲目多次轮换 |
| 2026-08-06 20:33 | Managed credential rotate 有界复现 | 当前已认证 NyxID 用户 | 失败，无写入 | 请求仍为 HTTP 502，CLI 响应没有可提取 stable code；立即回查 state 仍为 3、cleanup 0、active key hash 仍为 `3cffd111e4a0` 且 inactive 数 0，确定第二次也未进入外部 mutation | 从 Host endpoint 测试、lifecycle exception mapping 与脱敏服务端日志定位无 code 502；修复或选择正确显式恢复入口后再轮换 |
| 2026-08-06 20:36 | Managed credential rotate 前置契约归因 | Aevatar Actor descriptor / NyxID active key | 排除 expiry 漂移 | 两侧 expiry 均存在且 Unix 毫秒精确相等，delta 为 0；结合 key ID、policy、service/node scope 已精确匹配，`ValidatePersistedKey` 前置不会解释当前 502 | 核对经 NyxID proxy 进入 Aevatar 后的原始 access-token 提取；定位 lifecycle 写接口是否误用仅供 Aevatar 调用的入站 bearer |
| 2026-08-06 20:41 | Managed credential lifecycle 认证边界归因 | Aevatar/NyxID 源码与 live UserService 配置 | 已定位 Aevatar 分类缺陷 | live `aevatar` 为 `auth_method=none`、`forward_access_token=true`、identity JWT + delegation token，CLI 无环境 token/profile override，排除 service credential 覆盖；credential adapter 将 NyxID 401/403 error envelope 一律折叠为 `managed_api_key_issue_invalid`，Host 默认映射 502，导致运维入口丢失真实认证/授权边界 | 从最新 `origin/feature/integrate` 新建隔离 worktree，为 credential adapter 与 Host endpoint 补 401/403 稳定分类和测试，普通推送后等待部署复验 rotate |
| 2026-08-06 20:40 | Managed credential lifecycle 认证边界红态测试 | 基线 `5cddd8e15`；隔离 worktree | 预期失败，根因已复现 | adapter 参数化用例 2/2 失败：NyxID HTTP 401/403 均被折叠为 `managed_api_key_issue_invalid`；Host 参数化用例 2/2 失败：`managed_user_authentication_failed` 与 `managed_user_authorization_denied` 均被默认映射为 HTTP 502。四条红态与生产 rotate 502 边界一致，未调用生产、未产生写入 | 最小修复 adapter 的 401/403 stable code 与 Host 的 401/403 HTTP 映射，再重跑两个定向套件 |
| 2026-08-06 20:43 | Managed credential lifecycle 认证边界定向复验 | 基线 `5cddd8e15`；隔离 worktree | 通过 | adapter 2/2 与 Host 2/2 参数化用例全部转绿；NyxID error envelope 的 body 不进入异常，401/403 分别保留为 `managed_user_authentication_failed` / `managed_user_authorization_denied`，Host 分别返回 HTTP 401/403；Host 无构建复验再次 2/2 通过 | 串行运行完整 ChronoSandbox 与 Capabilities 受影响项目，再执行 stability、architecture、语义和 docs 门禁 |
| 2026-08-06 20:47 | Managed credential lifecycle 扩大项目测试 | 基线 `5cddd8e15`；本机 Redis `8.6.2` | 通过受影响代码；3 项环境前置不满足 | 完整 ChronoSandbox 213/213 通过；Capabilities 全量 647 项中 644 通过，3 项仅因测试固定要求 Redis `7.2.3` 而本机为 `8.6.2` 失败。精确排除这 3 个 pinned-Redis 用例后，其余 644/644 通过，未排除任何 credential、Host 或 runtime 用例 | 运行 stability、architecture、proto/语义、docs lint 与 `git diff --check`，复核最终改动范围 |
| 2026-08-06 20:49 | Managed credential lifecycle 提交前门禁 | 基线 `5cddd8e15`；隔离 worktree | 通过 | stability guard 及其 guard 负例自测、architecture 全扫描、Architecture Tests 15/15、proto、audit、workflow/channel/NyxID 语义、87 份 docs lint 与 `git diff --check` 全部通过；diff 仅 adapter、Host endpoint、两组回归测试和运维文档 5 文件 | fetch 最新 `origin/feature/integrate`；远端前进则 rebase 后重跑定向测试与门禁，否则提交最小修复 |
| 2026-08-06 20:49 | Managed credential lifecycle 推送前远端一致性 | 本地/远端基线 `5cddd8e15` | 通过 | fetch 后本地 HEAD 与 `origin/feature/integrate` 完整 SHA 精确一致，远端未前进，无需 rebase；隔离 worktree 仍仅 5 个预期文件修改 | 提交认证/授权边界修复；再次 fetch，确认远端基线未变化后普通推送 |
| 2026-08-06 20:49 | Managed credential lifecycle 修复提交 | 提交 `b12b748f6`；父提交 `5cddd8e15` | 通过 | 仅提交 adapter 的 NyxID 401/403 stable code、Host HTTP 401/403 映射、两组参数化回归测试和运维说明，共 5 文件；提交后隔离 worktree 干净 | 再次 fetch `origin/feature/integrate`；无远端前进则普通推送 `HEAD:feature/integrate` 并回读完整 SHA |
| 2026-08-06 20:49 | Managed credential lifecycle 最终推送门槛 | 本地 `b12b748f6`；远端 `5cddd8e15` | 通过 | 第二次 fetch 后本地提交父节点与 `origin/feature/integrate` 完整 SHA 精确一致；隔离 worktree 干净、仅 ahead 1，无需 rebase | 普通推送 `HEAD:feature/integrate`，随后用 `git ls-remote` 回读远端完整 SHA |
| 2026-08-06 20:52 | Managed credential lifecycle 修复推送 | `origin/feature/integrate`；提交 `b12b748f6` | 通过 | 普通推送成功；`git ls-remote` 回读远端 tip 为 `b12b748f6db1f165baeba1af5cb60eecdf3ab736`，与本地提交精确一致，未使用 force push | 低频轮询 registry 与 Ready workload；只有目标提交或明确后代成为唯一 1/1 Ready 后才复验 rotate |
| 2026-08-06 20:53 | Managed credential lifecycle 部署首次轮询 | Ready 镜像 `5cddd8e1`；目标 `b12b748f` | 等待构建 | deployment generation/observedGeneration 为 2701/2701，父提交镜像是唯一副本且 1/1 Ready、updated/available 均为 1、0 restart；生产尚未引用目标提交 | 低频等待目标或明确后代镜像；期间只做本地 inventory/validator 复核，不在父镜像复验 rotate |
| 2026-08-06 20:55 | 动态 inventory 与 validator registry 复核 | 当前验收工作树 | 通过 | 动态发现 25 个 workflow、25 个唯一且一一对应的 Ornn skill、3 个 channel case、21 个 risk case、1 个 schedule、6 个 fixture、16 个 replacement 及完整 validation/report 文件；已完整读取 production、Assistant、admission、channel、risk、workflow、skill 和 report validator，未发现 orphan 定义或执行入口遗漏 | 继续轮询 `b12b748f` 或明确后代的 registry/Ready 状态；部署前不执行生产 rotate |
| 2026-08-06 20:55 | Managed credential lifecycle 部署第二次轮询 | target tag `b12b748f`；Ready `5cddd8e1` | 等待构建，无状态变化 | registry 尚无目标 manifest；deployment 仍为 generation 2701/2701、父提交镜像唯一 1/1 Ready、updated/available 均为 1，没有 rollout 失败信号 | 利用构建窗口执行验收仓库完整静态检查；随后继续低频轮询目标或后代镜像 |
| 2026-08-06 20:56 | 验收仓库完整静态提交前检查 | 当前验收工作树 | 通过 | 25/25 workflow、25/25 Ornn skill、3/3 channel 定义、21/21 risk 定义、production/Assistant/admission 语法、报告契约、示例配置 materialize、生成的 25 份 YAML 解析及 `git diff --check` 全部通过。首次 YAML 统计 one-liner 仅因输出字符串引号错误在解析前退出，修正命令后 25/25 解析通过 | 继续轮询目标或后代镜像；生产复验前用忽略的 `config.local.yaml` 重新 materialize，避免示例占位符进入运行 |
| 2026-08-06 20:58 | Managed credential lifecycle 部署第三次轮询 | target `b12b748f6`；远端后代 `f984def70` | 等待后代构建 | `git merge-base --is-ancestor` 确认远端新 tip `f984def70` 包含目标修复，后代仅增加 Admin Studio UI 调整；目标 tag 仍无 manifest，deployment 仍是父提交 `5cddd8e1` 的唯一 1/1 Ready 副本 | 接受 `f984def7` 作为可追溯后代候选并继续低频等待其 registry manifest 与唯一 Ready workload |
| 2026-08-06 21:00 | Managed credential lifecycle 部署第四次轮询 | target `b12b748f` / 后代 `f984def7` | 等待构建，无状态变化 | 两个候选 tag 均尚无 registry manifest；deployment 仍为 generation 2701/2701，`5cddd8e1` 唯一副本 1/1 Ready、0 已知 rollout 故障，尚未开始目标或后代 rollout | 拉长轮询间隔，继续等待 manifest；不在旧部署重复 rotate 或 Case 11 |
| 2026-08-06 21:02 | Managed credential lifecycle 目标 rollout 启动 | 目标镜像 `b12b748f`；runtime digest `sha256:a351bfb72e0359362893c7d1718caa2eec63a8ee21df4e54e00ed6e59c1b1bdc` | 等待 Ready | deployment generation/observedGeneration 为 2703/2703、2 个副本中 1 updated/1 Ready；目标 Pod 已拉取运行、0 restart 但 readiness 尚未通过，旧 `5cddd8e1` Pod 继续承载流量 | 低频等待目标 Pod Ready 且旧 Pod 退出；只有目标成为唯一 1/1 Ready 后才复验 rotate |
| 2026-08-06 21:03 | Managed credential lifecycle 目标 Ready 部署 | 目标镜像 `b12b748f`；runtime digest `sha256:a351bfb72e0359362893c7d1718caa2eec63a8ee21df4e54e00ed6e59c1b1bdc` | 通过 | rollout status 成功；deployment generation/observedGeneration 为 2703/2703，目标提交镜像成为唯一副本且 1/1 Ready、updated/available 均为 1、0 restart | 通过 NyxID 正式调用 rotate，仅保留 HTTP、stable code 与 typed receipt 字段存在性；随后只读查询 authoritative state |
| 2026-08-06 21:03 | Managed credential lifecycle 目标部署 rotate 复验 | Ready 镜像 `b12b748f` | 失败，无 typed receipt | 经 `nyxid proxy request aevatar` 调用正式 rotate 仍为 HTTP 502；CLI 进程正常结束但响应无 stable code，`status`、actor/key/command/expiry receipt 字段均不存在。新 401/403 映射未被触发，不能盲目重复轮换 | 只读关联目标 Pod 最近日志与 authoritative state，定位 adapter 前置失败、非标准 NyxID error envelope 或其他 lifecycle code |
| 2026-08-06 21:08 | Managed credential rotate 生产日志首轮关联 | Ready 镜像 `b12b748f`；当前目标 deployment | 无关联日志，诊断未收敛 | 只读拉取最近 2 小时目标 deployment 日志并按 managed Codex、credential、NyxID、rotate、502 过滤，没有命中；Pod 状态查询因未引用的 zsh 数组路径被 shell 在执行前拒绝，本批未产生生产变更，也不能据此判断 502 来源 | 修正只读 Pod 查询并扩大到有界请求/异常日志模式；同时补真实 HTTP 401/403 客户端边界测试，区分应用 lifecycle code 与 ingress/gateway 502 |
| 2026-08-06 21:10 | Managed credential rotate 生产与源码边界复核 | Ready 镜像 `b12b748f`；唯一 Pod | Pod 健康，失败码映射仍不完备 | 修正后的只读查询确认目标 Pod 1/1 Ready、0 restart；最近 30 分钟通用 warn/error/request 过滤仍无命中。源码枚举发现 rotate 前置链的 `nyxid_identity_invalid`、`managed_service_catalog_invalid`、`managed_api_key_list_invalid`、`managed_api_key_issue_invalid` 及 descriptor/vault 校验码仍会默认映射为 HTTP 502，现有 401/403 测试只覆盖其中一个 adapter envelope 分支 | 先增加真实 HTTP 401/403 客户端边界和 endpoint stable-code 完整映射红态；不重复生产 rotate，直到新的失败能返回 typed code |
| 2026-08-06 21:13 | Managed credential rotate NyxID/remote 根因边界 | NyxID 本地源码；Aevatar `origin/feature/integrate=f984def70` | 失败位于 NyxID mutation 之前 | NyxID proxy 对下游非 2xx 原样透传 status/body，不会主动把 401/403 改写为 502；NyxID key rotate 本身先停用旧 key 再创建新 key，而生产旧 key 在两次失败后均保持 active，证明请求未进入该 mutation。Aevatar 远端后代 `f984def70` 明确包含 `b12b748f6` | 从最新远端创建新隔离 worktree，先用真实 HTTP status 的 adapter 红态和 rotate 各前置阶段的 typed endpoint 诊断测试锁定边界，再做最小修复 |
| 2026-08-06 21:15 | Managed credential rotate 新隔离基线 | Aevatar `00c5a22f0`；分支 `codex/managed-rotate-diagnostics-20260806` | 通过 | 创建 worktree 时远端已再次前进到 `00c5a22f0`，ancestry 检查确认它包含 `b12b748f6`；新 worktree 从该最新 tip 创建且初始干净，未接触 Aevatar 主工作树 | 补 endpoint 有界 stable-code 日志红态与真实 HTTP 401/403 adapter 覆盖，再实施最小诊断修复并跑定向/扩大门禁 |
| 2026-08-06 21:17 | Managed credential rotate 诊断红态首次执行 | Aevatar `00c5a22f0`；新隔离 worktree | 未执行测试，构建前置缺失 | 两个精确 `dotnet test --no-restore` 均在编译前因新 worktree 缺少 `obj/project.assets.json` 以 `NETSDK1004` 退出；未把该环境前置误记为红态或通过 | 允许 NuGet restore 后重跑真实 HTTP 401/403 adapter 与 endpoint 有界日志两个精确用例 |
| 2026-08-06 21:20 | Managed credential rotate 诊断红态 | Aevatar `00c5a22f0`；新隔离 worktree | 客户端 2/2 通过；日志红态 1/1 失败 | restore 后真实 HTTP 401/403 adapter 参数用例 2/2 通过，排除 HTTP status 包装差异；endpoint `managed_api_key_issue_invalid` 502 日志断言按预期失败，捕获集合为空，证明当前服务端没有可关联的 lifecycle stage/code 诊断 | 在 endpoint catch 仅记录 operation、stable code 和映射 HTTP status，不记录 exception message、bearer、用户或资源标识；随后重跑两个精确套件 |
| 2026-08-06 21:22 | Managed credential rotate 诊断定向复验 | Aevatar `00c5a22f0`；新隔离 worktree | 通过 | 真实 HTTP 401/403 adapter 2/2 继续通过；endpoint 有界日志 1/1 转绿，断言仅出现固定 operation、stable lifecycle code 与映射 HTTP status，bearer、用户及上游 message 均未进入日志；响应契约和 status mapping 未改变 | 运行完整 ChronoSandbox 与 Capabilities 项目；通过后执行仓库 stability/architecture/语义/docs 门禁并复核 diff |
| 2026-08-06 21:23 | Managed credential rotate ChronoSandbox 全量 | Aevatar `00c5a22f0`；新隔离 worktree | 通过 | `Aevatar.AI.Infrastructure.ChronoSandbox.Tests` 完整项目 213/213 通过，无失败或跳过；真实 HTTP transport 分类与 credential lifecycle 其他路径均未回归 | 运行完整 Capabilities 项目，若仅命中已知 Redis 7.2.3 本地前置则精确排除环境用例复核其余集合 |
| 2026-08-06 21:26 | Managed credential rotate Capabilities 全量 | Aevatar `00c5a22f0`；本机 Redis `8.6.2` | 受影响代码通过；3 项环境前置不满足 | 完整 Capabilities 648 项中 645 通过；仅 3 个 `AgentToolAdmissionLedgerTests` 在启动 fixture 时因固定要求 Redis `7.2.3`、实测 `8.6.2` 失败，新增 endpoint 日志及全部 credential/Host 用例均通过 | 仅按三个完整测试名精确排除环境用例重跑其余 645 项，不排除整个类或任何 managed credential 用例 |
| 2026-08-06 21:28 | Managed credential rotate Capabilities 环境隔离复验 | Aevatar `00c5a22f0`；本机 Redis `8.6.2` | 通过 | 仅按三个完整测试名排除固定 Redis 7.2.3 前置后，其余 645/645 通过；未排除整个类、credential lifecycle、Host endpoint 或本轮新增测试 | 运行 stability、architecture、proto/语义、docs lint 与 `git diff --check`，复核最终 diff 只含 endpoint、两组测试及必要文档 |
| 2026-08-06 21:29 | Managed credential rotate 修复范围复核 | Aevatar `00c5a22f0`；新隔离 worktree | 通过 | 当前 diff 仅 3 文件：Host endpoint 增加有界 stable-code warning、adapter 测试改为真实 HTTP 401/403、Host 增加日志脱敏回归测试；响应、状态映射和 lifecycle 编排均未改变 | 在既有 managed Codex rollout 运维文档补充日志字段/脱敏边界，再执行完整提交前门禁 |
| 2026-08-06 21:32 | Managed credential rotate 提交前门禁 | Aevatar `00c5a22f0`；新隔离 worktree | 通过 | test stability 及全部 guard meta-tests、完整 architecture full-scan、proto、NyxID chat 语义、workflow closed-world/binding/run-id、channel、CQRS、audit、runtime callback、docs lint 与 `git diff --check` 均通过；并行显式 Architecture Tests 首次因缺 assets 未执行，但完整 architecture guard 内部 restore 后同一项目 15/15 通过 | 最终复核 4 文件 diff、敏感信息和工作树；fetch 最新远端，若前进则 rebase 并重跑定向测试/门禁，否则提交最小诊断修复 |
| 2026-08-06 21:33 | Managed credential rotate 最终 diff/文档复核 | Aevatar `00c5a22f0`；新隔离 worktree | 通过 | 最终仅 4 个预期文件、96 insertions/6 deletions：endpoint、真实 HTTP status 测试、日志脱敏测试和既有运维文档；docs lint 87/87、`git diff --check` 通过。新增 bearer/key/message 均为合成负断言，没有真实凭据或运行标识 | fetch 最新 `origin/feature/integrate`；远端前进则 rebase 后重跑定向测试与门禁，否则提交并普通推送 |
| 2026-08-06 21:34 | Managed credential rotate 提交前远端一致性 | Aevatar 本地/远端 `00c5a22f0` | 通过 | fetch 后本地 HEAD 与 `origin/feature/integrate` 完整 SHA 精确一致，远端未前进；worktree 仍只有 4 个预期文件修改，无需 rebase | 仅暂存 4 个预期文件并提交；随后再次 fetch，确认远端基线未变化后普通推送 |
| 2026-08-06 21:35 | Managed credential rotate 诊断修复提交 | Aevatar 提交 `ee031038b`；父提交 `00c5a22f0` | 通过 | 仅提交 endpoint 有界 stable-code 日志、真实 HTTP 401/403 回归测试、日志脱敏回归测试和运维说明 4 文件；提交信息为 `fix: log managed credential lifecycle failures` | 再次 fetch `origin/feature/integrate`；远端仍为父提交才普通推送 `HEAD:feature/integrate` |
| 2026-08-06 21:36 | Managed credential rotate 最终推送门槛 | 本地 `ee031038b`；远端 `00c5a22f0` | 通过 | 第二次 fetch 后本地提交父节点与 `origin/feature/integrate` 完整 SHA 精确一致；worktree 干净、仅 ahead 1，无需 rebase | 普通推送 `HEAD:feature/integrate`，随后用 `git ls-remote` 回读远端完整 SHA |
| 2026-08-06 21:37 | Managed credential rotate 诊断修复推送 | `origin/feature/integrate`；提交 `ee031038b` | 通过 | 普通推送成功；`git ls-remote` 回读远端 tip 为 `ee031038b3d498648d90283b55f6e30a1fa2549f`，与本地提交精确一致，未使用 force push；隔离 worktree 干净 | 低频轮询 registry 与 Ready workload；只有该提交或明确后代成为唯一 1/1 Ready 后才复验 rotate 并读取有界 stable-code 日志 |
| 2026-08-06 21:38 | Managed credential rotate 部署首次轮询 | 提交 `ee031038b`；错误 registry probe tag `ee031038b`；Ready `00c5a22f` | 等待构建 | registry probe 误用了 9 位 tag，返回 manifest 不存在，不能作为正确 8 位部署 tag 的 manifest 证据；deployment generation/observedGeneration 为 2707/2707，父提交镜像唯一 1/1 Ready、updated/available 均为 1；远端 tip 仍精确为目标提交，没有 rollout 失败信号 | 改用 8 位镜像 tag `ee031038` 复核 registry 与 Ready workload，部署前不重复 production rotate |
| 2026-08-06 21:39 | Managed credential rotate 部署第二次轮询 | 提交 `ee031038b`；错误 registry probe tag `ee031038b`；Ready `00c5a22f` | 等待构建，无状态变化 | 约 30 秒后的 registry probe 仍误用了 9 位 tag，因此 manifest 不存在结果无效；deployment 保持 generation 2707/2707、父提交镜像唯一 1/1 Ready，尚未开始目标 rollout | 改用 8 位镜像 tag `ee031038`；等待窗口复核验收 README/report 与最终 inventory，不在旧部署重复写探针 |
| 2026-08-06 21:40 | 验收 README/report 与 Skill 等待窗口复核 | 当前验收工作树 | report/inventory 通过；Skill validator 环境阻塞 | report validator 通过；动态清单仍为 25 workflow、25 skill、3 channel、21 risk、1 schedule、6 fixture，`git diff --check` 通过。系统 `python3` 缺少 PyYAML，官方 `quick_validate.py` 在导入阶段退出，未进入 Skill 校验，不能记为 Skill 失败 | 使用 Codex 随附依赖环境重跑同一官方 validator；随后继续低频轮询目标部署 |
| 2026-08-06 21:41 | 验收闭环 Skill 官方校验 | `.agents/skills/aevatar-acceptance-loop` | 通过 | 系统与随附 Python 均缺 PyYAML；改用一次性 `uv --with pyyaml` 环境运行同一官方 `quick_validate.py` 后输出 `Skill is valid!`，未修改仓库或全局 Python。Codex 入口与 Claude Code 软链接继续指向同一 Skill | 继续轮询 `ee031038b` registry/Ready 部署；部署后仅重试一次 rotate 并用新有界日志定位 stable code |
| 2026-08-06 21:42 | Managed credential rotate 部署第三次轮询 | 提交 `ee031038b`；错误 registry probe tag `ee031038b`；Ready `00c5a22f` | 等待构建，无状态变化 | registry probe 仍误用了 9 位 tag，不能据其 manifest 不存在判断正确 8 位 tag 的构建状态；deployment 继续为 generation 2707/2707、父提交镜像唯一 1/1 Ready；远端 tip 未变化且无 rollout 失败 | 改用 8 位镜像 tag `ee031038` 并继续检查 Ready workload |
| 2026-08-06 21:43 | Managed credential rotate 部署第四次轮询 | 提交 `ee031038b`；错误 registry probe tag `ee031038b`；Ready `00c5a22f` | 等待构建，无状态变化 | 本轮 manifest 不存在仍来自错误 9 位 tag，不能作为目标 manifest 证据；deployment 仍为父提交唯一 1/1 Ready、generation 2707/2707，未观察到失败 rollout | 改用 8 位镜像 tag `ee031038`，并继续检查远端后代与 Ready workload |
| 2026-08-06 21:44 | Managed credential rotate 部署第五次轮询 | 提交 `ee031038b`；错误 registry probe tag `ee031038b`；Ready `00c5a22f` | 等待构建，无状态变化 | registry probe 的 9 位 tag 错误使 manifest 结论无效；deployment 父提交继续唯一 1/1 Ready、generation 2707/2707；远端 tip 仍精确为目标提交，没有后代候选 | 改用 8 位镜像 tag `ee031038`，部署前不运行 production 写探针 |
| 2026-08-06 21:45 | Managed credential rotate 部署第六次轮询 | 提交 `ee031038b`；错误 registry probe tag `ee031038b`；Ready `00c5a22f` | 等待构建，无状态变化 | registry probe 的 manifest 不存在结果因 9 位 tag 错误而无效；deployment 仍为父提交唯一 1/1 Ready、generation 2707/2707，无失败信号 | 改用 8 位镜像 tag `ee031038`，随后复查 registry、deployment 与远端 tip |
| 2026-08-06 21:46 | Managed credential rotate 部署第七次轮询 | 提交 `ee031038b`；错误 registry probe tag `ee031038b`；Ready `00c5a22f` | 等待构建，无状态变化 | registry probe 仍错误使用 9 位 tag，不能据此判断正确 tag 是否已有 manifest；deployment 父提交唯一 1/1 Ready、generation 2707/2707，远端 tip 仍精确为目标提交 | 改用 8 位镜像 tag `ee031038` 检查 manifest 与 Ready workload |
| 2026-08-06 21:39 | Managed credential rotate 目标 Ready 复核 | 镜像 `ee031038`；提交 `ee031038b`；runtime digest `sha256:f7d85dca2c81e89f230aad7b88fc8287db279e195e64b32239793700f5c097d2` | 通过 | 使用正确 8 位部署 tag 复核；rollout status 成功，deployment generation/observedGeneration 为 2709/2709，目标镜像唯一副本 1/1 Ready、updated/available 均为 1、0 restart | 仅通过 NyxID 正式重试一次 rotate；随后读取 authoritative credential state 和新有界 stable-code 日志 |
| 2026-08-06 21:45 | Managed credential rotate 目标部署复验 | Ready 镜像 `ee031038`；authoritative state 3 | 失败，mutation 未开始 | 仅通过 `nyxid proxy request aevatar` 调用一次 rotate，返回 HTTP 502 且无 typed receipt；前后状态均为 active/ready、state version 3、cleanup 0。新有界日志精确命中 `chrono_sandbox_delegation_misconfigured`，未保存原始响应、Pod 名称或任何凭据标识 | 对照 Aevatar catalog resolver 与 NyxID 本地源码，区分生产 UserService 配置缺口和 Aevatar 过严校验；先补最小红态再修复 |
| 2026-08-06 21:47 | Managed credential rotate NyxID 配置归因 | 当前用户的 `chrono-sandbox` / `chrono-llm-public` UserService | 配置阻塞，不是 Aevatar 缺陷 | 只读回读确认两条服务均 active/personal；`chrono-sandbox` 当前为 `forward_access_token=true`、`inject_delegation_token=true`、scope=`llm:proxy`，不满足运维文档固定前置 `false/true/proxy:*`。Aevatar resolver 与测试按设计在 key mutation 前 fail closed；当前安装的 `nyxid service update` 也未提供三项策略参数，未编写临时 HTTP 绕过 CLI | 继续所有不依赖 managed credential 的 case；最终仅把这项列为外部配置 blocker，取得显式安全变更路径后再调整 UserService 并复验 rotate/Case 11 |
| 2026-08-06 21:48 | 生产配置 materialize fresh 批次 | `config.local.yaml`；当前动态 inventory | 通过 | 16 个本地 replacement 均已解析，重新生成 25 个 ignored build workflow；25/25 份 YAML 均通过 `YAML.safe_load`，命令输出未包含 replacement 值 | 从 `/api/auth/me` 恢复并交叉校验 scope，在不启动 workflow 的前提下执行全部 production preview |
| 2026-08-06 21:50 | Production explicit-request preview fresh 批次 | Ready 镜像 `ee031038`；scope hash `237314c29964` | 通过 | 使用本地生产 materialization 动态验证全部 25 个 workflow；25/25 preview 通过，call-site method、path template、risk、approval enforcement 与 interactive execution mode 均满足 registry 契约；未启动 workflow、未产生外部写入 | 先运行无副作用和纯只读 direct runtime；Case 11 保持 NyxID 配置 blocker，副作用分支另批复核目标与清理条件 |
| 2026-08-06 22:01 | 无副作用与只读 direct runtime fresh 批次 | Ready 镜像 `ee031038`；21 个 case | 20 通过，1 个 invoke 失败 | 01-05、07-09、12-23 均取得 committed `completed`、完整步骤和严格 artifact；05、07-10 使用 preview 分支且未写入，14/17 各观察并恢复 1 次 typed read-only approval。Case 10 在 `invoke_workflow` 收到 HTTP 502，未取得 run evidence，不能记为运行失败或通过 | 单独 fresh 重跑 Case 10 preview 分支；复现则关联有界生产日志和 NyxID proxy 边界，不重复整批 |
| 2026-08-06 22:02 | Case 10 invoke 定向复验 | Ready 镜像 `ee031038`；fresh run hash `638a9e09e789` | 通过 | 相同生产 materialization 与 preview prompt 下取得 committed `completed`、state 67、10/10 步，严格 artifact 命中；`approval_created=false`、`message_sent=false`。首轮 502 未复现且未形成 run，当前归为瞬时传输失败 | fresh 运行 Case 11 固定 managed probe，保留 committed 终态并与已确认的 NyxID delegation 配置 blocker 对照 |
| 2026-08-06 22:03 | Case 11 managed probe fresh runtime | Ready 镜像 `ee031038`；run hash `be807f86ddab` | committed 失败，配置阻塞 | preview 通过且 0 外部 call site；运行 committed `failed`、state 31、4/4 步，公开失败为 `codex_execution_admission_denied`、无 final artifact。有界内部日志同时出现 `managed_proxy_authentication_failed`；结合 rotate 的 `chrono_sandbox_delegation_misconfigured` 与只读 UserService 配置，仍归因于 NyxID delegation 前置未满足 | 不修改 Aevatar；继续副作用 probe、Assistant/channel/risk/schedule/Ornn 验证，待获得安全的 UserService 策略更新路径后复验 rotate 与 Case 11 |
| 2026-08-06 22:05 | 可清理 Base 写探针 fresh runtime | Ready 镜像 `ee031038`；Cases 05/24/25 | 通过 | 三项均 committed `completed` 并命中严格 artifact；05 与 24 各观察/恢复 1 次 typed approval，25 连续观察/恢复 2 次，共创建 4 条固定合成前缀 Base 记录。未执行消息或审批写分支 | 只读按三个固定合成前缀定位本轮记录；仅在记录集合和字段契约精确匹配时删除并回读确认 |
| 2026-08-06 22:07 | Base 探针清理只读盘点 | 资产盘点合成表；固定验收 key/字段契约 | 通过盘点，待清理 | 表内 7 条记录中，6 条精确命中仓库固定验收 key：3 条 `manual-asset-attestation-20260804`、1 条本轮 runtime、2 条本轮 sequential；另 1 条不匹配验收模式并排除。Case 05 key 固定且无自动创建时间字段，无法只区分本轮与两条历史残留 | 删除前再次校验 6 条的 Control/Owner/Status/Sequence/Reviewed At 全字段；仅删除完整匹配的验收记录并回读要求匹配数为 0 |
| 2026-08-06 22:09 | Base 探针精确清理 | 本轮 4 条 + 同 key 历史残留 2 条 | 通过 | 删除前 6/6 条同时满足固定 key 与 Control/Owner/Status/Sequence/Reviewed At 契约；逐条 DELETE 均返回 code 0，重新列表后四类验收 key 匹配数为 0，未匹配的第 7 条记录未触碰。仓库未保存 record ID；可恢复性取决于目标 Base 回收站策略 | 运行 `/api/chat` Assistant 动态案例并按 typed artifact 区分 validated、typed failure 与未启动 |
| 2026-08-06 22:19 | Assistant `/api/chat` fresh 批次 | Ready 镜像 `ee031038`；01/12/13/14/15 | 5/5 通过 | 五项均按 Ornn 搜索、精确 skill、typed mount approval、workflow 启动与 committed current state 完成，`workflowValidationStatus=validated`、`workflowValidated=true`、`caseValidated=true`，严格 artifact 全部命中。12/14 的最终自然语言仍描述旧 Running/Awaiting 状态，未覆盖 committed `completed` 机器证据 | 使用 Lark channel canary 真实验证 channel cases；不得从 `/api/chat` 结果外推 webhook、callback 或 Lark 回传通过 |
| 2026-08-06 22:35 | Lark channel fresh 批次 | Case 19 镜像 `19b5906b`；Channel 20-22 镜像 `ee031038` | 2 通过，2 失败 | Case 19 最新 run hash `03c3f4ded68e` committed 4/4；114 字节文件卡片经尾随 LF 归一化为 113 字节 committed descriptor/extraction，文件、SHA、Lark ingress、脱敏与无副作用 artifact 全命中。Case 22 新 run hash `08cdd96d61dd` 从 `awaiting_tool_approval` 经最后一张 workflow 卡只批准一次后 committed 3/3，严格 artifact 命中。Cases 20/21 均未出现新 mount 审批卡，以 `InvalidWorkflowYaml` 结束，目标 workflow run delta=0 | 同步 risk/channel 机器摘要、Markdown/HTML 报告并修复报告校验器；随后定位 Cases 20/21 的 Aevatar definition resolution 根因 |
| 2026-08-06 19:41 | 动态未通过 case 账本复核 | 当前 risk/channel 机器摘要 | 通过盘点、未全绿 | 3 个 channel 为 pending-execution；risk 中 4 failed、2 blocked、1 pending-execution、5 not-configured。稳定缺口覆盖 Lark typed deny/AgentRun、Assistant `code_execute` authority、`workflow_call` definition resolution、parallel/race deterministic worker，以及缺少 disposable source target 的发送/提交/schedule | Case 11 恢复后先处理无需新增外部资源的平台/runtime 缺陷，再执行真实 Lark；源副作用项只在可清理目标与 authority 齐全时运行 |
| 2026-08-06 19:42 | Aevatar 诊断镜像与 Ready 联合轮询 | tag `f0337b80` / Ready `5240e518` | 等待构建，无状态变化 | registry 仍无目标 manifest；deployment 仍为 generation 2691、1/1 Ready、updated/available 为 1。没有 rollout 失败，目标代码尚未进入镜像 | 等待期间只读定位 Risk 37 当前源码边界；随后继续低频联合轮询 |
| 2026-08-06 19:43 | Risk 37 workflow_call 当前源码归因 | 历史基线 `6df43b83` / Ready `5240e518` | 需要 fresh 复验 | 旧失败报告锁定 Studio provision 缺 inline definition；当前 Aevatar 已有 registry definition resolver 与 resolved binding 主链，且 Ready 镜像精确包含新的 workflow catalogue 修复 `5240e518`，历史 30 秒超时不能继续代表当前状态 | 找到现有无副作用 workflow_call probe/入口，在 `5240e518` fresh 运行并以 child start 与 committed artifact 判定 |
| 2026-08-06 19:44 | Risk 37 catalogue 修复边界复核 | Ready/提交 `5240e518` | Aevatar 缺陷仍存在 | 精确 diff 显示该提交只新增 scope workflow catalogue 查询，未给 Studio `/provision-workflow` 增加 inline child definitions，也未改变验收入口的单 YAML 绑定；registry resolver 本身不能让未绑定子定义可见，旧 30 秒超时机制仍由当前源码解释 | Case 11 诊断部署完成后，为 provisioning 多定义契约补回归测试与最小 Aevatar 修复，再新增正式无副作用 workflow case 覆盖 workflow_call |
| 2026-08-06 19:44 | Aevatar 诊断镜像与 Ready 再轮询 | tag `f0337b80` / Ready `5240e518` | 等待构建，无状态变化 | registry 仍返回目标 manifest 缺失；deployment generation 2691 完全 observed、1/1 Ready，无 rollout 错误。目标修复还未进入任何可复验镜像 | 再有界等待 45 秒；若仍无 manifest，检查外部部署节奏/后续远端提交，不重复运行失败 case |
| 2026-08-06 19:45 | Aevatar 诊断部署 45 秒有界等待 | tag `f0337b80` / Ready `5240e518` | 等待构建，无状态变化 | 等待后 registry 仍无 manifest，deployment 仍 1/1 Ready、generation 2691/2691；无失败 rollout，也无可追溯目标镜像 | 查看 registry 最近 tag 时间和 ReplicaSet 历史以确认外部构建节奏；继续处理不依赖目标部署的诊断 |

## 财务源工作流 post-fix 验收

这组结果独立于下方公开案例统计，使用 `~/workflows` 源定义、同结构安全变体和脱敏生产输入取得。财务 scope 的历史同一 run 对照显示 Base 读取成功、随后 `code_execute` 401；源码契约将根因定位为 sandbox UserService 未转发 caller bearer。Aevatar execution delegation 已包含所需执行权限，只调整 sandbox 转发策略且未添加静态 credential。下列四项成功证明对应时间点的功能主链曾在线执行，但 #3290 已证明它们不能代表 2026-08-07 14:15 后的当前连续可用性，也不代表安全限制下的发送、审批和排程分支已运行。

| 源定义 | Preview / 输入边界 | 真实终态 | 副作用与结论 |
|---|---|---|---|
| 单步 `code_execute` probe | 新 member；一个固定输出的无副作用步骤 | 单次 invoke，run catalog `0 -> 1`，1/1 completed，`lastSuccess=true`，final output 非空 | scope 特定 sandbox bearer 转发链通过 |
| P2 shared-Base no-send 同结构运行定义 | 基于 `budget_monitor_weekly.shared-base.nosend.yaml` 刷新 live selector，不是当前文件的逐字副本；6 个唯一 GET；全部 read-only；0 approval | 单次 invoke，run catalog `0 -> 1`，8/8 completed，`lastSuccess=true`；首个 Base 输出与 final output 非空 | 未发送消息，未创建 schedule；#3161 `nyxid_proxy -> code_execute` 功能主链通过，不外推到当前文件或旧 Base |
| `invoice_file_chain.v5.workflow.json` | exact JSON；5 个唯一 call site；sanitized PNG；`submit=false` | 只 invoke 一次；14/14 实际步骤 completed，`lastSuccess=true`，final output 非空 | 图片抽取、只读 lookup、preview presentation 通过；完整提交分支未执行，无 approval、无 Lark 写入 |
| PDF attachment probe | 无副作用 PDF 输入 | run catalog +1，2/2 completed，`lastSuccess=true`；extract 与 final output 非空 | PDF 附件接收与抽取主链通过 |
| #3290 时间连续性对照 | 同一 P2 member/binding/definition 与 byte-identical preview；另一个已绑定 P1 | P2 14:09:19 为 14/14 completed 且复算一致，14:18:27 为 7/8 / `NYXID_PROXY_HTTP_502`；14:20-14:42 同一 preview 连续 5 次 `NYXID_ADMISSION_SOURCE_CREDENTIAL_REQUIRED`；P1 为 0/27 / `Forwarding failed` | 当前 `regression-blocked`；issue/同事证据不冒充本仓库独立复验；历史成功继续保留 |

源目录中明确未运行：P2 send workflow、P1 v6、durable/weekly schedule 和 P1 v2 旧定义。它们分别由 Risk 40、41、30 和 42 约束；前三个源副作用定义缺一次性目标、显式授权或清理闭环，P1 v2 已按 legacy replacement 口径退休。现有 Risk 28、Risk 29、Risk 40 与 Risk 41 均不验证成功后重新绑定、member/部署变化或 silo relocation 的时间连续性。下一条 continuity case 至少需要：同一 exact source 在切换前后两次 preview + run、source-readable caller credential 与 delegation credential 分离可解析、P2 14/14 复算、P1 首步 forwarding 与 27 步进展、脱敏 deployment/silo transition 证据。真实 Lark attachment canary 已由 Risk 24 关闭；公开案例 15 的 schedule 成功不能替代源排程或该连续性证据。

## 工作流矩阵

| # | 工作流 | 步骤 | 主要能力 | 直接生产验证 | 副作用 |
|---:|---|---:|---|---|---|
| 01 | `release_readiness_review` | 13 | `assign`、解析、分支、并行 `foreach` | committed 通过，`ready_for_review=true` | 无 |
| 02 | `candidate_document_compliance_preview` | 3 | 类型化文件、并行 `document_extract`、受约束 LLM | committed 通过，五类材料字段均为 true | 无 |
| 03 | `email_access_approval_audit` | 5 | 审批列表、ID 提取、动态详情 GET | committed 通过，`instance_reachable=true` | 无 |
| 04 | `saas_license_utilization_review` | 10 | 六路 Base 汇聚、确定性利用率与成本聚合 | committed 通过，全部合计命中 | 无 |
| 05 | `asset_inventory_attestation` | 7 | preview/submit、受保护 Base POST | 最新 preview 分支 completed，`mutation_executed=false` | 无；历史 submit 分支另有通过证据 |
| 06 | `project_shared_mailbox_approval` | 8 | Base GET、审批 POST、实例回读 | committed 通过，回读为 `PENDING`，`stateVersion=55` | 创建一条合成验收审批 |
| 07 | `quarterly_access_review_reminder` | 7 | preview/submit、受保护 Lark 私信 | 最新 preview 分支 completed，`message_sent=false` | 无；历史 submit 分支另有通过证据 |
| 08 | `saas_license_optimization_digest` | 19 | 六路 Base、标准化、摘要、interactive card | 最新 preview 分支 completed，六源汇总正确 | 无；未发送卡片 |
| 09 | `contractor_access_package_approval` | 25 | 附件、LLM、身份目录、历史去重、审批创建与验证 | 最新 preview 分支 completed，`approval_created=false` | 无；历史 submit/idempotent 分支另有通过证据 |
| 10 | `monthly_access_certification` | 23 | 月末门禁、聚合、审批、提醒与完成通知 | 最新 preview 分支 completed，审批和消息均未创建 | 无；历史写入分支另有通过证据 |
| 11 | `complex_codex_exec_validation` | 32 | 固定 managed probe、五项 gate、receipt 恢复、并行证据 | committed 通过，30/30 步，`parallel_check_count=5` | 无 |
| 12 | `safe_code_execute_validation` | 4 | 固定 JavaScript、结构化 receipt、金额断言 | 连续两次 committed 通过，`total_cents=16623` | 无 |
| 13 | `invoice_ocr_policy_review` | 10 | 合成 PDF、字段提取、SGD/金额归一化、历史去重 | committed 通过，`stateVersion=82` | 无 |
| 14 | `lark_contact_batch_resolution` | 3 | `contact/v3/users/batch_get_id`、标识脱敏 | committed 通过，`stateVersion=25`；preview `approvalEnforcement=bind_time_confirmation` | 无 |
| 15 | `weekly_budget_variance_digest` | 11 | 六路 Base、预算差异、周报/月报、schedule 契约 | committed 通过，`stateVersion=73` | 无 |
| 16 | `nyxid_read_receipt_probe` | 4 | 单次 Base GET、provider receipt、首步输出与终态 | committed 通过，`stateVersion=31` | 无 |
| 17 | `lark_post_search_approval_probe` | 4 | 语义只读 POST、bind-time 批准契约、nested resume | committed 通过，`stateVersion=31`；preview `approvalEnforcement=bind_time_confirmation` | 无 |
| 18 | `supplier_control_attestation_review` | 15 | `guard`、`conditional`、`while` 三个确定性原语 | committed 通过，`stateVersion=92`，15/15 步 | 无 |
| 19 | `lark_bot_file_upload_validation` | 3 | Lark 入站 file ref、单次 `document_extract`、SHA-256 与 transport 证据分层 | preview 通过；direct committed 4/4、`stateVersion=30`；fresh Lark committed 4/4、`stateVersion=32`、run hash `03c3f4ded68e`，typed artifact 精确命中 | 无业务副作用；skill `1.1`；114 字节卡片归一化为 113 字节 descriptor/extraction；原始标识未持久化 |
| 20 | `supplier_risk_tier_aggregation` | 13 | `map_reduce` 与 `cache` 两个确定性原语 | committed 通过，`stateVersion=89`，15/15 步 | 无 |
| 21 | `approval_window_integrity_audit` | 7 | 审批时间窗口审计、legacy/active 双窗口对照、过期陷阱上报 | clean isolated materialization 后 7/7 completed，`stateVersion=49`，artifact 命中 | 无 |
| 22 | `acceptance_fixture_drift_attestation` | 8 | 四组 canary fixture 只读体检、漂移显式化 | clean isolated materialization 后 8/8 completed，`stateVersion=55`，artifact 命中 | 无 |
| 23 | `readonly_attested_post_probe` | 4 | `risk: read_only` 声明 POST、免运行时审批执行 | clean isolated materialization 后 4/4 completed，`stateVersion=31`，artifact 命中 | 无 |
| 24 | `runtime_tool_approval_write_probe` | 5 | 未声明风险 POST、运行时 tool approval 单次挂起-恢复、写入回执 | committed 通过，`approvalResumeCount=1`，`mutation_executed=true` | 新增一条可清理探针记录 |
| 25 | `sequential_tool_approval_write_probe` | 8 | 一次运行内连续两次审批挂起-恢复组合 | committed 通过，`approvalResumeCount=2`，`sequential_mutations=2` | 新增两条可清理探针记录 |
| 26 | `vendor_policy_inline_delegation` | 6 | 固定 inline `workflow_call` bundle、child definition 解析与父子 artifact 汇合 | Ready `4c0596c7` 上 preview 通过；committed 5/5、`stateVersion=44`，child resolved/completed | 无 |
| 27 | `deterministic_parallel_evidence_review` | 12 | 三路确定性 `parallel` worker、receipt fan-in 与合并顺序 | Ready `4c0596c7` 上 preview 通过；committed 14/14、`stateVersion=86`，3 个 receipt 与顺序断言命中 | 无 |
| 28 | `deterministic_race_policy_review` | 3 | 三路确定性 `race`、first success 与迟到完成忽略 | Ready `4c0596c7` 上 preview 通过；committed 6/6、`stateVersion=38`，winner/first success/later ignored 命中 | 无 |
| 29 | `invoice_approval_routing_preview` | 10 | 合成发票附件、审批历史百条窗口、attested read-only contact 路由与 no-submit artifact | Ready `6a656d75` 上 preview 2/2 call site；run `a729912ee5d9` committed 11/11、`stateVersion=77`，严格 artifact 命中 | 无 |

## Lark channel E2E 矩阵

这三项不是新的 workflow，也不增加 29 个 Ornn skill 的数量。它们复用 Case 14 的 `lark-contact-batch-resolution`，分别覆盖 `/api/chat` 和 direct runtime 无法证明的 Lark AgentRun skill mount 审批，以及 skill 已挂载后的 workflow 运行期工具审批回调边界。

| # | Channel case | 用户决策 | 严格通过条件 | 当前状态 |
|---:|---|---|---|---|
| 20 | `lark_agent_run_skill_approval_approved` | 批准 | 显式“挂载”触发精确 skill recovery；mount 审批卡与 callback 各 1 次；同一 AgentRun 恢复且 `use_skill=Completed`、`mount_executed=true`；随后 workflow 工具审批卡与 callback 各 1 次；同一 workflow run 恢复；普通 AgentRun 可见回复与 awaiting 文本均为 0；3/3 committed 脱敏 artifact 精确命中 | `passed` |
| 21 | `lark_agent_run_skill_approval_rejected` | 拒绝 | 显式“挂载”触发精确 skill recovery；mount 审批卡与 callback 各 1 次；同一挂起调用返回 typed `Denied` / `approval_denied`；`mount_executed=false`；workflow 审批卡、workflow start 与 run catalog 增量均为 0 | `passed` |
| 22 | `lark_workflow_runtime_tool_approval_approved` | 批准 | skill 已挂载且不出现新 mount 审批；workflow start=1；新 run 晚于本次 Lark inbound 启动；审批卡投递与回调各一次；同一 workflow run 恢复；3/3 steps、stateVersion 30、脱敏 artifact 精确命中 | `passed` |

Case 20/21 的目标修复提交 `de801ca70a37db624b27155c1870d0c99ad93b7c` 已由 Ready 镜像 `de801ca7` 真实验证，当前 Ready 前滚 `49244090` 直接包含该提交。Case 20 新 run hash `e18081f2211b` committed `completed`、state 30、3/3，workflow 审批前后保持同一 run，observatory 的 request/output/timeline 均只含脱敏标记。Case 21 拒绝回调期间发生无损前滚，但同一挂起 AgentRun 返回 typed `Denied` / `approval_denied`，目标 workflow catalog 增量仍为 0。旧 `ee031038` / `InvalidWorkflowYaml` 结果只作为历史负证据保留。Case 22 最新真实运行 hash `08cdd96d61dd` 继续证明已挂载路径的 workflow 审批恢复。Bot 文案、`[tool receipt] Approval pending`、审批卡本身或 direct workflow 成功仍不能替代这些证据。

最新全量回归使用部署镜像 `0c4ff023`。其中 11 曾在账号 managed credential 已显示 `execution_ready=true` 的情况下连续两次于 `execute_probe` 以 `codex_execution_admission_denied` 失败；修复镜像 `f7f543c5` 部署后，11 的定向复验已 committed `completed`。12 的连续两次成功证据继续保留。14 和 17 的业务 artifact 均成功，但没有出现 preview 所要求的 per-run typed approval identity，因此不能把终态完成写成严格通过；历史批准路径证据继续保留。

## 可靠性探针与替代系列（workflow 21-29 与 Risk 23-43，2026-08-07）

针对证据不足或容易假绿的边界建立三类 case：可安全重放的能力用 direct workflow；Lark transport/callback 用 channel case；依赖外部副作用目标或外部配置的能力用 risk case。该系列的集中验证 Ready 镜像为 `4c0596c7`。机器证据：`validation/production-validation-2026-08-06-cases-21-25.json`、`validation/production-validation-2026-08-07-cases-26-28.json` 与 `validation/risk-validation-2026-08-06.json`。

- 静态校验：29/29 workflow 通过 `validate_workflows.rb`（含 21 窗口常量、23 `risk: read_only` 契约、24/25 探针标记与 `side_effects: true` 诚实断言、26-28 组合原语契约和 29 发票 no-submit 路由契约）；29/29 skill 通过 `validate_skills.rb`；risk-cases 21/21 通过 `validate_risk_cases.rb`。
- Preview：21-29 全部通过 interactive explicit-request preview。关键契约：23 被准入封为 `effectiveRisk=read_only`、`approvalRequired=false`、`approvalEnforcement=none`；24/25 为 `write` + `bind_time_confirmation_and_run_time_tool_approval`；26 带一个固定 inline definition，27/28 为零 external call site；29 精确包含审批历史 GET 与 attested read-only contact POST。
- 真实终态按 clean isolated materialization 的最新运行计算为 9/9：21/22/23 分别达到 7/7、8/8、4/4 committed completed 且严格 artifact 命中；24/25 最近一次仍分别以 1 次、2 次 typed approval resume 达到 5/5、8/8 completed；26/27/28 在 Ready `4c0596c7` 上达到 5/5、14/14、6/6 completed；29 在 Ready `6a656d75` 上达到 11/11 completed、0 approval/side effect。共享 `build/workflows` 被示例配置覆盖时的三次 `NYXID_PROXY_HTTP_400` 只保留为无效物化负面对照，不再作为平台回归。
- 准入探针（`scripts/run_admission_probes.rb`，risk-cases 33-36）：33 缺失 capability 在 provision 即被 `NYXID_OPERATION_SELECTION_REQUIRED` 拒绝（passed）；35 合成 n8n 导出在 preview 被 `Unsupported workflow YAML root field 'nodes'` 类型化拒绝（passed）；36 durable 写准入保持 `DURABLE_AUTHORIZATION_UNAVAILABLE` fail-closed（passed）。**34 已由红转绿并改写期望契约**：平台把 path 拆成两个单义字段——selector 的 `path_template` 必须逐字静态且进入 `request_contract_digest`，运行期只为其中已声明的槽位提供值，值本身允许来自 workflow 表达式（canon `workflow-primitives.md` 的示例即 `{"path_params":{"object_id":"${input}"}}`）。issue #2944 当年被拒的是 legacy 的 raw `path` 运行参数（同一字段既当准入模板又当具体路径），修复方案 #2984 方案 C / PR #2996 明确保留 `path_params` 取值模板化，#3071 的 `nyxid_request` 契约示例同样如此。因此 2026-08-06 的观测（放行到 provider、仅 `NYXID_PROXY_HTTP_400`、无外部写入）是契约一致行为。三条子探针已于 `09:14:57Z` 全部实测：正例槽位取值经准入放行；内联表达式的 `path_template` 在 preview 被 `NYXID_OPERATION_SELECTION_REQUIRED` 拒绝（模板化 selector 匹配不到精确 operation，等价于"selector 必须逐字静态"）；含分隔符与 dot segment 的槽位值在调用 provider 之前被 `NYXID_OPERATION_PATH_PARAMETER_INVALID` 拦下。即"路由由 admitted `path_template` 固定、运行期只能填已声明槽位且槽位不可越段"三条同时成立。
- 维护注记：21 的 active 窗口常量止于 2027-08-03，到期前必须重基线（`validate_workflows.rb` 钉住常量，改动需同步）；24/25 每次真实运行会在资产盘点表新增带 `runtime-approval-probe-` / `sequential-approval-probe-` 前缀的可清理记录，仅在显式 `--allow-side-effects` + `--approve` 下执行。三轮成功复验累计 9 条，本轮未获清理授权。
- 独立多模型审查后的加固（均已在生产重跑复验，24/25 仍 `approvalResumeCount=1/2` 通过，33/35/36 在全新 member 上复现 fail-closed）：preview 契约的多值断言改用显式 `AcceptedValues` 封装，避免把 14/17 的精确数组 `allowedExecutionModes: ["interactive"]` 误判成成员包含而永久判红；`probe_note` 由长度校验升级为 `number()` 数值守卫（bounded 模板只提供 `append/data/date/get/number/json/keys/round`，无 `string.*`，而 `number()` 可解析字符集不含引号、反斜杠与花括号，从而封死注入 tool arguments 的路径），并由 `validate_workflows.rb` 钉住；探针 sanitizer 补齐 Lark `tbl/rec/vew` ID 脱敏并在截断前还原稳定错误码，`validate_risk_cases.rb` 的原始身份扫描同步扩到该 ID 类且覆盖 `validation/` 下全部机器证据；准入探针每轮使用新 nonce 建 member 并对绑定投影延迟重试，避免复用旧绑定或把传输层失败误记为"平台已拦截"。

| Risk Case | 验证目标 | 严格状态 | 关键证据 / 缺口 |
|---:|---|---|---|
| 23 | Lark workflow 工具拒绝 | `passed` | Ready `4c0596c7` 上唯一 fresh run 从 `awaiting_tool_approval` 进入 committed failed；拒绝只分发一次，稳定 `approval_denied`，下游未执行、无 final artifact、未保存 raw ID |
| 24 | Lark 附件经 catalog 启动 | `passed` | public skill `1.1` 精确解析；隔离基线后目标 run 数量 `6 -> 7`，唯一新增 run hash `03c3f4ded68e` committed `completed`、state 32、4/4，typed artifact 精确命中 |
| 25 | Lark sender service scope | `passed` | 同 sender 的 fresh Case 22 已解析精确 skill，运行期批准后同一 run committed completed；contact artifact 精确命中，历史 `NYXID_PROXY_SERVICE_SCOPE_FORBIDDEN` / `NYXID_PROXY_UNAUTHORIZED` 均未出现 |
| 26 | `/api/chat` 安全 code_execute | `passed` | Ready `6558db8d` 上 Ornn search、精确 skill mount、workflow start 与 committed artifact 全部通过；run `6fa89cd62b15` 和 direct run `6659aabee079` 均为 state 31、4/4 completed，UserService `/execute` 固定探针同步通过 |
| 27 | Ornn public catalog 完整性 | `passed` | 29/29 格式、精确名称、version 与 public readback 全部一致，独立 verify-only 未做写入 |
| 28 | 当前部署精确源 P1 v5 `submit=false` | `passed` | exact source + sanitized PNG 只 invoke 一次；14/14 completed，写入/审批分支未执行 |
| 29 | P2 四表 schema 保真 no-send | `not-configured` | 未配置 disposable 合成财务表，拒绝 shared Base 语义替代 |
| 30 | 源 no-send Durable schedule cleanup | `not-configured` | 源 target/authority 未配置，公开 Case 15 不作替代 |
| 31 | 全 direct artifact contract | `passed` | 29/29 strict contracts，Assistant 复用同一契约 |
| 32 | 报告与仓库计数一致性 | `passed` | workflow、skill、contract、README、Markdown、HTML、机器摘要统一为 28 |
| 33 | 缺 capability 的 provision 准入 | `passed` | `NYXID_OPERATION_SELECTION_REQUIRED` fail-closed，无外部写入 |
| 34 | path 槽位契约（路由由 template 固定） | `passed` | 三条子探针实测：槽位取值放行且路由未改写（仅 provider 侧 `NYXID_PROXY_HTTP_400`）；模板化 selector 被 `NYXID_OPERATION_SELECTION_REQUIRED` 拒绝；越段槽位值被 `NYXID_OPERATION_PATH_PARAMETER_INVALID` 拦下；无外部写入 |
| 35 | n8n 导出摄入拒绝 | `passed` | preview 类型化拒绝 root field `nodes` |
| 36 | Durable write fail-closed | `passed` | `DURABLE_AUTHORIZATION_UNAVAILABLE`；interactive 对照可准入 |
| 37 | `workflow_call` inline definition resolution | `passed` | Case 26 fresh run hash `07f1fc930866` committed completed、state 44、5/5；inline definition bound、child resolved/started、artifact 命中 |
| 38 | `parallel_fanout` 确定性 runtime | `passed` | Case 27 fresh run hash `5b0e99a928f5` committed completed、state 86、14/14；3 个 worker receipt 与 merge order 命中 |
| 39 | `race` 确定性 runtime | `passed` | Case 28 fresh run hash `ebe447a8479b` committed completed、state 38、6/6；first success 与 later completions ignored 命中 |
| 40 | 源 P2 send | `not-configured` | exact source/hash 已钉住；缺一次性接收目标和本次显式发送授权，未执行 |
| 41 | 源 P1 v6 submit | `not-configured` | exact source/hash 已钉住；缺安全合成审批目标与清理方案，未执行 |
| 42 | 源 P1 v2 legacy | `skipped-expired` | 保留 exact source/hash 与旧要求；由 Case 29 原生 no-submit feature 替代，static/preview/11/11 committed artifact/Ornn public 全部通过 |
| 43 | materialized workflow 输出隔离 | `passed` | example/local 分别输出到独立目录；stale 文件清理、源/产物集合一致，isolated local 目录的 21-23 preview 全部通过 |

## 新增能力证据

### 11 managed `codex_exec`

固定 probe 与公开 canonical sample payload 完全一致，账号 readiness 为 enabled、eligible、active、`execution_ready=true`。历史镜像 `0c4ff023` 上两次真实运行都在 `execute_probe` 以 `codex_execution_admission_denied` committed failed；根因是 Aevatar 把 managed Agent Key 放入 `Authorization: Bearer`，NyxID 的 `forward_access_token` 策略漂移后又把同一 bearer 转发到 chrono-sandbox。提交 `f7f543c51` 改为专用 `X-API-Key` 入口认证并保持 `Authorization` 缺失。镜像 `f7f543c5` 上的定向复验 committed `completed`，`stateVersion=179`，30/30 步完成，固定输出 `CODEX_EXEC_READY`、五项 typed gate、脱敏 `diagnostic_id` 与 `parallel_check_count=5` 全部命中，`side_effects=false`。

### 12 安全 `code_execute`

固定 JavaScript 只计算合成结算金额，不接收任意代码、路径、凭据或环境变量输入。静态契约和最近 production preview 通过。Ready `6558db8d` 上 UserService 为 `forward=true`、`inject=true`、scope=`proxy:*`；NyxID `/execute` 固定合成探针 6 秒成功。fresh direct run `6659aabee079` 与 `/api/chat` run `6fa89cd62b15` 均 committed `completed`、state 31、4/4，并命中 `structured_receipt=true`、`total_cents=16623`、`side_effects=false`。同部署首次 direct run `c257460dc47a` 的 `NYXID_PROXY_HTTP_524` 保留为瞬时 capacity 对照，不覆盖随后严格成功。

### 13 发票 OCR 与规则审查

使用仓库内合成 PDF/PNG，提取供应商、金额、币种、日期和发票号，断言 `vendor_key=harbor-cloud`、`amount_minor=123450`、`currency=SGD`，并读取审批历史验证精确发票号和同供应商匹配数。真实 run committed `completed`，`stateVersion=82`，没有创建审批。

### 14 Lark contact 批量解析

工作流精确调用 `POST /open-apis/contact/v3/users/batch_get_id`，只输出解析计数和布尔值，不输出真实联系人 ID。最新 run committed `completed`，`stateVersion=25`，3/3 步完成，业务 artifact 仍正确；但 preview 明确 `approvalRequired=true`，本轮却没有观察到 typed pending/resume，所以严格判定为 `TOOL_APPROVAL_IDENTITY_NOT_OBSERVED`。镜像 `8cf280e2` 上 state 28 的历史批准路径仍保留为对照证据。

### 15 周度/月度预算差异摘要

六路 Base GET 后对合成财务数据计算周度实际、预算、超支与观察类别，并生成四周月度投影。镜像 `b010ba61` 上的直接 run committed `completed`，`stateVersion=73`；周度 2340/2400、月度 9360/9600、`over_count=1`、`watch_count=1` 均命中。schedule 示例仍提供每周一和每月一日的正式 Cron，本次生产 canary 使用每分钟 Cron 完成 confirmation、HTTP 202 typed receipt、binding/provisioning committed success 与 schedule 回读。六次真实 cron 触发均无失败，抽查 workflow 11/11 committed `completed`，最终 artifact 与直接运行断言一致；DELETE 后 list 消失，跨下一分钟 run count 未增长。

### 16 NyxID 只读 provider receipt

单次读取验收 Base 的一页记录。Production preview 精确确认一个 `get` 调用点、`effectiveRisk=read_only` 且无需批准；真实 run committed `completed`，`stateVersion=31`，4/4 步完成，首个 tool step 输出非空，最终 artifact 同时包含 `success=true`、`provider_response_verified=true` 和 `side_effects=false`。它回归了 #3161 最初的 managed-workflow `tool_outcome_unknown`；随后源 P2 no-send 又补齐了 published-operation authority 分支的 6 个只读调用和 8/8 committed 完成证据。

### 17 POST 搜索批准恢复

调用语义只读的 Lark Base `records/search` POST，不创建或修改记录。Production preview 仍确认单次 `post`、`effectiveRisk=write`、`approvalRequired=true`；最新 run 却未出现 typed pending/resume 即 committed `completed`，`stateVersion=31`。即使 artifact 写有 `approval_resumed=true`，也不能替代运行时身份链证据，因此严格判定为 `TOOL_APPROVAL_IDENTITY_NOT_OBSERVED`。历史 state 34 run 曾完整消费 nested `toolApproval`，继续保留；当前回归使拒绝路径不可达，durable preview 仍未验证。

### 19 Lark Bot 文件上传验证

使用仓库内 114 字节合成 JSON，通过 `input_file_refs -> document_extract` 验证文件名、媒体类型、字节数和固定 SHA-256，全程不调用 Lark 写接口。Production preview 已通过且为 0 个外部 call site；public skill `1.1` 已精确回读。Direct run hash `e6d17331400b` committed `completed`、`stateVersion=30`、4/4，并按入口分层得到 `lark_bot_ingress_validated=false` 和 114 字节 LF 变体。fresh Lark canary 在 6 条既有目标 run 的隔离基线上只新增 1 条；run hash `03c3f4ded68e` committed `completed`、`stateVersion=32`、4/4。Lark 文件卡片显示 114 字节，下载后的 committed descriptor/extraction 因尾随 LF 归一化为 113 字节；artifact 精确满足 `success=true`、文件登记/抽取/内容匹配、`lark_bot_ingress_validated=true`、文件名/正文/SHA-256 匹配、脱敏和 `side_effects=false`。历史 `service_catalog_missing` 与不完整 artifact 只保留为恢复路径证据，不覆盖最新严格通过。公开摘要不保存文件消息、resource key 或其他 opaque ID。

## Ornn skills

`skills/` 下有 29 个与 workflow 一一对应的 skill。Ornn 服务端只接受 `SKILL.md`、`scripts/`、`references/`、`assets/` 等根目录，因此 workflow 放在 `assets/*.yaml`。本地同步器保证 asset 与公开 workflow 字节一致；Case 26 还携带固定 inline 子定义 asset。

截至 2026-08-06 的 missing-only 发布证据：

- 29/29 个 ZIP 均通过 Ornn `/api/v1/skill-format/validate`；
- 原 25 个名称回读到预期版本和 `isPrivate=false` 后精确跳过；
- 只创建并公开新增的 `vendor-policy-inline-delegation`、`deterministic-parallel-evidence-review` 和 `deterministic-race-policy-review`；
- 新增 3 个逐项回读名称、版本和 `isPrivate=false` 一致，未记录 GUID。
- 发布后的正式 verify-only 与独立 catalog 验证器均确认 29/29 精确一致，缺失、private 和版本不匹配集合均为空。

发布命令：

```bash
ruby scripts/materialize_workflows.rb config.local.yaml
ruby scripts/sync_skills.rb config.local.yaml
ruby scripts/package_skills.rb config.local.yaml
ruby scripts/publish_skills.rb
ruby scripts/publish_skills.rb --verify-only
```

## 自然语言调用示例

以下提示适用于 Lark Bot 或 `/api/chat`。为避免模型只回答业务问题，每条都明确要求搜索 Ornn skill、实际运行 workflow，并以 typed artifact 为准。

| # | 示例提示 |
|---:|---|
| 01 | 帮我做一次上线准备度审查，寻找合适的 Ornn skill，实际运行 workflow，并以 typed artifact 报告结果。 |
| 02 | 检查这组候选人材料是否齐全，使用 Ornn skill 运行材料合规预览，不要输出敏感原文。 |
| 03 | 审计最近一条邮箱访问审批，使用 Ornn skill 读取实例详情，只做只读检查。 |
| 04 | 分析 SaaS 许可证利用率和成本，运行对应 Ornn workflow，给出机器断言。 |
| 05 | 预览资产盘点确认，不要提交；使用 Ornn skill 并报告是否会触发写入。 |
| 06 | 为共享邮箱申请创建审批并回读状态，使用对应 Ornn skill；需要平台批准时先停下。 |
| 07 | 预览季度访问审查提醒，不发送消息，使用 Ornn skill 实际运行 workflow。 |
| 08 | 生成 SaaS 许可证优化摘要，不发送卡片，使用 Ornn skill 并读取 typed artifact。 |
| 09 | 检查外包人员访问资料并判断是否重复，不创建审批，使用对应 Ornn skill。 |
| 10 | 预览本月访问认证结果，不发审批和消息，运行对应 Ornn workflow。 |
| 11 | 运行复杂 codex_exec 验收，只接受 `CODEX_EXEC_READY` 和五项 typed gate 作为成功证据。 |
| 12 | 用安全代码执行验收案例核对合成结算，失败时原样返回 typed blocker。 |
| 13 | 从这张合成发票图片提取字段、归一化金额和日期并检查重复，必须使用 Ornn workflow。 |
| 14 | 把验收入职邮箱解析为 Lark 联系人 ID，只返回成功与数量，缺权限时报告 typed blocker。 |
| 15 | 生成合成预算的周度和月度差异摘要，不发送消息，使用 Ornn skill 并读取 typed artifact。 |
| 16 | 运行 NyxID 只读回执探针，只接受 committed typed artifact，不执行写入。 |
| 17 | 运行语义只读的 Base POST 搜索批准恢复探针；批准由 bind 时的 explicit-request confirmation 兑现。 |
| 18 | 运行无副作用的供应商控制项自证审查，覆盖 guard、conditional 与 while 三个确定性原语。 |
| 19 | 验证当前消息中的 `lark-bot-upload-manifest.json`，运行对应 Ornn workflow；只有 typed artifact 的 `lark_bot_ingress_validated=true` 才报告 Lark Bot 入站通过。 |
| 20 | 运行无副作用的供应商风险分档汇总，覆盖 map_reduce 与 cache 两个确定性原语。 |

可复现 `/api/chat` 验证：

```bash
ruby scripts/assistant_validate.rb --cases 01,12,13,14,15 --approve 01,12,13,14,15
```

`--approve` 只会批准验证器从 current state 匹配到的 typed tool approval，不会批准未声明的工具或案例。验证器只保存哈希、工具名、typed 错误码和脱敏摘要，并显式输出 `chatCompleted`、`workflowValidationStatus`、`workflowValidated`。它不会把 Assistant 回合完成写成 workflow 通过。

## `/api/chat` 与 Lark Bot 的区别

两者会进入相同的 Assistant/AgentRun、工具目录、Ornn 和 workflow 能力，但入口与回传链不同。

| 层次 | `/api/chat` | Lark Bot |
|---|---|---|
| 入站 | 已登录用户经 NyxID 代理直接 POST SSE | Lark webhook 经 NyxID channel relay |
| 身份 | 当前 NyxID 用户和 scope | Bot 注册、会话映射、发送者解析与 agent identity |
| 输入 | 文本和类型化 `inputParts` | Lark 消息、附件与平台事件转换 |
| 回传 | SSE 直接返回调用方 | Agent 回复经 relay 再发送到 Lark |
| 能证明 | Assistant、Ornn、tool、workflow 核心链 | 还需额外证明 webhook、relay、映射和 Lark 回传 |

所以 `/api/chat` 成功仍不能外推 Lark Bot transport；反过来，Lark 中出现回复也不能证明 workflow completed。17:44 的 Aevatar Bot 实测已经补齐 transport 证据：NyxID committed Bot 为 `active`、`webhook_registered=true`，生产日志观察到 inbound callback、发送者绑定、channel agent run、Ornn 搜索、Lark 回传和 conversation turn completed。该次 workflow 本身未通过，Bot 返回 skill mount、`AgentNotFound` 和 capability admission 三类失败回执。

历史上，生产镜像 `8cf280e2` 的两次 Lark Bot contact 重试和 `e30fdd94` 的 fresh `/init` 后重试均在 `resolve_contact` 以 `NYXID_PROXY_SERVICE_SCOPE_FORBIDDEN` committed `failed`，用于定位 authorization-code resource narrowing。这些历史失败不覆盖当前 fresh channel 结果：Cases 20/21/22 已分别严格通过 mount 批准、mount 拒绝和 workflow 运行期工具批准，Risk 23 也在 Ready `4c0596c7` 上取得稳定 `approval_denied` 的拒绝证据。现行结论只覆盖仓库声明的合成目标和精确 UserService grant，不外推到其他 Lark 业务资源。

## 当前 `/api/chat` 结果

- 01：最近 Ready `6a656d75` fresh 严格状态 `validated`，run `535e9029486f` 为 13/13、state 80，`ready_for_review=true`、`side_effects=false`。
- 12：Ready `6558db8d` 上 fresh 完成 Ornn search、精确 mount、workflow start 与 artifact read；run `6fa89cd62b15` committed `completed`、state 31、4/4，`total_cents=16623`、`side_effects=false`，严格状态为 `validated`。
- 13：严格状态 `validated`，图片 file ref 进入真实执行，12/12 步完成，`success=true`，`stateVersion=82`。
- 14：Ready 镜像 `ee031038` 上 fresh 严格状态 `validated`，workflow committed `completed`，`success=true`、`resolved_count=1` 且联系人标识未回显。Assistant 最终文案仍描述旧 Awaiting 状态，严格判定只采用 committed artifact。
- 15：在生产镜像 `d7844b5e` 上严格状态 `validated`，六路 Base 读取与周/月差异断言通过，11/11 步完成，`stateVersion=73`。Assistant 最终读取并报告 committed typed artifact，`artifactPendingReportedAsFinal=false`。

历史五案例在 Ready `ee031038` 上为 5/5 `validated`；最近代表对照现为 2/2：Case 01 保留最近严格成功，Case 12 已在 Ready `6558db8d` fresh 严格恢复。两项都完成 Ornn search、精确 skill mount、workflow start 与 committed observation。公开 SSE 中成功工具结果仍可能只有通用 `completed`，所以验证器使用 typed run identity 查询 workflow current state；它不会把 Assistant 文案改写成机器终态。

issue #3182 的历史症状不能再作为“Ornn 或聊天当前整体阻塞”的证据：Case 01 已在最近验证的 Ready 上 fresh 严格通过；案例 14 在 `8cf280e2` 上完成 Ornn mount/run 复测，案例 15 又在 `d7844b5e` 上关闭短 run ID 到 artifact actor identity 的最终 pending 误报。Lark webhook、NyxID channel relay 和 Lark 回传已有独立证据，Cases 20/21/22 又分别严格通过 mount 批准、mount 拒绝和 workflow 运行期工具批准；早期 sender binding/resource narrowing 失败只保留为历史诊断。

## 配置与本地验证

将 `config.example.yaml` 复制为已忽略的 `config.local.yaml`，替换 16 个占位符，然后执行：

```bash
ruby scripts/materialize_workflows.rb config.local.yaml
ruby scripts/validate_workflows.rb
ruby scripts/validate_channel_cases.rb
ruby scripts/sync_skills.rb config.local.yaml
ruby scripts/validate_skills.rb
ruby scripts/package_skills.rb config.local.yaml
ruby scripts/validate_report.rb
```

新增案例的 production preview 与只读运行入口：

```bash
ruby scripts/materialize_workflows.rb config.local.yaml
ruby scripts/production_validate.rb --cases 16,17,19
ruby scripts/production_validate.rb --cases 16 --run
ruby scripts/production_validate.rb --cases 17 --run --approve-read-only 17 --approve
ruby scripts/production_validate.rb --cases 19 --run
```

Case 17 必须逐行消费 SSE 并在 pending 出现时立即发送 nested `toolApproval` resume；`202 Accepted` 不是终态成功证据。如果 preview 要求批准但运行没有产生 typed pending，必须记为契约回归，不能用 artifact 的 `approval_resumed=true` 代替。拒绝路径和 durable preview 需要单独的新 run。

Case 19 的 `--run` 仍只验证 direct synthetic fixture，预期 `lark_bot_ingress_validated=false`；真实 Lark canary 则已从唯一 fresh committed run 的 typed artifact 读取到 `lark_bot_ingress_validated=true`。这两层证据不能互相替代，Bot 回复文案和文件卡片也仍不参与 workflow 成功判定。当前通过仅适用于公开合成 fixture 的三个精确规范变体：113 字节无换行、114 字节 LF、115 字节 CRLF，不接受模糊正文匹配。

`build/`、`tmp/` 和 `config.local.yaml` 均被忽略。合成附件位于 `fixtures/`，schedule 示例位于 `schedules/`。

## 生产验证边界

- 所有生产请求必须使用 `nyxid proxy request aevatar ...`；不得直连后端、复制 bearer 或借用浏览器 session。
- 一律先 preview。写入分支需要明确的用户意图、允许列表和 typed tool approval。
- 平台工具批准不等于 Lark 业务审批；新建审批通常仍是 `PENDING`。
- 只有 typed receipt、run ID、业务断言和 committed terminal evidence 齐全，才可写成 workflow 通过。
- managed `codex_exec`、typed approval 和 schedule 的现有边界不得通过 mock 成功结果或业务 artifact 掩盖；contact 的业务成功必须同时核对批准身份链与脱敏断言。
- #3161 已关闭；Case 16 覆盖共同 receipt/runtime 平面，源 P2 no-send 又在当前部署上覆盖 published-operation authority 主链并 committed 完成。该结论只适用于 no-send 只读执行，不外推到消息发送或 durable schedule。
- #3184 仍开放；Case 17 的历史 run 证明过批准 resume，但最新 `0c4ff023` run 未观察到 typed pending/resume，当前记为契约回归。拒绝路径在此状态下不可达，durable preview 仍需独立证据。
- Case 19 已有静态、preview、direct committed 与 fresh Lark committed 4/4 证据；public skill 版本为 `1.1`，最新 run hash `03c3f4ded68e` 的 typed artifact 明确给出 `lark_bot_ingress_validated=true`，114 字节文件卡片与 113 字节 committed extraction 的尾随 LF 归一化已分层记录，Risk 24 严格通过。
- Case 20/21 已在包含 `de801ca70` 的生产 Ready workload 上分别严格通过首次 skill mount 批准和拒绝路径；Case 22 的已挂载 workflow 运行期批准路径也已通过。三项证据互不替代；旧 `InvalidWorkflowYaml` 只保留为“使用”提示词的历史负证据，不能覆盖 fresh channel 结果。
- 不得提交 token、真实组织标识、业务载荷、审批表单或未脱敏运行证据。

新增和维护案例的完整规则见 [AGENTS.md](AGENTS.md)。

## 许可证

MIT。`LICENSE` 保留标准英文法律原文。
